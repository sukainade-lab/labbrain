# Score — feat/s14-design-revamp — 2026-06-06

> **Cross-cutting UX initiative**, not BRD story S14 (the backlog "Slack/email weekly digest").
> Front-end design-language revamp: dark theme → polished light navy+amber design system,
> elevated to 10/10 UX per the founder's request, matching the Team Capacity Portal reference.
> Presentation-only — zero business-logic / data-path / API change.

| Hat | Score | Notes |
|-----|:-----:|-------|
| Product | 9 | Polished, trustworthy UI directly serves the ICP (Quality Manager who shares 375px WhatsApp demos; credibility matters for a compliance product). The product safety contract is untouched — source-traced retrieval, citations, RLS all unchanged. −1: polish, not a new BRD capability. |
| Architecture | 10 | Exemplary token architecture: a single design-token source (`tailwind.config.ts` + `globals.css`) with every component consuming **semantic** tokens (`bg-canvas`, `bg-card`, `border-line`, `text-navy/ink/muted`, `brand-amber`, `*-soft`/`*-strong`) instead of raw hex. One knob per concept; the theme is now centrally tunable. |
| Engineering | 9 | Pure presentation; 571/571 existing tests still green, tsc/eslint/`next build` clean, no `any`, no secret prefixes. `migration/view.ts` statusClass swapped to light tokens with existing tests asserting presence (not exact strings). −1: no automated visual-regression guard for token drift (axe is the only automated UI gate). |
| QA | 9 | Full suite 571/571; **live `npm run axe:walk` @375px (wcag2a+aa) = 0 violations** across `/dashboard /documents /admin /founder /settings` (satisfies L7). Error/empty-state styling preserved on every form (role=alert / role=status pills retained). −1: axe walk covers the 5 gated app surfaces, not marketing/auth pages; no per-surface screenshot diff. |
| UX | 10 | The heart of the change and the explicit goal. Light navy+amber system with KPI/card accent stripes, soft layered shadows, hover-lift, refined tnum typography, navy gradient topbar with radial amber glows. RTL-first identity fully preserved (IBM Plex Arabic, all Arabic copy); `<bdi>` 8/8 + `dir=` 1/1 preserved, `aria-*` **+7/−0** (a11y improved). AA contrast honored (brand-amber #B45309 / hover #92400E only behind white; bright amber accent-only). ≥44px tap targets. 0 axe violations @375px. |

**Composite: (9 + 10 + 9 + 9 + 10) × 2 = 94 ✅**

**Decision: Ship**

Matches the characterized story-mix signal — net-new *visible* UI lands ~94 (cf. S12 branding 94),
vs ~90 for mock-tested infra/seam/data work.

## Caps checked (none bit)
- **L6** (Eng cap 8 — literal secret prefix in tracked files): secret scan clean → not triggered.
- **L7** (QA cap 9 — UI story without cited manual 375px + axe walk): live axe walk @375px 0 violations **cited** → not triggered.
- **L5** (UX cap 8 — dynamic Latin/digit in RTL without bidi isolation): `<bdi>` preserved, aria improved → not triggered.
- **L1/L3/L4** (route/CTA caps): no new API routes or CTAs → not applicable.

## Gate evidence
- tsc --noEmit: clean
- eslint: clean
- next build: success
- vitest: 571/571 passing (66 files)
- axe:walk @375px (wcag2a+aa): 0 violations across /dashboard, /documents, /admin, /founder, /settings
- Elegance pause: present in commit `8f47076`
- Secret scan: clean · `any` types: none added · RTL: bdi 8/8, dir 1/1, aria +7/−0

## Next
`/7-eo-ship` — **STOP at merge gate.** Per project ops rules, push to remote / PR merge requires
explicit founder authorization naming the PR. No direct push to `main`.
