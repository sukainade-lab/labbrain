# EO Score: S7 — SMS 2FA via Unifonic · 2026-06-02

**Branch:** feat/s7-sms-2fa
**Commit scored:** f08e951 (+ verify-form `⚠️` non-color-signal fix applied during scoring)
**BRD:** architecture/brd.md (AC-7.1…7.6) · plan: docs/handovers/plan-S7-sms-2fa.md
**CI:** PR #9 — Quality Gate + live integration both green (run 26808384508)
**Composite:** 96/100 | **Decision: ✅ Ship**

| Hat | Score | Notes |
|-----|:-----:|-------|
| Product | 10 | All 6 ACs traced (61 @AC-7.x tests). Diff tightly scoped to the 2FA surface, zero feature creep. End-to-end path wired + reachable: `/settings` in nav-links, enroll/disable/verify all have UI callers (no orphaned routes), login routes a 2FA user to `/login/verify`, disable requires a fresh OTP. Strong MENA fit (Arabic SMS body, Jordan `07…`→`+9627…` normalization, RTL + `<bdi>`). Out-of-scope (TOTP, backup codes, WhatsApp OTP, tenant-enforced policy, remember-device) explicitly deferred. |
| Architecture | 9 | Exemplary module separation: pure helpers (phone/otp/mfa-cookie) vs DB orchestration (mfa.ts) vs thin route handlers vs swappable fail-closed SMS seam. Data flow explainable in 2 sentences. `mfa_challenges` RLS-enabled-no-policies = service-role-only; FK cascade; index matches query order. Cookie-based elevation vs Supabase AAL2 is a deliberate, documented decision. −1: no explicit growth/TTL-pruning note for `mfa_challenges` rows (Q8 scalability). |
| Engineering | 10 | SMS seam fails closed on every mode (missing key / non-2xx / `success:false`). `crypto.timingSafeEqual` for OTP + cookie compares; HMAC-SHA256; httpOnly+secure cookie. `evaluateVerify` ordering (consumed → exhausted → expired → compare) fails closed, no timing leak on dead challenges; dead-challenge row deleted on SMS failure. Zero `any`/`@ts-ignore`. Substantive Elegance Pause block in the feat commit. L6: no secret prefixes in any S7-introduced file (verified). |
| QA | 9 | Happy + empty + error + edge across all ACs. L1 satisfied — story-7-2fa-routes.test.ts POSTs to the real handlers for 400/401/429/200 on every route. L2 satisfied — live story-7-mfa-db suite runs `--no-file-parallelism` (ci.yml:76). Fail-closed paths tested (SMS throws; wrong code → 401 no cookie; rate-limit → 429); live suite asserts hashed-never-plaintext, single-use, attempt-increment, RLS opacity, cooldown. −1: verification is automated/CI only — no manual 375px/axe walk cited. |
| UX | 10 | Consistent RTL (global `dir="rtl"` + IBM Plex Arabic), ≥44px targets, loading/disabled/error/empty states, `inputMode`/`autoComplete="one-time-code"`, conversational Jordanian Arabic. L5 satisfied — phone `<bdi dir="ltr">`, OTP inputs `dir="ltr"`, inline "6" in `<bdi>`. The one flagged defect (verify-form error was color-only) was **fixed during scoring** — it now carries the `⚠️` non-color signal matching mfa-settings. |

**Composite: (10 + 9 + 10 + 9 + 10) × 2 = 96** ✅ (no hat < 8)

## Lessons caps — none applied
- L1 (QA): route-handler integration coverage present → not capped.
- L2 (QA): live suite serialized in CI → not capped.
- L3 (Architecture): CI confirmed green on PR #9 (retro P1) → not capped.
- L4 (Product): /settings reachable, login→verify routing confirmed, no orphaned routes → not capped.
- L5 (UX): `<bdi>` + `dir="ltr"` on all dynamic mixed-script values → not capped.
- L6 (Engineering): no real-looking secret prefix in any S7-introduced file → not capped.

## Gaps (non-blocking, deferred to backlog)
1. 🟢 Architecture: add a TTL/pruning job (or note) for consumed/expired `mfa_challenges` rows before scale.
2. 🟢 QA/UX: capture a manual 375px + axe-core walk of `/login/verify` and `/settings` as PR verification evidence.
3. 🟢 Product: WhatsApp OTP channel is the most native fit for the WhatsApp-first ICP — consider as a follow-up to SMS.

## Next action
✅ Ship (96 ≥ 90, no hat < 8). Run `/7-eo-ship` for S7 — **stop at the merge gate; founder must authorize the merge of PR #9 by name** (PR is not auto-merged to main).
