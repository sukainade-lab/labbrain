# Score — S1 Lab Onboarding & Auth — 2026-06-01

**Branch:** main · **Commits:** 3b84de7 (impl) + 353e66c (review fixes)
**ACs:** AC-1.1…1.6 (6/6 implemented, 6/6 @AC-tagged) · **Tests:** 20/20 passing
**Gates:** `next build` ✓ · `eslint .` ✓ · `npm audit --audit-level=high` ✓ (Next 16)

| Hat | Score | Notes |
|-----|:-----:|-------|
| Product | 9 | All 6 S1 ACs delivered. Product safety contract (tenant isolation) enforced + tested. Bilingual RTL, JOD plan tiers, Jordanian register. −1: the post-verification `/onboarding` is a simple landing, not the 4-step wizard in `onboarding-flow.jsx` — correct, since steps 3–4 (plan/invoice) are S4 scope, but the end-to-end journey is partial until S4. |
| Architecture | 9 | Clean layering: validation → auth logic → supabase clients (anon/server/admin) → routes → components. Pure helpers (`safeNext`, `mapAuthError`) extracted for testability. Service-role client isolated server-side with explicit guardrails. RLS as a real migration (`0002`). Lazy Resend/Stripe clients for build safety. −1: seat-counting logic is duplicated across `assertSeatAvailable` (accept-time) and `getSeatUsage` (create-time); `confirm` route handles only the `?code=` (PKCE) link shape. |
| Engineering | 9 | TypeScript strict, no `any`, no hardcoded secrets, env via gitignored `.env.local`. Test-first. Typed `SignupError` → HTTP status mapping (402/409/400/500). Orphan-tenant rollback on signup failure. Invite acceptance bound to invited email. −1: a couple edge cases deferred (`token_hash` link fallback). |
| QA | 8 | 20/20 green, 100% AC coverage, **live** RLS isolation tests (never mocked) — above-average rigor for the security-critical path. Regression tests added for the bug found in review. −2: **no integration test exercises the API route handlers** — the invite `labName` bug reached review precisely because every test calls `provisionSignup`/schemas directly, bypassing the HTTP seam. Pure schema tests now cover the symptom, but a route-handler regression (form↔schema↔route wiring) still wouldn't be caught. |
| UX | 9 | RTL-first, IBM Plex Arabic, matches artifact tokens (Navy `#1B2A3D` / Amber `#D97706`/`#F59E0B` / BG `#0F172A`), BiDi isolation for emails, Jordanian Arabic copy, loading/disabled + error/success states, responsive (`max-w-md`, invite form stacks at mobile). −1: no explicit 375px screenshot verification (WhatsApp-share viewport per CLAUDE.md). |

**Composite: (9 + 9 + 9 + 8 + 9) × 2 = 88**

**Decision: 🩹 Bridge gaps (80–89)** — lowest hat is **QA (8)**. Add route-level integration tests for `/api/auth/signup` (new-lab + invite paths) and `/api/invitations` (authz + seat limit) so the HTTP seam that hid the `labName` bug is covered. Then re-score.

## Gap to close (→ `/6-eo-bridge-gaps`)
1. **QA — route-handler integration coverage** (the −2): POST-level tests for signup (new-lab 201, invite 201, duplicate 409, seat_limit 402, bad-body 400) and invitations (401 unauth, 403 member, 201 owner, 402 over-limit).
2. *(Optional, lifts Arch/Eng)* shared seat-count helper; `token_hash` fallback in `confirm`; a 375px UX screenshot check.

Closing #1 alone lifts QA 8→9 → composite **90 ✅ Ship**.
