# Score — feat/s12-branding (vs main) — 2026-06-02

**Story:** S12 — Multi-tenant branding (lab logo in app header)
**PR:** #23 (feat/s12-branding → main) · CI green: run 26830185748 (quality ✓ + integration ✓)
**Scope:** AC-12.1 … AC-12.8 (docs/stories/S12.md)

| Hat | Score | Notes |
|-----|:-----:|-------|
| Product | 9 | Full reachable loop (L4): admin-only `BrandingSettings` in `/settings` → `POST/DELETE /api/branding` → header reads `logo_path`. Graceful fallback chain logo+name → name → wordmark. Arabic copy throughout. Holds back from 10 only because remove is button-only (no drag-replace / crop affordance) — acceptable for MVP scope. |
| Architecture | 9 | Tenant-namespaced storage key `{tenant_id}/logo.{ext}` + RLS policy `branding_storage_tenant_isolation` (migration 0013) mirrors documents isolation; public-read bucket, writes RLS-gated. Replace-in-place with rollback on DB failure keeps storage/`logo_path` consistent. CI green confirmed before scoring (L3); `npm audit --omit=dev` clean. Pure `resolveSidebarBrand` resolver isolates the fallback logic from JSX. |
| Engineering | 10 | Layered tests: 11 route-seam (201/400/413/403/401 · 200/403/401), 13 service-layer (mocked admin client — runs anywhere), validation unit, + live RLS-isolation suite (serialized, unique-tenant, cleanup per L2). 547/547 green. Discriminated-union route gate. No `any`, no secret prefixes (L6). `<bdi>` on all mixed-script values (L5). |
| QA | 9 | Every AC has a `@AC` tagged test (review fix added the missing service layer + AC-12.4/12.6 tags). L1 route-seam per branch ✓, L2 serialized live ✓, L7 **live** `npm run axe:walk` @375px = 0 serious/critical across /dashboard,/admin,/founder,/settings ✓, L8 enumerated spec pre-coding ✓. Capped at 9 by L7/L8 ceilings (both satisfied — no further bite). |
| UX | 10 | RTL-first branding section, ≥44px tap targets (`min-h-[44px]`), 375px verified, primary CTA `#B45309` on white = 5.02:1 (AA pass, L9), hover `#92400E` 7.09:1. Logo `<img>` carries Arabic alt + explicit 28×28 dims (no layout shift). Live axe walk 0 violations on the new surface. |

**Composite: (9 + 9 + 10 + 9 + 10) × 2 = 94 ✅**

## Lesson cap audit (no cap bites)

| Lesson | Cap | Status for S12 |
|--------|-----|----------------|
| L1 | QA ≤8 if no route-handler test per branch | ✓ 11 route-seam tests — no cap |
| L2 | QA ≤8 if live-DB not serialized/cleaned | ✓ `describe.skipIf(...).sequential` + cleanup — no cap |
| L3 | Arch ≤8 if CI not confirmed green pre-score | ✓ run 26830185748 both jobs green — no cap |
| L4 | Product ≤8 if route has no UI caller | ✓ BrandingSettings → /api/branding; header reads logo_path — no cap |
| L5 | UX ≤8 if no `<bdi>` on mixed-script | ✓ layout + settings + preview — no cap |
| L6 | Eng ≤8 if secret prefixes in tracked files | ✓ scan clean, no new env vars — no cap |
| L7 | QA ≤9 unless live axe walk cited | ✓ live walk 0 violations cited — ceiling, not a cut |
| L8 | QA ≤9 unless Phase-2 AC enumerated pre-code | ✓ docs/stories/S12.md — ceiling, not a cut |
| L9 | UX ≤9 if AA fail on story CTA | ✓ CTA AA-pass — no cap |

No lesson cap reduces any hat below its honest score. First story since S9 to clear the 90 plateau — driven by the first **live** axe walk on a net-new UI surface (/settings) returning 0 violations.

## Decision: **Ship**

Composite 94 ≥ 90 and no hat < 8. Proceed to `/7-eo-ship story-12` → **stop at the merge gate** (PR #23 merge to main is founder-authorized only).
