# Score — feat/s2-upload (S2 Document Upload & Indexing) — 2026-06-01

| Hat | Score | Notes |
|-----|:-----:|-------|
| Product | 8 | Every AC maps to real ICP value; Arabic-first meaningful error states; cap→402 upgrade prompt. **Gap:** upload→ready loop is a single blocking spinner — `parsing`/`indexing` UI states never render because POST returns only after `ready`; AC-2.2's *visible* progression isn't experienced. No usage/runway counter on the docs page itself. |
| Architecture | 9 | Clean route→orchestrator→seam layering; external deps lazy + injectable (`fetchImpl`). Tenant isolation enforced in Postgres (RLS + `match_document_chunks` filters `tenant_id` before cosine). Coherent checkpoint/cleanup failure path. **Cliff:** inline LlamaParse polling (≤120s) on the request thread — extract to a background job before scaling. |
| Engineering | 9 | No `any`, no hardcoded secrets, lazy clients survive empty-key build. Strong status-code discipline (401/400/413/402/403/404), correct token math + cosine→similarity, storage rollback on failure. **−1:** `embedTexts` length not asserted before `embeddings[i]` map (silent-misalign/opaque crash); status-flip `.update` errors unchecked (stuck-row risk); `deleteDocument` removes Storage before row. |
| QA | 9 | **L1 cap does NOT apply** — `story-2-documents-routes.test.ts` POSTs/DELETEs the real handlers across success + every error status. All 6 ACs @AC-tagged. P0 AC-2.4 proven LIVE (Lab A queried with Lab B's exact embedding → `[]`). Caps + cascade asserted against live DB. **Gap:** `ingestDocument` real failure path (parse/embed throw → 500, partial-ingest cleanup) untested (seams mocked on happy path). |
| UX | 8 | RTL inherited not hardcoded; all 5 status states have natural Jordanian-dialect labels + correct color tokens; tap targets ≥44px with `aria-label`. **−2:** filename lacks BiDi isolation (`<bdi>`/`dir="auto"`) → mixed-script SOP filenames mis-render; cap line dropped the reference's richer "خطة Starter (50) · الاستخدام: X%" context. |

**Composite: (8 + 9 + 9 + 9 + 8) × 2 = 86**

**Decision: Bridge gaps** (80–89 → `/6-eo-bridge-gaps`). Lowest hats: Product (8) and UX (8).

## Path to ≥90
Lift the two 8s to 9+ (each +1 hat = +2 composite; 86 → 90 needs both):
- **UX (8→9, cheapest):** wrap filename in `<bdi>` / `dir="auto"` at `documents/page.tsx:148`; enrich the cap line to show plan + usage context. Mechanical.
- **Product (8→9):** make the status pipeline visible — return the row at `parsing` immediately and let the UI poll to `ready`, so AC-2.2's progression actually renders instead of an opaque spinner. (Larger; alternatively a clearer multi-stage upload affordance.)
- Optional Engineering hardening (already 9): assert `embeddings.length === chunks.length` before the row map.

## Notes
- No new lesson triggered — L1 held and was satisfied for S2. Pattern to watch (not yet a lesson): "async pipeline status columns exist but the request blocks until terminal state, so intermediate UI states are dead" surfaced in both Product and Architecture hats.
