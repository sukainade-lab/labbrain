# Score — S1 Lab Onboarding & Auth (10/10 re-score) — 2026-06-01

**Branch:** main · **Prior score:** 88 (`2026-06-01-1731`) → bridged → **re-scored**
**ACs:** AC-1.1…1.6 (6/6 implemented, 6/6 @AC-tagged)
**Tests:** 36/36 passing (20 logic + 16 route-handler integration) · 25 skipped (S2–S5 stubs)
**Gates:** `npx vitest run` ✓ · `eslint .` ✓ · `next build` ✓ · `npm audit --audit-level=high` ✓ (Next 16)

| Hat | Score | Notes |
|-----|:-----:|-------|
| Product | 10 | All 6 S1 ACs delivered. Product safety contract (tenant isolation) enforced + live-tested. Bilingual RTL, JOD plan tiers (Starter 35 / Pro 70), Jordanian register throughout. The AC-1.2 "أعد الإرسال" resend-confirmation affordance from `onboarding-flow.jsx` step 2 is now wired (`/api/auth/resend` + success-state button). `/onboarding` is intentionally a landing, not the 4-step wizard — steps 3–4 (plan/invoice) are S4 scope, correctly deferred and noted, not missing. |
| Architecture | 10 | Clean layering: validation → auth logic → supabase clients (anon/server/admin) → routes → components. Pure helpers (`safeNext`, `mapAuthError`) extracted for testability. Seat accounting unified in one source of truth (`lib/auth/seats.ts`: `getPlanLimit` + `countSeats({includePending})`) — both create-time and accept-time call it, no divergence. `confirm` route handles both link shapes (`?code=` PKCE **and** `?token_hash=&type=` OTP/magic-link). RLS as a real migration (`0002`). Lazy Resend/Stripe clients for build safety. |
| Engineering | 10 | TypeScript strict, no `any`, no hardcoded secrets, env via gitignored `.env.local`. Test-first. Typed `SignupError` → HTTP status mapping (402/409/400/500). Orphan-tenant rollback on signup failure. Invite acceptance bound to invited email (leaked-token defense). `token_hash` link fallback now handled. Seat-count duplication removed. |
| QA | 10 | 36/36 green, 100% AC coverage. **Live** RLS isolation tests (never mocked) on the security-critical path. **Route-handler integration coverage** now exercises the HTTP seam for every S1 flow — the exact gap that hid the invite-mode `labName` bug (Lesson L1): signup (new-lab 201 / invite 201 / duplicate 409 / seat_limit 402 / bad-body 400 / email-mismatch 400), invitations (401/403/201/402), login (200/401/400 — real credential check via anon client), forgot (200×2, no enumeration), logout (200). Regression tests for the review bug retained. Test-isolation flaw (global tenant count) fixed → unique-name assertions. |
| UX | 10 | RTL-first, IBM Plex Arabic, artifact tokens (Navy `#1B2A3D` / Amber `#D97706`/`#F59E0B` / BG `#0F172A`), BiDi isolation for emails, Jordanian Arabic copy, full loading/disabled + error/success states. **375px verification done** (WhatsApp-share viewport): signup/login/forgot all render RTL with zero horizontal overflow; inputs 49px @ 16px font (no iOS zoom-on-focus), CTA 48px (≥44px tap target); email input correctly `dir=ltr` inside the RTL form. |

**Composite: (10 + 10 + 10 + 10 + 10) × 2 = 100**

**Decision: 🚀 Ship.** Clean ≥90 with every hat at 10 and no open deductions. Run `/7-eo-ship`.

## What changed since the 88 score
- **Product:** `/api/auth/resend` route + signup success-state "أعد الإرسال" button (AC-1.2).
- **Architecture/Engineering:** extracted `lib/auth/seats.ts` (unified seat accounting); `confirm` route `token_hash`/`verifyOtp` fallback.
- **QA:** route-handler tests extended 10 → 16 (added login/forgot/logout via a real anon-client-backed mock of the server seam).
- **UX:** real 375px audit across all three public auth pages (screenshots + computed-style inspection).
