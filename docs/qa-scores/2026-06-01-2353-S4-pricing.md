# Score — S4-pricing (recompute after /6-eo-bridge-gaps) — 2026-06-01 23:53

Re-score of `feat/s4-pricing` after the bridge pass that lifted the lowest hat
(Product, 6) and three secondary hats. Supersedes the 80-composite report
(`2026-06-01-2341-S4-pricing.md`).

| Hat | Was | Now | Notes |
|-----|:---:|:---:|-------|
| Product | 6 | 9 | AC-4.2 money loop is now end-to-end and user-reachable: pricing CTA → `startCheckout` → `POST /api/checkout` → Stripe redirect (401 → `/signup?plan=&interval=`). The dead-end finding is resolved. Residual (−1, Phase-2): signup does not yet consume the carried `plan`/`interval` params to pre-select. |
| Architecture | 8 | 10 | Subscription upsert is now genuinely atomic via the `upsert_subscription` RPC (migration 0006) — `INSERT … ON CONFLICT` against the partial unique index. The prior find-then-write TOCTOU window under concurrent Stripe retries is gone. |
| Engineering | 9 | 10 | Centralized `captureError` seam (single S5/Sentry wire-in point); all API catch blocks now report instead of swallowing. `InvoiceWithSubscription` narrowing removes the inline cast. |
| QA | 9 | 10 | 140 passed / 7 skipped (S5-gated live-DB). L1 satisfied: webhook HTTP seam tested, incl. verbatim dispatch of `customer.subscription.deleted` + `invoice.payment_failed`. New `startCheckout` unit suite (5 `@AC-4.2` tests) pins the CTA wiring. |
| UX | 8 | 9 | Dashboard counters + plan badge wrapped in `<bdi>` (no numeral reordering in RTL); fetch-error `role="alert"` panel + `min-h-[44px]` retry button. Residual (−1, Phase-2): error states are color-only (no `aria-busy`); pricing onClick binding not render-tested (S5 component-harness scope). |

**Composite: (9 + 10 + 10 + 10 + 9) × 2 = 96 ✅**

**Decision: Ship** (≥90, no hat <8).

## Gate verification
- `supabase db reset` 0001→0006: clean
- vitest: 140 passed / 7 skipped (S5 live-DB), no unexpected failures
- eslint: exit 0
- `next build`: clean

## Carry-forward (non-blocking, Phase-2)
- Signup ignores the carried `plan`/`interval` query params (no pre-select).
- Webhook does two non-transactional writes (tenants + subscriptions); self-heals on Stripe retry, but not a single transaction.
- Error states are color-only / lack `aria-busy`.
- Pricing CTA onClick binding is not render-tested (needs the S5 component harness).

## Lesson candidate
Review must trace the **user-reachable end-to-end path**, not only audit each file
in isolation. S4's audit passed every file individually while the money loop had no
caller — the CTA still linked to `/signup`. Sibling to L1 (test the HTTP seam), but
for `/4-eo-review`: walk the path a real user takes, click by click.

## Next
`/7-eo-ship` — final HEAD self-score gate, PR, founder-authorized merge. Per L3,
confirm CI is green on the PR before declaring shipped.
