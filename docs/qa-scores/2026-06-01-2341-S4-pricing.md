# Score — feat/s4-pricing — 2026-06-01 23:41

**Story S4 — Pricing, Stripe Checkout & Account Activation** (AC-4.1…4.5)
Scored via 5 parallel hat subagents on branch `feat/s4-pricing` (diff vs `main`: 30 files, +1754/−80).

| Hat | Score | Notes |
|-----|:-----:|-------|
| Product | 6 | **AC-4.2 broken end-to-end** — Stripe self-serve path unreachable from UI (see 🔴). Bank-transfer fallback + AC-4.1/4.3/4.4/4.5 genuinely met. |
| Architecture | 8 | Access-gate split (`tenants.status` vs `subscriptions.status`) sound; isolation preserved; single-source limits. Idempotency is find-then-write — correct but race-safe only via the partial-unique index backstop, not atomic by design. |
| Engineering | 9 | Zero `any`, defensive DB writes (throw on failed update), best-effort email, all named edge cases covered & tested. Minor: catch-all 500s have no Sentry logging; ad-hoc Stripe `invoice.subscription` cast. |
| QA | 9 | Every AC @AC-tagged across pure + route-seam + live; idempotency proven (same event twice → 1 row); P0 signature gate + tenant-id throw covered. L1 cap lifted. Gap: `subscription.deleted`/`payment_failed` verified live but not at the route-dispatch seam. |
| UX | 8 | RTL + BiDi correct on pricing (`<bdi>JOD</bdi>`), Jordanian dialect natural, ≥44px, single-sourced caps. Gaps: dashboard `X / limit` ratios + plan badge lack `<bdi>` (slash reorder risk); dashboard has no fetch-error state (500 → silent "—"). |

**Composite: (6 + 8 + 9 + 9 + 8) × 2 = 80**

**Decision: 🩹 Bridge gaps** (80–89 band, AND Product 6 < 8 floor). Not ship-ready.

---

## 🔴 Critical finding (Product 6) — AC-4.2 money loop is a dead-end

The Stripe pieces are individually correct and seam-tested, but **nothing in the UI ever calls `/api/checkout`** (`grep /api/checkout|createCheckoutSession **/*.tsx` → 0 hits). The reachable flow loops:

1. `onboarding/page.tsx` (post-verify) → "اختيار الخطة" → `/pricing`
2. `pricing/page.tsx:102` CTA → `/signup?plan=${id}&interval=${interval}` — but the user is already signed up & verified
3. `signup/page.tsx` reads only `?token=` — `plan`/`interval` are silently dropped

So a logged-in lab has **no path to Stripe Checkout**. The founder-override Stripe path is dark; only the bank-transfer fallback actually activates an account. `/4-eo-review` (file-by-file) missed this because every file is correct in isolation — the break is in the *wiring between* them.

**Fix to bridge:** make the authenticated plan CTA POST to `/api/checkout` and redirect to `session.url`; carry `plan`/`interval` through the onboarding→checkout step (not via signup). Add a test asserting the CTA reaches checkout. This converts AC-4.2 from broken to met and lifts Product 6→9+.

## 🟡 Secondary (to reach 10/10 on the bridge pass)
- **Architecture 8→10:** atomic `ON CONFLICT (stripe_subscription_id) WHERE … IS NOT NULL` upsert via RPC, eliminating the SELECT/INSERT window.
- **Engineering 9→10:** wire Sentry into the checkout/dashboard/webhook catch blocks (failures currently swallowed into an opaque 500).
- **QA 9→10:** route-seam dispatch assertions for `subscription.deleted` + `payment_failed` (only `checkout.session.completed` is checked at the seam today).
- **UX 8→10:** `<bdi>` on dashboard ratios + plan badge; explicit dashboard fetch-error state (message + retry) so a 500 doesn't masquerade as empty data.

## Lesson candidate
`/4-eo-review` passed this branch clean (no 🔴) yet the score caught a broken core flow. The review checks each file; it did not trace the **user-reachable path end-to-end** (CTA → route → Stripe). Candidate lesson: review must walk the primary flow as a user (which component invokes which route), not only audit files in isolation — sibling to L1 (test the seam) but for review, not tests.
