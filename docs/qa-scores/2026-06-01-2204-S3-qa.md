# Score — feat/s3-qa — 2026-06-01 22:04

Story S3 — Bilingual Q&A with Mandatory Citation (AC-3.1…3.7). Scored via 5 parallel hat subagents on HEAD `035f669`.

| Hat | Score | Notes |
|-----|:-----:|-------|
| Product | 9 | Delivers cited-answer value; zero-hallucination contract honored end-to-end; bilingual real. −1: no empty-corpus guidance (new tenant gets generic refusal), citation badge not click-through/shareable, no confidence cue. |
| Architecture | 9 | Clean module boundaries; tenant-before-cosine guarantee holds; not-found path structural; lazy AI clients. −1: drop+recreate of `match_document_chunks` silently drops EXECUTE grants (works today only by PUBLIC-default luck) — add explicit `grant execute … to authenticated`. |
| Engineering | 8 | Errors surfaced not swallowed; no `any`/secrets; dedupe handles null page. **−1.5: `detectLang` returns "ar" on ANY Arabic codepoint** → a predominantly-English question with one inline Arabic term is misclassified, driving wrong answer + refusal language (AC-3.1/3.5). −0.5: log-insert failure discards a valid answer (re-pays embed+LLM). |
| QA | 9 | All 7 ACs tagged + behaviorally asserted; P0 (AC-3.5, model NEVER called) asserted via `not.toHaveBeenCalled()`; L1 route-seam cap LIFTED. −1: tenant isolation not exercised *live for the Q&A path* (live test seeds one tenant; cross-tenant leakage in the S3 RPC call surface unproven). |
| UX | 9 | Faithful to product-demo.jsx; BiDi handling stronger than reference (`dir="auto"` + `<bdi>` everywhere); not-found visually isolated; loading/empty/error states added. −1: citation meta `text-[11px]` on slate-400 ≈ 4.0:1 < WCAG AA 4.5:1. Minor: nav links <44px, no active-route highlight, dead `.bidi-term` CSS. |

**Composite: (9 + 9 + 8 + 9 + 9) × 2 = 88**

**Decision: Bridge gaps (80–89).** No hat < 8. Two points to the 90 gate.

## Bridge targets (ranked by impact)

1. **Engineering 8 → 10 (the real correctness fix).** Rewrite `detectLang` to pick the *majority* script (count Arabic vs Latin codepoints) instead of first-Arabic-wins. This fixes a genuine AC-3.1/3.5 bug: an Arabic-native engineer typing mostly-English with one Arabic word currently gets an Arabic answer/refusal. Update the helpers test that encodes the old "any Arabic ⇒ ar" rule, keeping the legitimate "Arabic sentence + English technical terms ⇒ ar" case. Optionally soften the log-insert failure (Sentry + return answer) — but compliance-first throw is defensible, so leave it.
2. **Architecture 9 → 10.** Append `grant execute on function public.match_document_chunks(vector(1536), int, float) to authenticated;` to migration 0004 so the privilege is explicit and migration-safe (no reliance on PUBLIC default).
3. **QA 9 → 10.** Add a `@AC-3.2` live cross-tenant test: seed Tenant B with a chunk at the same basis index, sign in as Tenant A, assert `ask()` returns zero B-chunks / `found=false` (re-proves the P0 isolation loop for the S3 RPC surface).
4. **UX 9 → 10.** Bump citation meta to `text-slate-300` (clear AA), give nav links `block min-h-[44px] py-2` + an active-route highlight, drop the dead `.bidi-term` rule.
5. **Product 9 → 10.** Empty-corpus hint (zero ready docs → "upload documents first" instead of a generic refusal) + make the citation badge link to the source document/page.

Lowest hat is Engineering (8); fixing #1+#2 alone reaches 90. Bridging all five reaches 100.
