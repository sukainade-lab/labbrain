# Score — feat/s5-deploy — 2026-06-02

Story 5: Deploy, Health & Observability (AC-5.1…5.7). Founder overrides: Contabo VPS, Stripe. SaaSfast M3.

| Hat | Score | Notes |
|-----|:-----:|-------|
| Product | 10 | Full S5 loop delivered + reachable: health probe (AC-5.1), Contabo docker-compose + Caddy auto-SSL + zero-downtime deploy.sh (5.2/5.3), Sentry tenant-scoped error capture (5.4), PostHog conversion funnel wired into all 4 real handlers (5.5), complete value-free env contract (5.6), live RLS proof on all 7 tenant tables (5.7). No orphaned step (L4 ✓). |
| Architecture | 8 | **Capped by L3** — CI green on the PR is not yet confirmed (no PR open). On merit the design is 10: DSN/key-guarded seams, metadata-only service-role-only RLS introspection RPC, init-only Sentry (no build-time token), dependency-free PostHog fetch. Cap lifts to 10 once `/7-eo-ship` opens the PR and confirms the `integration` + `quality` jobs green. |
| Engineering | 10 | One no-PII surface (events.ts), one error seam (captureError → reportException) with documents 500 brought to parity. No `any`, no secrets. Everything inert without DSN/key so build/CI stay green with zero config. Lint clean, build clean, audit exit 0 (moderate-only, below high gate). |
| QA | 10 | 231/231 passing. Route seams tested (L1 ✓): health handler + analytics fired from real signup/documents/qa/invoice handlers. Live AC-5.7 RLS test asserts RLS + ≥1 named policy on all 7 tables. No-PII asserted at the builder boundary. CI `integration` job runs live suites serialized `--no-file-parallelism` (L2 ✓). |
| UX | 10 | Backend story — no UI surface changed. User-facing error copy stays Arabic; RTL layout untouched. Health endpoint returns clean, well-typed JSON; deploy runbook documented in README for the operator. |

**Composite: (10 + 8 + 10 + 10 + 10) × 2 = 96 ✅**

**Decision: Ship** (≥90, no hat <8).

## Bridge to 100 (at /7-eo-ship)
- **Architecture 8 → 10**: open PR #5, confirm both CI jobs (`quality` + the new live-DB `integration`) report green via `gh run`, then the L3 cap lifts and the design's merit score (10) stands. This is the one remaining −2; it closes the moment CI is confirmed on the PR.

Run `/7-eo-ship` when ready (founder-authorized merge to main).
