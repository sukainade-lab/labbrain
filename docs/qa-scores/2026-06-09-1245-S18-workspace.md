# Score — S18 Two-Panel Document Workspace — 2026-06-09

**Branch:** `feat/s18-workspace` (commit `d11e963`) · **PR:** #37 · **Base:** `main`
**CI:** ✅ green — run 27206935183 (PR Quality Gate, `success` on head `d11e963`) confirmed **before** scoring (L3 / retro-P1)
**Diff:** 13 files, +1835 / −51 · single commit
**Story spec:** `docs/stories/S18.md` (AC-2.1…2.10 enumerated — L8)

> Redesign Epic story 1 of 4 (S18 workspace → S19 filtered Q&A → S20 SOP generator → S21 dynamic-pricing admin). Implements the updated BRD **S2** — a two-panel document workspace replacing the flat `/documents` list.

| Hat | Score | Notes |
|-----|:-----:|-------|
| Product | 9 | Complete, reachable two-panel redesign: permanent خدماتي الحالية panel + dynamic خدمة جديدة tabs (`+`/`✕`), per-section sub-tabs, per-section upload/list/delete, founder-confirmed destructive tab delete cascading docs+chunks. Migration backfills all existing production docs into Existing Services with zero data loss (AC-2.8). **−1:** the differentiators this foundation sets up — panel-scoped retrieval (S19) and SOP-draft generation (S20) — are deferred; standalone S18 value is "organize," and the SOP slot ships as a visible inert "قريبًا" stub. |
| Architecture | 9 | Additive-only schema (new `service_tabs` table + nullable `panel_type`/`service_tab_id`/`doc_section` columns) → existing retrieval unchanged (AC-2.5, regression-safe). FK `on delete cascade` makes tab-delete atomic. Tenant always resolved from session, never client-supplied (foreign tab → 404/400). Chunk-level panel/tab denormalization is a documented S19 pre-filter seam. Clean module split (migration / pure validation / service / route / UI). Zero new deps. CI green → L3 cap does **not** apply. **−1:** the two per-panel section vocabularies are enforced app-side (Zod `superRefine`) not by a DB check — a deliberate flexibility tradeoff. |
| Engineering | 9 | 606/606 tests (22 new S18): route-seam per branch + pure workspace validation + live cascade/tagging. Full error handling on every route (try/catch → `captureError` → 500; `DocLimitError` → 402; ownership guard → 400). `setSentryTenant` attribution. No `any`, no hardcoded secrets (L6 clean). |
| QA | 9 | **L1** route-seam tests for all three `/api/service-tabs` verbs + documents-POST tagging, every branch (200/201/400/401/404). **L2** live serialized unique-tenant suite proves tab-delete cascade, cross-tenant isolation (foreign tab 404 + untouched), and chunk tag-inheritance by driving the background pipeline. Every AC `@AC`-tagged; **L8** spec enumerated; **L7** live `npm run axe:walk` @375px `/documents` 0 violations cited. **−1:** no explicit empty-state UI assertion (a tab/section with zero docs). L7/L8 caps do not bite (walk cited + spec enumerated). |
| UX | 9 | Net-new RTL surface matching `product-demo.jsx` (navy `#1B2A3D` / amber `#D97706` / BG `#F8FAFC`). `<bdi>` on filenames **and** lab-named tabs from first impl (L5). ≥44px controls with `aria-label` on icon-only ✕/+/replace/delete buttons (L9). Live axe @375px 0 violations → L9 cap does not bite. **−1:** tab-add uses native `prompt()` and deletes use native `confirm()` — functional and Arabic-worded, but they bypass the design system (not RTL/brand-styled), a real polish gap that holds UX at the 90 baseline rather than 94. |

**Composite: (9 + 9 + 9 + 9 + 9) × 2 = 90 ✅**

**Decision: Ship** (composite ≥ 90, no hat < 8).

## Lesson cap audit (all satisfied — none bit)
- **L1** (QA cap 8 w/o route-seam) — satisfied: per-branch route tests for service-tabs + documents tagging.
- **L2** (QA cap 8 if unserialized live suite) — satisfied: CI integration job `--no-file-parallelism`, unique-tenant + cleanup.
- **L3** (Architecture cap 8 w/o green CI) — satisfied: run 27206935183 `success` on `d11e963` confirmed pre-score.
- **L4** (Product cap 8 if orphaned flow) — satisfied: all three verbs + tagged upload have UI callers in `documents/page.tsx`.
- **L5** (UX cap 8 if no BiDi) — satisfied: `<bdi>` on filenames + tab names from first write.
- **L6** (Engineering cap 8 if secret prefix) — satisfied: no literal secret prefixes in tracked files.
- **L7** (QA cap 9 w/o cited walk) — satisfied: live axe @375px `/documents` 0 violations cited.
- **L8** (QA cap 9 if backlog one-liner) — satisfied: `docs/stories/S18.md` AC-2.1…2.10.
- **L9** (UX cap 9 if own-CTA a11y fail) — satisfied: 0 a11y violations on the redesigned surface.

## No new lesson
Clean first-pass 90, consistent with the characterized story-mix signal (net-new visible UI shipping full design-system polish → 94; this story holds at 90 because tab-add/delete fall back to native `prompt`/`confirm` instead of on-brand dialogs). No recurring defect surfaced; minting a rule here would be lesson-bloat. If S19+ keep using native dialogs on redesigned surfaces, revisit.

## Next
`/7-eo-ship story-18` — re-score HEAD + build/test/audit + post ship verdict to PR #37 → **STOP at merge gate**. Merge of PR #37 to `main` needs explicit founder authorization naming the PR (a bare "y" is insufficient — classifier-enforced). **Migration `0017` prod apply stays founder-gated** (applied locally only). No production deploy.
