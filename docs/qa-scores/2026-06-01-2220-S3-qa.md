# Score (recompute) — feat/s3-qa — 2026-06-01 22:20

Story S3 — Bilingual Q&A with Mandatory Citation (AC-3.1…3.7). Full 5-hat recompute on HEAD `bb767ed` after `/6-eo-bridge-gaps` closed all five targets from the 22:04 report (composite 88).

| Hat | Score | Notes |
|-----|:-----:|-------|
| Product | 10 | Cited-answer value, zero-hallucination contract, real bilingual. Bridge: empty-corpus path now nudges a new lab to upload (distinct from a genuine miss) and citation badges are click-through to the source document (lands + ring-highlights + scrolls the exact file). No remaining gap for this story's scope. |
| Architecture | 10 | Tenant-before-cosine guarantee holds; not-found path structural; lazy AI clients. Bridge: explicit `grant execute … to authenticated` on `match_document_chunks` — privilege is now migration-safe, no PUBLIC-default reliance. Migration 0004 applies clean on a fresh `supabase db reset`. |
| Engineering | 10 | Errors surfaced not swallowed; no `any`/secrets. Bridge: `detectLang` rewritten to majority-script-by-word with Arabic-first tiebreak — fixes the real AC-3.1/3.5 bug where a mostly-English question with one Arabic word was misclassified. Regression cases pinned in helpers test (Arabic+EN-terms ⇒ ar; mostly-EN+1 AR word ⇒ en; bare technical EN ⇒ en). Log-insert-throws is a deliberate compliance-first choice (documented), not a defect. |
| QA | 10 | All 7 ACs tagged + behaviorally asserted; P0 (AC-3.5, model NEVER called) asserted via `not.toHaveBeenCalled()`; L1 route-seam satisfied. Bridge: added a live cross-tenant P0 test — Lab A querying Lab B's *exact* embedding retrieves nothing (tenant filter beats cosine 1.0), and a live empty-corpus test (Lab C, zero docs ⇒ `emptyCorpus=true`, model not called); existing miss test now asserts `emptyCorpus=false`. 96 tests green against a fresh DB. |
| UX | 10 | Faithful to product-demo.jsx; BiDi stronger than reference (`dir="auto"` + `<bdi>`); not-found visually isolated. Bridge: citation meta → `text-slate-300` (clears WCAG AA); nav links are ≥44px tap targets with an active-route highlight (`aria-current`); dead `.bidi-term` CSS removed. Clickable citation has visible hover + focus-visible ring (keyboard reachable). |

**Composite: (10 + 10 + 10 + 10 + 10) × 2 = 100**

**Decision: Ship.** No hat < 8; composite ≥ 90. Run `/7-eo-ship`.

## Gate evidence (HEAD bb767ed)

- `npx vitest run` → **96 passed, 12 skipped** (S4/S5 stubs), 0 failures.
- `npx eslint .` → clean.
- `npx next build` → clean; TypeScript clean; `/qa` and `/documents` remain static.
- `npx supabase db reset` → migrations 0001–0004 apply clean.
- `npm audit --omit=dev --audit-level=high` → 0 high/critical. (2 *moderate* advisories: build-time `postcss` XSS via `next`; fix requires a breaking `next` downgrade — documented, below the high gate, not blocking.)

## Bridge delta vs. 22:04 report

| # | Target | Status |
|---|--------|--------|
| 1 | Engineering: majority-script `detectLang` | ✅ `src/lib/qa/lang.ts` + helpers test |
| 2 | Architecture: explicit RPC grant | ✅ `supabase/migrations/0004_qa.sql` |
| 3 | QA: live cross-tenant Q&A isolation | ✅ `tests/story-3-qa.test.ts` (+ empty-corpus test) |
| 4 | UX: contrast + tap targets + active nav + dead CSS | ✅ qa/page, layout, nav-links, globals.css |
| 5 | Product: empty-corpus hint + clickable citation | ✅ ask.ts (`emptyCorpus`), qa/page, documents/page |
