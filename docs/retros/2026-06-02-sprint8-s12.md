# Sprint 8 retro — S12 Multi-tenant branding (lab logo)

**Date:** 2026-06-02
**Stories shipped:** 1 — S12 Multi-tenant branding (lab logo in app header), PR #23 squash-merged `4680092`
**Composite:** 94 (P9 A9 E10 Q9 U10, no hat <8)
**Trend:** +4 vs Sprint 7 (S11 = 90) — **breaks the two-sprint 90 plateau (S10, S11)**, first-pass, zero round-trip

## Hat averages (Sprint 8)
| Hat | Avg | Trend vs S11 | Status |
|-----|:---:|:---:|--------|
| Product | 9.0 | = | ✅ |
| Architecture | 9.0 | = | ✅ |
| Engineering | 10.0 | +1 | ✅ |
| QA | 9.0 | = | ✅ |
| UX | 10.0 | +1 | ✅ |

Two hats reached 10 (Engineering, UX) — the difference between an infra-plateau 90 and a feature-UI 94.

## Headlines

1. **The story-selection thesis from Sprint 7 paid off exactly as predicted.**
   Sprint 7's retro closed with a specific bet: S12 is "the first feature-UI story
   since the axe-walk harness landed where L5/L7/L9 *all* apply to net-new visible
   surface — a clean shot at breaking the 90 plateau (94+) **if** the L7 walk is
   first-pass clean." It landed: the live `npm run axe:walk` ran against the actual
   `/settings` Branding section (seeded owner session) at 375px and returned **0
   violations** on the net-new surface — so UX earned a clean 10 and QA was never
   L7-capped. The plateau wasn't a process failure; it was a story-mix artifact
   (S9/S10/S11 were mock-tested infra/seam stories that cap at 90 by necessity).
   Choosing a UX-heavy story with real visible surface converted directly into +4.

2. **Engineering 10 came from layered tests, not more tests.** S12's coverage is
   the cleanest in the project: 11 route-seam (every HTTP branch 201/400/413/403/401
   · 200/403/401), 13 service-layer (mocked admin client — runs anywhere), validation
   units, **plus** a live RLS-isolation suite proving cross-tenant write/list/delete
   denial. The `/4-eo-review` gate earned its keep here — it caught that `logo.ts`
   had *zero direct coverage* (route tests mock it out; the live L2 test hits raw
   Storage RLS) and that AC-12.4/12.6 carried no `@AC` tag, despite `docs/stories/
   S12.md` promising a service-test layer. Fixed inline before scoring (added the 13
   service tests + extracted the pure `resolveSidebarBrand` resolver). Verification-
   before-done working as designed: 25 green tests had masked a real seam gap.

3. **Zero lesson caps bit — third consecutive clean-hygiene ship.** Every applicable
   lesson was satisfied *proactively during coding/review*, none surfaced at scoring:
   L1 (route-seam per branch), L2 (live suite serialized + unique-tenant + cleanup),
   L3 (CI green on PR #23 before score), L4 (BrandingSettings → /api/branding, header
   reads logo_path), L5 (`<bdi>` on lab name in layout + settings + preview), L6 (no
   secret prefixes, no new env vars), L7 (live axe walk cited), L8 (AC-12.1…12.8
   enumerated pre-code), L9 (primary CTA #B45309 white 5.02:1 AA-pass). The lessons
   are now design-time defaults, not score-gate catches — same posture as S11.

## Lessons status
- **Active: 9** (L1–L9). None triggered as a *cap* this sprint (all proactively
  satisfied). All <90d → **0 archived, 0 pruned**.
- **Proposed: none.** S12 surfaced no recurring defect. The one in-sprint catch (the
  `logo.ts` service-coverage gap) is already governed by L1's sibling principle
  ("direct unit tests are necessary but not sufficient" / "test the seam") — the
  review applied it correctly; no new rule needed. Minting a lesson here would be
  bloat.

## SaaSfast mode
S12 fits **M3** cleanly — a Supabase Storage bucket + RLS + one settings section +
a header read inside the existing M3 app; one migration (0013), zero new npm deps.
No outlier; no `SAASFAST-MODES.md` change proposed.

## Next-sprint focus
- **Backlog from here is S13–S16** (document versioning + re-index, Slack/email
  weekly digest, API access for LIMS/ERP, webinar demo flow). The 94 pattern is now
  reproducible: a story with real net-new visible surface + a clean first-pass L7
  walk clears 90. S13 (versioning/re-index) is more domain/data than UI — expect it
  to plateau nearer 90 like the infra stories unless it ships meaningful UI; S14
  (digest) and S16 (webinar demo) are UI-bearing and can target 94+.
- **Process is healthy; don't add ceremony.** Three consecutive zero-round-trip
  ships (90, 90, 94) with no cap bites. The marginal gain remains story selection
  and keeping lessons design-time, not new gates.
- **Adopt L8 immediately** for the next Phase-2 story (enumerate AC-13.x into
  `docs/stories/S13.md` before coding).

## Standing founder-gated items (surfaced, not worked around)
- 🔴 **Rotate the GitHub PAT** — pasted plaintext earlier, still live/active for
  `sukainade-lab` (#1 risk).
- `.claude/settings.json` SessionStart hook (auto-load lessons) still awaiting
  explicit founder approval.
- Live Contabo cutover (self-hosted Ollama + LlamaParse for S11 air-gap) +
  live AWS me-central-1 cutover (S10 KSA/PDPL) remain founder-gated (host + on-prem
  secrets only the founder holds).
