# Score — feat/s6-tap-payments — 2026-06-02

Story S6 — Tap Payments card integration (JOD + KWD + SAR). Scored on HEAD
`2e3dead` after `/4-eo-review` (clean) + one `/6-eo-bridge-gaps` auto-fix.

| Hat | Score | Notes |
|-----|:-----:|-------|
| Product | 9 | L4 money path connects end-to-end: pricing CTA → `startCheckout(JOD)` → `/api/checkout` → `pickProvider("JOD")="tap"` → `createTapCharge` → HPP url → `/api/webhooks/tap` → shared `applyOutcome`. No orphaned CTA. JOD-first MENA buyer served; no FX guessing (KWD/SAR gated). Held at 9: ships without auto-renewal (per-interval charge, deferred by plan). |
| Architecture | 8 → 9* | Clean `PaymentProvider` seam, single shared `activation-core` reducer, migration 0008 additive + correct unique-index re-key + `security definer` RPC granted only to `service_role`. **L3 cap → 8 until CI confirmed green** (resolved post-push, see below). Design otherwise merits 9. |
| Engineering | 9 | Webhook verifier fails closed (missing header/secret/parse → reject), constant-time `timingSafeEqual` with length guard, only `CAPTURED` activates. No `any`, no hardcoded secrets. tsc/lint/build green; suite 270/270 after the bridge fix. (Was 7 pre-fix: `.env.example` comment embedded `sk_live_` → tripped S5 leak guard; reworded to `sk_…`.) |
| QA | 9 | 6/6 ACs `@AC`-tagged (AC-6.4 retagged in review). Exhaustive webhook negatives (tampered/forged/non-CAPTURED/no-secret), all route branches tested (L1: checkout currency-switch + webhook 400/200/500), live activation serialized + unique-tenant scoped (L2). Held at 9: no explicit missing-`tenant_id` reducer test. |
| UX | 9 | L5 satisfied — `<bdi>` on every mixed-script amount + the `JOD` code from first implementation. 44px tap targets at 375px, `aria-pressed`/`aria-busy`/`role="alert"`+icon, localized Arabic (Jordanian dialect) error/pending states. Held at 9: pending state is text-only (no visual spinner). |

**Composite (pre-CI): (9 + 8 + 9 + 9 + 9) × 2 = 88** — Architecture capped at 8 by Lesson L3 (no confirmed-green CI; branch not yet pushed at score time).

**Decision: bridge the L3 cap by pushing the branch → CI green → Architecture 9 → composite 90.** The one substantive gap (Engineering test failure) was already auto-fixed. The remaining −2 is purely the procedural L3 cap. After CI confirms green: composite **(9+9+9+9+9)×2 = 90 ✅ → ship gate met.**

**Stop point:** PR opened, NOT merged — awaiting explicit founder merge authorization (must name the PR).

---

## Post-CI re-score (2026-06-02)

Branch pushed → **PR #8** opened → CI run `26787911025` **conclusion: success**
(PR Quality Gate: lint/test/build/audit ✓; integration job: fresh Supabase +
all migrations incl. `0008` + serialized live tests ✓). Lesson L3 cap cleared.

| Hat | Score |
|-----|:-----:|
| Product | 9 |
| Architecture | 9 (L3 cleared — CI confirmed green) |
| Engineering | 9 |
| QA | 9 |
| UX | 9 |

**Composite: (9 + 9 + 9 + 9 + 9) × 2 = 90 ✅ — ship gate met (no hat < 8).**

**Decision: Ship — but HOLD at the merge gate.** PR #8 is green and ship-ready.
Per standing constraint, do NOT merge to `main` without explicit founder
authorization naming the PR (e.g. "merge PR 8"). `/7-eo-ship` will complete the
merge + deploy + health check only on that authorization.
