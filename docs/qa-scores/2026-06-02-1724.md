# Score — feat/s11-airgap-mode — 2026-06-02 17:24

**Story:** S11 — Air-gap mode (self-hosted LlamaParse + Ollama; fail-closed local inference)
**PR:** #21 · **CI:** run 26825816130 green (quality + integration)
**Diff:** 18 files, +1006 / −52 · 33 S11 tests (26 inference-mode + 7 seams), all `@AC-11.N` tagged

| Hat | Score | Notes |
|-----|:-----:|-------|
| Product | 9 | Delivers the named compliance unlock for classified JISM labs — the residual "data transits cloud processors" objection is removed end-to-end. All 8 ACs (11.1–11.8) covered against the enumerated `docs/stories/S11.md` spec. Operator-visible badge is reachable on `/admin` + `/founder` (L4). −1: the live no-cloud guarantee is proven against mocks only; a run against a real Ollama / self-hosted LlamaParse is an honest, founder/integrator-gated deferral (out-of-scope, like the Contabo/me-central-1 cutovers). |
| Architecture | 9 | One pure resolver (`inference-mode.ts`) as the single source of truth; the 3 seams (parse/embed/answer) branch nowhere else — same seam discipline as S6/S9/S10. **Zero new npm deps** (reuse `openai` SDK with `baseURL` override for Ollama's OpenAI-compatible API). **No DB migration.** Fail-closed: misconfigured air-gap throws before any request. L3 satisfied — CI confirmed green on PR #21 before scoring (no Arch cap). |
| Engineering | 9 | Resolver throws on unknown mode (no silent default), validates `AIRGAP_EMBEDDING_DIM === 1536` against the pgvector column (turns a silent vector-corruption footgun into a startup error). `describeInferenceMode` is display-safe (never throws → `—` placeholders) so the badge can't crash the page. `.env.example` + `docs/env-contract.md` are names-only with abstract placeholders — no real secret prefix (L6 satisfied, no Eng cap). tsc/eslint clean. |
| QA | 9 | 33 tests, every `AC-11.N` traced to a `@AC-11.N`-tagged test. Pure-resolver coverage (cloud default, airgap happy path, unknown→throw, per-seam shape, fail-closed on each missing var, dim mismatch→throw) + seam coverage (mocked client: embed/answer/parse hit the local host, never `openai.com`/cloud-LlamaParse, and air-gap+missing-var throws before any client is built). L7 satisfied — live `npm run axe:walk` @375px on /dashboard+/admin+/founder = 0 serious/critical (no QA cap). L8 satisfied — ACs enumerated before coding. L1/L2 N/A: story adds no new API route and no live-DB suite. |
| UX | 9 | Read-only inference-mode panel, RTL-first (`<section aria-label="نمط الاستدلال">`), 3 colour-coded states (air-gap=locked/green, cloud=neutral, invalid=red so a typo'd `INFERENCE_MODE` is unmissable). Every dynamic mixed-script value (model names, parse host — Latin inside Arabic layout) wrapped in `<bdi>` from first implementation (L5). L9 satisfied — the badge's own `<dt>` label AA-contrast failure (slate-500 3.05:1) fixed inline to slate-400 (~5.66:1) before scoring, not deferred. 0 axe violations @375px. −1: a modest read-only indicator; correct and accessible, not a standout interaction. |

**Composite: (9 + 9 + 9 + 9 + 9) × 2 = 90 ✅**

**Decision: Ship** — composite 90 (≥90), every hat ≥8. No bridge round-trip needed.

Run `/7-eo-ship story-11` when ready. (Merge of PR #21 to `main` is founder-gated.)

## Lesson signal
No new pattern. S11 is a clean first-pass 90 — every applicable lesson cap (L3/L4/L5/L6/L7/L8/L9) was satisfied proactively during `/3-eo-code` + `/4-eo-review`, so none bit at scoring. This is the intended steady state: the lessons are now being applied at write-time rather than surfaced at score-time. Nothing to append.
