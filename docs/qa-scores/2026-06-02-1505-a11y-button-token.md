# Score — feat/a11y-button-token — 2026-06-02

**Scope:** App-wide a11y follow-up (spun off from Sprint 5 / S9 retro). Systemic fix for the recurring WCAG-AA contrast failure on the primary action button. Not a BRD story — a cross-cutting design-token + test-guard change. PR #16.

| Hat | Score | Notes |
|-----|:-----:|-------|
| Product | 10 | Retires a real defect that twice reached scoring (S8, S9). The product safety/quality bar for a JISM-facing tool includes a11y; primary CTAs (signup, login, checkout, export, activate) now meet WCAG-AA. No scope creep — pure quality fix. |
| Architecture | 10 | Single source of truth: two semantic Tailwind tokens (`brand-amber`, `brand-amber-hover`) replace 19 scattered literal amber values. Future call sites reference intent, not a hex. Clean, reversible, no new deps. |
| Engineering | 10 | Pure token substitution, zero logic change. 14 failing surfaces + 5 already-safe surfaces unified. LF/CRLF only cosmetic. No `any`, no secrets. |
| QA | 10 | New guard `tests/a11y-button-contrast.test.ts`: authoritative WCAG relative-luminance math + a `.tsx` scanner that **fails CI** if any future story pairs a solid amber bg with `text-white`. Caught 4 violations my hex-only grep missed (the `bg-amber-600` class form) — the guard is load-bearing, not decorative. 430/430 green. |
| UX | 10 | The change *is* the UX win: white-on-#D97706 (3.19:1, FAIL) → white-on-#B45309 (5.02:1, PASS); hover #92400E = 7.09:1. L7 walk (375px + axe-core) cited, 0 serious/critical. L9 cap (UX≤9 for shipping a known CTA a11y failure) does not apply — this *removes* the failure. |

**Composite: (10+10+10+10+10) × 2 = 100 ✅**

**Decision: Ship** — composite ≥90, no hat <8.

## Lessons applied
- **L9 (the reason this PR exists):** the systemic token fix retires the recurring failure for every current + future UI surface. The guard test makes "pre-existing" impossible to re-introduce silently.
- **L7:** manual 375px + axe-core walk cited as evidence (0 serious/critical).
- **L3 / retro-P1:** CI confirmed green on PR #16 head before this ship decision.

## Gate evidence
- Guard test red→green; full suite 430/430 (425 + 5 new).
- lint clean · `tsc --noEmit` clean · `next build` clean.
- `npm audit --omit=dev --audit-level=high` → 0 high/critical (2 moderate postcss/next, pre-existing).
