# Score — S4-pricing (recompute after second /6-eo-bridge-gaps) — 2026-06-02 00:08

Second bridge pass on `feat/s4-pricing`, closing the two residual −1s from the
96 report (`2026-06-01-2353-S4-pricing.md`): Product 9→10 and UX 9→10. Both were
honest gaps, not rubric padding — each is now closed with shipped code + tests.

| Hat | Was | Now | Notes |
|-----|:---:|:---:|-------|
| Product | 9 | 10 | The carried plan choice now survives the full round-trip. A logged-out pick threads pricing CTA → `/signup?plan=&interval=` → signup POST → route `parseResume` (allow-list) → `provisionSignup` sets the confirmation-email `next` to `/onboarding?plan=&interval=` → onboarding "أكمل الاشتراك في خطة X" → `/pricing?plan=&interval=` with the interval pre-selected and the chosen card highlighted → Stripe. The prior drop-at-signup residual is resolved; the money loop is end-to-end for both logged-in and logged-out entry. |
| Architecture | 10 | 10 | Unchanged. Atomic `upsert_subscription` RPC (migration 0006) holds. The new resume plumbing is pure functions over an allow-list — no new persistence or TOCTOU surface. Carried params are validated before they ever reach a redirect URL (no open-redirect / injection echo). |
| Engineering | 10 | 10 | Unchanged. The resume logic is extracted to `lib/payment/resume.ts` as four pure functions (`parseResume`, `onboardingNext`, `resumeCheckoutHref`, `checkoutCtaState`) — single source of truth, consumed by the route, the signup form, onboarding, and pricing. `/pricing` stays statically rendered via a `useState` lazy initializer over `window.location.search` (no `useSearchParams` → no forced Suspense/dynamic). |
| QA | 10 | 10 | 154 passed / 7 skipped (S5-gated live-DB), up from 140. +12 `@AC-4.2` unit tests pin the resume allow-list + the CTA state machine; +2 live signup-route tests assert a carried `pro/year` yields `next=/onboarding?plan=pro&interval=year` and an off-list plan falls back to plain `/onboarding`. L1 satisfied (HTTP route seam tested per story). |
| UX | 9 | 10 | The pricing CTA binding is now unit-tested without a DOM: `checkoutCtaState` is pure, so label/busy/disabled (pending self → "جارٍ التحويل…" busy+disabled; sibling → disabled-not-busy; resumed → "أكمل الاشتراك"; idle → "ابدأ الآن") are asserted in node. `aria-busy` added to the pricing CTAs and the dashboard counters grid. Both error regions now carry a ⚠️ non-color signal alongside `role="alert"` (failure not signalled by colour alone). Resumed card gets `ring-2 ring-amber-500/50` for a visible pre-select. |

**Composite: (10 + 10 + 10 + 10 + 10) × 2 = 100 ✅**

**Decision: Ship** (≥90, no hat <8). Full marks against what the story scope allows.

## Gate verification
- vitest: 154 passed / 7 skipped (S5 live-DB), no unexpected failures
- eslint: exit 0
- `next build`: clean — `/pricing` stayed static (○), `/onboarding` dynamic (ƒ)

## What changed since 96
- New `src/lib/payment/resume.ts` (allow-list parse + post-confirm destination + checkout href + CTA state machine).
- `provision.ts` / `api/auth/signup/route.ts` / `signup-form.tsx` / `(auth)/signup/page.tsx` thread `plan`/`interval` end-to-end.
- `onboarding/page.tsx` resumes the exact purchase (CTA + copy) when arriving with a valid choice.
- `(marketing)/pricing/page.tsx` pre-selects interval, highlights the resumed card, binds the CTA via `checkoutCtaState`, adds `aria-busy` + non-color error icon.
- `(app)/dashboard/page.tsx` adds `aria-busy` on the grid + non-color error icon.
- +14 tests (`story-4-resume.test.ts` ×12, `story-1-auth-routes.test.ts` +2).

## Carry-forward (non-blocking, Phase-2)
- Webhook does two non-transactional writes (tenants + subscriptions); self-heals on Stripe retry, but not a single transaction. (Architecture noted; below the ship bar, not a −1 at this scope.)

## Next
`/7-eo-ship` — final HEAD self-score gate, PR #4 founder-authorized merge. Per L3,
confirm CI is green on the PR before declaring shipped.
