# Score — feat/s9-audit-export — 2026-06-02

Story S9 — Audit export: download a lab's Q&A history as an A4, RTL, Arabic-shaped PDF (audit evidence for a JISM assessment).
Scored after a clean `/4-eo-review` (static review + L7 live 375px + axe-core walk) and a **confirmed-green CI run** (PR #14, `quality` + live `integration` both success — retro P1 honored).

| Hat | Score | Notes |
|-----|:-----:|-------|
| Product | 9 | Full BRD S9 scope against the L8-enumerated `docs/stories/S9.md`: owner/admin-gated `GET /api/audit/export` (AC-9.1) → chronological Q&A rows with citations + asker + DD/MM/YYYY HH:MM (AC-9.2) → inclusive from/to range, reversed/malformed → 400, omitted → full history (AC-9.3) → A4 RTL PDF, empty log → valid PDF not an error (AC-9.4) → `application/pdf` attachment + owner/admin-reachable admin-page trigger (AC-9.5) → `audit_exports` audit row (AC-9.6). Preserves the product safety contract: the export carries the same source citations as the live answer. Reachable end-to-end (L4). −1: export of existing data (incremental capability); date control is a bare pair of inputs (no presets like "last assessment period"). |
| Architecture | 9 | Clean seam design: the non-deterministic Chromium dependency lives behind `renderPdfFromHtml()` (mocked in every CI test → CI needs no browser), while the print HTML is a **pure, deterministic builder** unit-tested without a browser. Residency-conscious: rendering is in-house (VPS/Frankfurt), no third-party PDF service ever sees lab Q&A. User-scoped Supabase client → RLS guarantees tenant isolation on the export rows; `audit_exports` has named RLS (`audit_exports_tenant_isolation`, migration 0011). Dockerfile provisions system Chromium + Noto Arabic/Latin fonts + `PUPPETEER_EXECUTABLE_PATH`. −1: the route handler inlines tenant lookup + query + html + render + audit insert; an orchestrator would isolate the happy path from the I/O. |
| Engineering | 9 | Type-strict — no `: any` (embedded user cast via `row as unknown as AuditQueryRow`); typed browser interfaces (`PdfPage`/`PdfBrowser`/`BrowserLauncher`); lazy `import("puppeteer-core")`. Defensive: every dynamic value HTML-escaped (`esc`), filename injection-safe (ASCII `labSlug` + digit `stampYmd`), deterministic dates via `Intl` with explicit `Asia/Amman`, render seam throws a clear message when `PUPPETEER_EXECUTABLE_PATH` is unset. Error path: try/catch → `captureError("audit-export", …)` → 500. `.env.example` adds only names, no secret prefix (L6 clean). −1: the report `<h1>` shipped with `esc()` instead of `bidi()` on the lab name (an implementation slip in `report-html.ts`) — caught + fixed in `/4-eo-review`, not from first write. |
| QA | 9 | All six ACs (AC-9.1..9.6) covered by `@AC-9.x` tagged cases across 4 mocked files (validation 14 · report-html 12 · render-pdf 4 · routes 10 = 40) + a live-DB suite. **L1**: route-seam test per HTTP branch (401 unauth, 401 no-tenant, 403 member, 400 bad range, 200 owner, 200 admin, 200 empty). **L2**: `story-9-audit.test.ts` serialized, unique-tenant, **cross-tenant exclusion** asserted (sibling to AC-1.3/2.4), `afterAll` cleanup, skipIf-no-Supabase. **L7**: manual 375px + axe-core walk cited. **L8**: ACs enumerated before coding. Empty-state + not-found entry styling tested. −1: AC-9.6 was implemented + asserted but initially **untagged** (`@AC-9.6` missing) — a traceability gap caught in review, not clean from first write. |
| UX | **8** | RTL-first: `dir="rtl"` document, IBM Plex Arabic, bilingual labels (`غير موجود / not found`, `لا توجد سجلات`), DD/MM/YYYY dates, date inputs correctly `dir="ltr"` + aria-labeled, all tap targets ≥44px (walk measured 47–48px), `<bdi>` on every dynamic mixed-script value (after the h1 fix). **−2 (the cap reason):** the primary export CTA ("تصدير سجل الأسئلة (PDF)") ships with a **WCAG-AA contrast failure** — white-on-`#D97706` = **3.19:1** (needs ≥4.5:1 for 16px/500). It is the app-wide button pattern (inherited, not an S9 regression), but it is the primary CTA *of this story's surface* and ships unfixed at scoring time. Honesty over leniency: a compliance product shipping a known AA failure on its audit-export button costs the hat. (S8 precedent: its walk found contrast and **fixed it before final score** → UX 9; S9 must do the same to reach 9.) |

**Composite: (9 + 9 + 9 + 9 + 8) × 2 = 88 🟡**

**Decision: Bridge gaps** (80–89 band; lowest hat UX 8 → lift to ≥9 to reach the ≥90 ship gate). Run `/6-eo-bridge-gaps` targeting UX.

## Lesson caps — all satisfied (none triggered)
- **L1** (QA cap 8): route-seam test per HTTP branch in `tests/story-9-audit-routes.test.ts` ✓
- **L2** (QA cap 8): live DB suite serialized + unique-tenant + cross-tenant exclusion + afterAll ✓
- **L3** (Arch cap 8): CI for PR #14 confirmed green (quality + integration) BEFORE scoring ✓
- **L4** (Product cap 8): export UI reachable owner/admin → route → file; gate mirrors route ✓
- **L5** (UX cap 8): `<bdi>` on all dynamic mixed-script values (h1 miss fixed in review, complete at HEAD) ✓
- **L6** (Eng cap 8): no literal secret prefix in tracked files ✓
- **L7** (QA cap 9): manual 375px + axe-core walk cited ✓ (the walk's *finding* — contrast — is a UX merit deduction, separate from the cap)
- **L8** (QA cap 9): AC-9.1..9.6 enumerated in `docs/stories/S9.md` before coding ✓

> Note: the UX 8 is a **merit deduction for an unfixed AA-failing CTA**, not a lesson cap. No lesson cap is triggered.

## Next
`/6-eo-bridge-gaps` (UX): bring the S9 review surface (the /admin page) to WCAG-AA — darken the two amber CTAs on it (export = S9, invite = S1 same page) to amber-700 `#B45309` (white ≈ 5:1) with an AA-passing hover, and add an accessible name to the invite role `<select>` (axe `select-name`). Re-verify (contrast math + focused axe re-run on /admin → 0 violations), re-score UX → 9 → composite 90, then `/7-eo-ship` (re-score HEAD, build/test/audit; PR #14 already open) and **STOP at the merge gate**. The remaining ~10 app-wide `#D97706` files stay in the spun-off a11y follow-up task.

---

## Re-score after `/6-eo-bridge-gaps` (UX) — commit `8eef618`

| Hat | Before | After | Change |
|-----|:------:|:-----:|--------|
| Product | 9 | 9 | — |
| Architecture | 9 | 9 | — |
| Engineering | 9 | 9 | — |
| QA | 9 | 9 | — |
| **UX** | **8** | **9** | the /admin surface is now WCAG-AA clean: export + invite CTAs darkened to amber-700 `#B45309` (white **5.02:1** ✓, hover `#92400E` **7.09:1** ✓), invite role `<select>` gains `aria-label="دور العضو"`. **Verified live** — re-ran axe-core wcag2a+wcag2aa on /admin at 375px (real owner session) → **0 violations / 24 passes**; computed `background-color: rgb(180,83,9)`; select accessible name resolves. −1: a one-shade cross-page inconsistency remains until the spun-off follow-up standardizes the other ~10 `#D97706` files into a shared token. |

**Re-scored composite: (9 + 9 + 9 + 9 + 9) × 2 = 90 ✅**

**Decision: Ship** (≥90, no hat <8). → `/7-eo-ship` (re-score HEAD, build/test/audit; PR #14 already open), then STOP at the merge gate pending explicit founder authorization naming the PR.
