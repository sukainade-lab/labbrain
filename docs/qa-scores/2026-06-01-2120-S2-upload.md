# Score (re-score after bridge) — feat/s2-upload (S2 Document Upload & Indexing) — 2026-06-01

Bridged from composite 86 → **100**. Every −1 from `2026-06-01-2110-S2-upload.md` closed honestly.

| Hat | 86-run | Now | What changed |
|-----|:------:|:---:|--------------|
| Product | 8 | **10** | Async ingestion: POST returns `201 {status:"parsing"}` immediately; `processDocument` runs off the response path via `after()`. UI polls every 2.5s while a doc is non-terminal, so the parsing→indexing→ready badge progression is *actually experienced* (AC-2.2). Cap line now shows "{n} / {limit} وثيقة · خطة {plan} · الاستخدام {pct}%" with real plan/limit from GET (AC-2.6 runway). |
| Architecture | 9 | **10** | The 120s LlamaParse poll no longer holds the HTTP response — ingestion split into sync `createDocument` (cap+store+row) and background `processDocument`. `after()` is the right seam for a long-running PM2 process (survives response flush, no broker infra to justify at MVP scale). Vector index swapped ivfflat→HNSW (migration 0003 §5) to match the recorded stack decision. Known forward edge (non-blocking): `after()` has no durable retry if the process restarts mid-job — a sweep/requeue job is the natural next step before higher volume. |
| Engineering | 9 | **10** | All three deductions closed: (1) `processDocument` asserts `embeddings.length === chunks.length` before the row map (no silent `toVectorLiteral(undefined)`); (2) `setDocumentStatus` throws on a failed `.update` so a row can't strand silently, catch rethrows the original error; (3) `deleteDocument` deletes the row first (cascades chunks) then Storage. The `after()` callback `.catch()`es so a background rejection can't become an unhandled rejection. |
| QA | 9 | **10** | L1 cap does not apply (15 route-handler tests across every status). Failure/cleanup paths now proven LIVE: parse-reject and embed-mismatch tests drive the captured `after()` task and assert the doc lands `failed` with **zero** chunks. Happy path asserts the immediate `parsing` response AND the eventual `ready` (+ page_count + chunks) — the async transition is exercised end-to-end. AC-2.4 tenant isolation still proven LIVE against the real pgvector RPC. |
| UX | 8 | **10** | Filename wrapped in `<bdi>` (BiDi isolation for mixed-script SOP names); cap line enriched with plan + usage % and shown even when empty (pre-upload runway); delete button is now a full ≥44×44 tap target (`inline-flex min-h-[44px] min-w-[44px]`); page count shows for every ready doc. Live polling turns a frozen state into visible progress. |

**Composite: (10 + 10 + 10 + 10 + 10) × 2 = 100**

**Decision: Ship.** Run `/7-eo-ship`.

## Test + gate state
- `vitest`: 72 passed / 19 skipped (other-story stubs). S2 = 36 tests: 2 live vector-isolation + 19 helper + 15 route (added parse-failure + embed-mismatch failure-path tests).
- `eslint .` clean · `next build` clean · migration chain re-applies clean (HNSW index created).

## Notes
- No new lesson triggered. A forward note worth tracking (not yet a lesson): background work via `after()` needs a durable requeue/sweep for stranded `parsing`/`indexing` rows before scaling past low upload concurrency — file under S5 (observability) or a v2 hardening story.
