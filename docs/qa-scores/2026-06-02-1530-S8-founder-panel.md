# Score — feat/s8-founder-panel — 2026-06-02

Story S8 — Founder super-admin panel (cross-tenant overview, pause/unpause, manual invoice activation).
Scored after a clean `/4-eo-review` (static review + L7 live 375px + axe-core walk) and a **confirmed-green CI run** (PR #12, `quality` + live `integration` both success).

| Hat | Score | Notes |
|-----|:-----:|-------|
| Product | 9 | Full BRD S8 scope: /founder gated by `PLATFORM_ADMIN_EMAILS` renders 4 metric cards (AC-8.2), tenants table w/ usage (AC-8.3), invoice queue + pause/unpause/activate (AC-8.4/8.5), all re-gated server-side (AC-8.6). Flow reachable end-to-end (L4): buttons POST to existing routes; pause genuinely enforced via proxy → /account-paused. −1: "mark invoice paid" reuses the `inactive` status as the queue rather than a first-class invoice entity (acceptable simplification for a solo-founder tool). |
| Architecture | 10 | Single `founder_tenant_overview` RPC feeds cards + table (no N+1). Security-definer RPC revokes from public/anon/authenticated, EXECUTE granted to `service_role` only — defense-in-depth behind the per-route email gate. Reuse-over-duplication exemplary: `activateInvoice` routes through `activation-core.applyOutcome` (provider `manual`, idempotent key `manual:<tenant>`). Constraint widenings additive/back-compat. /founder page-level gate deliberately outside proxy matcher; proxy `paused` enforcement correct. |
| Engineering | 10 | Type-strict — zero `: any`/`as any`; admin client typed via `ReturnType<typeof createAdminClient>`. Email gate robust: server-only (no `NEXT_PUBLIC_`), fails closed, case/whitespace-normalized both sides, enforced on every mutation via shared `runFounderMutation`; non-admins get 404 (no leak). Zod UUID validation (400 on bad id), DB errors propagated, real idempotency. `.env.example` adds only `PLATFORM_ADMIN_EMAILS=` — no secret prefix (L6 clean). |
| QA | 9 | All six ACs (AC-8.1..8.6) covered by 24 `@AC-8.x` tagged cases across 5 files. L1 route integration POSTs every handler (200 + 404 non-admin + 400 bad-id, asserting mutation in/not-invoked); L2 live DB suite serialized, unique-tenant, afterAll cleanup, CI `--no-file-parallelism --retry=2`; empty-state + fail-closed paths explicit; L7 manual 375px + axe walk cited (commit 6ae39f1, 0 violations). 381/381 passing. −1: S8 ACs defined as a backlog one-liner (brd.md:164) not enumerated AC-8.1..8.6, so traceability rests on test-tag convention. |
| UX | 9 | RTL-first: `text-right` tables, status pills, empty states, `aria-busy` cards, `role="alert"` banner, busy-state buttons, DD/MM/YYYY dates, JOD as د.أ, all tap targets `min-h-[44px]`. Jordanian colloquial tone (not MSA). 17 `<bdi>` wrappers (L5) + verified axe 20/0 contrast. −1: components rely on inherited `dir`/font rather than local `dir="rtl"`; paused-page CTA is mailto-only with no WhatsApp fallback for the WhatsApp-first ICP. |

**Composite: (9 + 10 + 10 + 9 + 9) × 2 = 94 ✅**

**Decision: Ship** (≥90, no hat <8).

## Lesson caps — all satisfied (none apply)
- **L1** (QA cap 8): `tests/story-8-founder-routes.test.ts` POSTs to each route handler ✓
- **L2** (QA cap 8): live DB suite `.sequential` + unique-tenant + afterAll; CI `--no-file-parallelism --retry=2` ✓
- **L3** (Arch cap 8): CI run for PR #12 confirmed green (quality + integration) ✓
- **L4** (Product cap 8): user flow reachable; no orphaned route/CTA ✓
- **L5** (UX cap 8): 17 `<bdi>` on all dynamic mixed-script values ✓
- **L6** (Eng cap 8): no literal secret prefix in tracked files ✓
- **L7** (QA cap 9): manual 375px + axe-core walk cited (found+fixed 15-node contrast) ✓

## Next
PR #12 → merge to main (founder authorized). Then post-merge trunk gates + tracker S8 → ✅ shipped.
