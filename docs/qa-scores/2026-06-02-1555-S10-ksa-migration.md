# Score — S10 KSA data migration (PDPL) — 2026-06-02 15:55

**Branch:** `feat/s10-ksa-migration` vs `main`
**PR:** #18
**Commit scored:** `28c32f7` (head; feature `04a023e`)
**CI:** green on PR #18 head — run 26820835248 (quality 1m34s ✓ · integration/live-DB 3m24s ✓), confirmed BEFORE scoring (L3 / retro-P1)

## Composite

| Hat | Score | Notes |
|-----|:-----:|-------|
| Product | 9 | Complete PDPL residency-migration capability — `tenants.data_region` pointer → per-tenant export → import → verify → cutover — reachable end-to-end through the founder panel "النقل والامتثال (KSA)" tab. Founder-internal tool (not customer-facing); the live AWS me-central-1 target is intentionally founder-gated (`createKsaTarget()` lazy). |
| Architecture | 9 | Clean `MigrationTarget` seam mirroring the established S9 Chromium and S6 PaymentProvider patterns; deterministic content-hash parity (canonical recursive key-sort, arrays order-preserved for vectors, SHA-256); monotonic state machine prevents double-cutover; non-destructive (source stays the reference); lazy KSA client keeps CI/local/route boot clean. RPC return-shape change done correctly (DROP+CREATE). −1: the live me-central-1 cutover path is documented as a runbook but never exercised against a real KSA project. |
| Engineering | 9 | Typed `MigrationError` codes (already_cutover/not_verified/verify_failed), no `any`, no secret prefixes in tracked files (L6), named RLS `tenant_migrations_tenant_isolation`, idempotent update-by-id-else-insert store, injection-safe. Full suite 476/476 / 57 files green; tsc + eslint + `next build` clean; `npm audit --omit=dev --audit-level=high` = 0 high/critical (2 moderate postcss/next documented). |
| QA | 9 | 46/46 S10 tests (bundle 6 · verify+state 9 · run 8 · routes 11 · view 9 · live-DB 3). Route seam covers every HTTP branch — 200/400/404/409/422/500 (L1). Live L2 suite serialized, unique-tenant, proves P0 cross-tenant export exclusion + `tenant_migrations` RLS isolation against real local Supabase (L2). ACs enumerated in `docs/stories/S10.md` before coding (L8). **L7 cap applied (→9):** S10 ships a UI surface (migration tab) but verification was cited as *static* (computed AA contrast, `min-h-[44px]` audit, `<bdi>` audit, reuse of the axe-cleared S8 panel chrome), not a live axe-core walk on the new surface. |
| UX | 9 | `<bdi>` on every dynamic mixed-script value from first write — `r.name`, `regionLabel(r.data_region)`, `ctrl.statusLabel`, `ctrl.cta.label`, `owner_email` (L5). Both new CTAs pass WCAG-AA: migrate `bg-brand-amber` #B45309 white 5.02:1 (hover #92400E 7.09:1), cutover `bg-indigo-700` #4338CA white ≈7.91:1 — no known a11y failure on the story's own CTA (L9). All interactive elements `min-h-[44px]` (≥44px target). RTL-correct Arabic labels. |

**Composite: (9 + 9 + 9 + 9 + 9) × 2 = 90 ✅ — no hat below 8.**

## Decision: Ship

Proceed to `/7-eo-ship story-10` — re-score HEAD, build/test/audit, finalize PR #18. **STOP at the merge gate:** merge requires explicit founder authorization naming the PR (a bare "y" is insufficient).

## Lesson caps audit

| Lesson | Caps | Status for S10 |
|--------|------|----------------|
| L1 | QA 8 | Satisfied — `story-10-routes.test.ts` POSTs every branch |
| L2 | QA 8 | Satisfied — live suite serialized + unique-tenant + cross-tenant exclusion; CI `--no-file-parallelism` |
| L3 | Arch 8 | Satisfied — CI green on PR #18 head confirmed before scoring |
| L4 | Product 8 | Satisfied — migration CTA → `runMigration` → route handler, no orphan |
| L5 | UX 8 | Satisfied — `<bdi>` from first write |
| L6 | Eng 8 | Satisfied — no secret prefixes in `.env.example`/docs |
| **L7** | **QA 9** | **Applied** — static verification cited, not a live axe-core walk on the new tab |
| L8 | QA 9 | Satisfied — ACs enumerated in `docs/stories/S10.md` |
| L9 | UX 9 | Satisfied — both new CTAs AA-pass |

## AC coverage (6/6 @AC-tagged)

- **AC-10.1** region pointer (`tenants.data_region`) + panel region label
- **AC-10.2** export bundle pulls every tenant-scoped table (live L2)
- **AC-10.3** import into KSA target via `MigrationTarget` seam
- **AC-10.4** verify parity + monotonic state machine (no double-cutover)
- **AC-10.5** confirmed residency flip (cutover)
- **AC-10.6 (P0)** cross-tenant export exclusion + `tenant_migrations` RLS isolation (live L2)

## Notes / deferrals (founder-gated, not blocking ship-score)

- Live AWS me-central-1 cutover runbook — needs the KSA project provisioned (`KSA_SUPABASE_URL/_SERVICE_KEY`); deferred until the first KSA paying lab.
- PR #18 carries the still-open PR #17 a11y bookkeeping commit (`9889972`) in its diff until #17 merges — cosmetic, no functional overlap.
- Live Contabo cutover (migrations 0010+0011+0012) remains founder-gated.
