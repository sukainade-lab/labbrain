# LabBrain — Dev Progress

> Read by `/eo-guide` on every session. The filesystem is the source of truth; this is a view.
> Bootstrapped 2026-06-01 from EO-Brain phases 0–4. SaaSfast mode M3. Payments: Stripe. Deploy: Contabo.

## Weekend MVP (Stories S1–S5)

| Story | Title | Loops | ACs | Status |
|-------|-------|-------|-----|--------|
| S1 | Lab Onboarding & Auth | auth, compliance | AC-1.1…1.6 (6) | ✅ shipped 2026-06-01 · 100/100 · PR #1 merged (`2c93614`) · runtime deploy deferred to S5 |
| S2 | Document Upload & Indexing | domain, compliance | AC-2.1…2.6 (6) | ✅ shipped 2026-06-01 · 100/100 · PR #2 merged (`22f6b30`) · runtime deploy deferred to S5 |
| S3 | Bilingual Q&A with Mandatory Citation | domain | AC-3.1…3.7 (7) | ✅ shipped 2026-06-01 · 100/100 · PR #3 merged (`1d3af51`) · runtime deploy deferred to S5 |
| S4 | Pricing, Stripe Checkout & Activation | money, notify | AC-4.1…4.5 (5) | ✅ shipped 2026-06-02 · 100/100 · PR #4 merged (`d562f19`) · 154 tests / 7 S5-skip · runtime deploy deferred to S5 |
| S5 | Deploy, Health & Observability | deploy, observability, compliance | AC-5.1…5.7 (7) | ✅ shipped 2026-06-02 · 100/100 · PR #5 merged (`9bc1af3`) · both CI jobs green (quality + live-DB integration) → L3 cap lifted Arch 8→10 · 231 tests · live Contabo cutover deferred (founder-gated: VPS + secrets) |

**Totals:** 5 stories · 31 ACs · all 7 loops covered. **🎉 Weekend MVP complete — all 5 stories shipped.**

## v2 (Phase 2 — Stories S6–S16)

| Story | Title | Loop | Status |
|-------|-------|------|--------|
| S6 | Tap Payments card integration (JOD + KWD + SAR) | money | ✅ shipped 2026-06-02 · **composite 90** (P9 A9 E9 Q9 U9) · **PR #8 merged** (`8a80179`) — founder-authorized · AC-6.1…6.6 green · 6/6 ACs @AC-tagged · post-merge trunk gates green (tsc · 287/287 tests · `next build` · audit documented) · score `docs/qa-scores/2026-06-02-0214-S6-tap-payments.md` · live Contabo cutover deferred (founder-gated: VPS + secrets) |
| S7 | SMS 2FA via Unifonic (Jordan numbers) | auth | ✅ shipped 2026-06-02 · **composite 96** (P10 A9 E10 Q9 U10, no hat <8, no lesson caps) · **PR #9 merged** (`5985c80`) — founder-authorized · AC-7.1…7.6 green · 6/6 ACs @AC-tagged · CI green on PR #9 (quality + live integration; one transient Supabase/Kong 502 cleared by job re-run, retro P1) · post-merge trunk gates green (tsc · 350/350 tests) · score `docs/qa-scores/2026-06-02-1144-S7-sms-2fa.md` · live Contabo cutover deferred (founder-gated: VPS + secrets) |
| S8 | Founder super-admin panel | domain | ✅ shipped 2026-06-02 · **composite 94** (P9 A10 E10 Q9 U9, no hat <8, no lesson caps) · **PR #12 merged** (`017c5bc`) — founder-authorized · AC-8.1…8.6 green · 6/6 ACs @AC-tagged · CI green on PR #12 head `50af591` (quality + live integration) · **L7 walk cited:** live 375px + axe-core → 1 🔴 (15-node WCAG-AA contrast) found+fixed (`6ae39f1`) → re-run **20 passes / 0 violations** · post-merge trunk gates green (lint · tsc · **381/381 tests** · `next build` · audit: 0 high/critical prod, 2 moderate documented) · score `docs/qa-scores/2026-06-02-1530-S8-founder-panel.md` · live Contabo cutover deferred (founder-gated: VPS + secrets + `supabase db push` incl. 0010) |
| S9 | Audit export — Q&A log as PDF | domain | ✅ shipped 2026-06-02 · **composite 90** (P9 A9 E9 Q9 U9, no hat <8) · **PR #14 merged** (`b90c3c9`) — founder-authorized ("merge PR #14") · AC-9.1…9.6 green · 6/6 ACs @AC-tagged · CI green on head `b00c19e` (run 26817077242 — quality + live integration) · post-merge trunk gates on `main` `b90c3c9`: lint ✓ · tsc ✓ · **425/425 tests / 50 files** ✓ · `next build` ✓ · `npm audit --omit=dev --audit-level=high` = 0 high/critical, 2 moderate documented · **/5-eo-score** first pass 88 (UX 8: export CTA white-on-`#D97706` 3.19:1, fails WCAG-AA) → **/6-eo-bridge-gaps** (UX): /admin CTAs → amber-700 `#B45309` (white 5.02:1 ✓, hover `#92400E` 7.09:1 ✓) + invite role `<select>` aria-label (commit `8eef618`) → re-ran axe-core live /admin @375px → **0 violations / 24 passes** → UX 9 → 90. Score `docs/qa-scores/2026-06-02-1730-S9-audit-export.md`. App-wide remaining ~10 `#D97706` files → spun-off a11y follow-up. AC-9.1 owner/admin gate (401/403) + RLS · AC-9.2 chronological rows w/ citations + asker + DD/MM/YYYY HH:MM · AC-9.3 inclusive from/to, reversed/malformed→400 · AC-9.4 A4 RTL PDF via puppeteer-core→system Chromium (seam-mocked in CI), empty→valid PDF · AC-9.5 application/pdf attachment `labbrain-audit-<slug>-<YYYYMMDD>.pdf` + reachable owner/admin UI · AC-9.6 `audit_exports` row (migration 0011, named RLS). L1/L2/L4/L5/L6/L7/L8 honored. live Contabo cutover deferred (founder-gated: VPS + secrets + `supabase db push` incl. 0010 + 0011). | `/5-eo-score` first pass **88** (UX 8: export CTA shipped white-on-`#D97706` 3.19:1, fails WCAG-AA) → `/6-eo-bridge-gaps` (UX): /admin CTAs darkened to amber-700 `#B45309` (white 5.02:1 ✓, hover `#92400E` 7.09:1 ✓) + invite role `<select>` `aria-label` (commit `8eef618`) → **re-ran axe-core on live /admin @375px → 0 violations / 24 passes** → UX 9 → **90**. CI green on PR #14 (quality + live integration) confirmed BEFORE scoring (retro P1). Score `docs/qa-scores/2026-06-02-1730-S9-audit-export.md`. App-wide remaining ~10 `#D97706` files → spun-off a11y follow-up. Review clean otherwise (commit `6738d1c` + review fixes). AC-9.1…9.6 enumerated in `docs/stories/S9.md` (L8). AC-9.1 owner/admin gate (401/403) + RLS tenant-scope · AC-9.2 chronological rows w/ citations + asker + DD/MM/YYYY HH:MM · AC-9.3 inclusive from/to, reversed/malformed→400 · AC-9.4 A4 RTL PDF via puppeteer-core→system Chromium (seam-mocked in CI), empty→valid PDF · AC-9.5 application/pdf attachment `labbrain-audit-<slug>-<YYYYMMDD>.pdf` + reachable owner/admin admin-page UI · AC-9.6 `audit_exports` row (migration 0011, named RLS). Dockerfile installs chromium + Noto Arabic/Latin fonts + sets `PUPPETEER_EXECUTABLE_PATH`. **Tests: 40/40 mocked passing** (validation 14 · report-html 12 · render-pdf 4 · routes 10) + live-DB suite `story-9-audit.test.ts` skipIf-no-Supabase (L2). Gates: tsc ✓ · eslint ✓. **/4-eo-review:** 2 review findings fixed — 🔴 AC-9.6 missing @AC-tag (added to 2 route tests, 6/6 ACs now @AC-tagged), 🟡 L5 lab-name not `<bdi>`-wrapped in report h1 (fixed + test added). Source clean: no secrets, no `any`, RLS user-scoped, HTML-escaped, filename injection-safe, error handling present. **L7 walk cited:** live owner session (signup→login→/admin) at 375px → export form owner-reachable (L4) · tap targets ≥44px (47–48px) · date inputs aria-labeled + dir="ltr" ✓ · axe-core (wcag2a+2aa) → 2 violations, **both pre-existing / non-S9-regression**: (1) color-contrast white-on-`#D97706` 3.19:1 = shared primary-button pattern across 12 files / all shipped stories S1–S8 (S9 export button merely inherits it); (2) select-name on the S1 invite-form role `<select>`. Both spun off to a dedicated app-wide a11y follow-up task (kept S9's button consistent, no one-off divergent shade). L1/L2/L4/L5/L6/L8 honored. |
| S10 | KSA data migration → AWS me-central-1 (PDPL) | compliance | ✅ shipped 2026-06-02 · **composite 90** (P9 A9 E9 Q9 U9, no hat <8) · **PR #18 merged** (`9ddfa28`) — founder-authorized ("merge PR #17 first, then merge PR #18") · rebased onto post-#17 main (dropped redundant a11y commit), CI re-confirmed green on `d4f0803` (run 26821626507 — quality + live integration) BEFORE merge (L3) · post-merge trunk gates on `main` `9ddfa28`: lint ✓ · tsc ✓ · **476/476 tests / 57 files** ✓ · `next build` ✓ (`/api/founder/tenants/[id]/migrate` + `/cutover` ƒ) · `npm audit --omit=dev --audit-level=high` = 0 high/critical, 2 moderate documented · AC-10.1…10.6 green · 6/6 ACs @AC-tagged · score `docs/qa-scores/2026-06-02-1555-S10-ksa-migration.md` · per-tenant export→verify→cutover via `MigrationTarget` seam · migration `0012_tenant_migrations.sql` · L7 cap applied at QA (static verification cited) · live AWS me-central-1 cutover + live Contabo cutover deferred (founder-gated: KSA project + VPS + secrets + migrations 0010+0011+0012) | (bundle 6 · verify+state 9 · run 8 · routes 11 · view 9 · live-DB 3; full suite 476/476 / 57 files) · per-tenant export→import→verify→cutover via `MigrationTarget` seam (real me-central-1 founder-gated, live-EU reader + fake target in CI — sibling to S9 Chromium / S6 provider seam) · migration `0012_tenant_migrations.sql` (run-log + `tenants.data_region` pointer + RLS + overview RPC extended w/ `data_region`+`migration_status`) · deterministic content-hash parity, verify-before-cutover, non-destructive · founder-panel migration tab (L4 reachable · L5 `<bdi>` on regions/status/hash · AA CTAs) · live L2 proves cross-tenant export exclusion + `tenant_migrations` RLS isolation (L1/L2/L4/L5/L6) · **review:** 0 🔴 / 0 🟡 blocking; secret+`any` scan clean, 6/6 ACs @AC-tagged, elegance block in `04a023e`; **L7 walk cited** (static): all migration-tab tap targets `min-h-[44px]`, AA contrast on both new CTAs (migrate brand-amber #B45309 5.02:1 / hover #92400E 7.09:1 · cutover indigo-700 #4338CA ≈7.91:1), `<bdi>` on every mixed-script region/status/label, tab reuses axe-cleared S8 chrome · **next: confirm CI green (L3) → `/5-eo-score`** |
| S11 | Air-gap mode (self-hosted LlamaParse + Ollama) | domain, compliance | ✅ shipped 2026-06-02 · **composite 90** (P9 A9 E9 Q9 U9, no hat <8, **no lesson cap bit** — L3/L4/L5/L6/L7/L8/L9 all satisfied proactively; L1/L2 N/A) · **PR #21 merged** (`2678e4d`) — founder-authorized ("merge PR #21") · CI green on head `fda2aa4` (run 26826634846 — quality + integration) before merge (L3) · post-merge trunk gates on `main` `2678e4d`: tsc ✓ · **509/509 tests / 59 files** ✓ · `npm audit --omit=dev --audit-level=high` = 0 high/critical, 2 moderate documented · AC-11.1…11.8 green, all 8 `@AC-11.N`-tagged · score `docs/qa-scores/2026-06-02-1724.md` · **live Contabo cutover (self-hosted Ollama + LlamaParse) deferred (founder-gated: host + on-prem secrets)** · review CLEAN (0 🔴), TDD on `feat/s11-airgap-mode` · pure resolver `src/lib/ai/inference-mode.ts` (AC-11.1/11.5/11.6, fail-closed, dim=1536) · seams swapped to resolver: `embeddings.ts`/`answer.ts`/`llamaparse.ts` (AC-11.2/11.3/11.4) · operator indicator `components/ops/inference-mode-badge.tsx` on admin + founder (AC-11.8, `<bdi>` L5) · env contract `.env.example`+`docs/env-contract.md` (AC-11.7, L6 names-only) · **L7 walk PASSED**: `npm run axe:walk` 0 violations on `/dashboard`+`/admin`+`/founder` @375px (caught + fixed a real AA `<dt>` contrast slate-500→slate-400) · **zero new npm deps** · no DB migration · fix: `tailwind.config.ts` require()→ESM import (+`types/tailwindcss-rtl.d.ts`) so webpack-dev PostCSS loads the .ts config on Windows · gates: tsc✓ eslint✓ **509/509**✓ build✓ audit(--omit=dev,high)✓ · tests `story-11-inference-mode.test.ts` (26) + `story-11-airgap-seams.test.ts` · Status stays 🔨 coding until `/4-eo-review` |
| S12 | Multi-tenant branding (lab logo) | domain | ✅ shipped 2026-06-02 · **composite 94** (P9 A9 E10 Q9 U10, no hat <8, **no lesson cap bit** — L1–L9 all satisfied proactively; first story to clear the 90 plateau since S8) · **PR #23 squash-merged (`4680092`)** — founder-authorized ("merge PR #23") · CI green on head `b0e2b9e` (run 26830185748 — quality + integration) before merge (L3) · post-merge trunk gates on `main` `4680092`: tsc ✓ · **547/547 tests / 63 files** ✓ · `next build` ✓ · `npm audit --omit=dev --audit-level=high` = 0 high/critical (2 moderate next/postcss documented) · score `docs/qa-scores/2026-06-02-1838-S12-branding.md` · **L7: live `npm run axe:walk` 0 violations @375px across /dashboard /admin /founder /settings** (first net-new UI surface actually walked) · **Decision: Ship** · `/4-eo-review` CLEAN → Status 🔨 coding → 🧪 scoring on `feat/s12-branding` (`ef8ce5a`) · **review fix:** logo.ts service layer had 0 direct coverage (route tests mock it out; live L2 hits raw Storage RLS) + AC-12.4/12.6 untagged → added `tests/story-12-branding-logo.test.ts` (13: uploadLogo replace-in-place/rollback/unsupported-mime · removeLogo null-then-delete · publicLogoUrl · logoStoragePath · resolveSidebarBrand fallback+alt) and extracted `resolveSidebarBrand` pure helper (layout.tsx now a thin view) · **7/8 ACs @AC-tagged** (12.1·12.2·12.3·12.4·12.5·12.6·12.8); AC-12.7 = cited L7 axe-walk evidence (precedent AC-11.7) · source scan clean (no secrets, no `any`) · elegance-pause in `ef8ce5a` · gates: tsc✓ eslint✓ **547/547 / 63 files**✓ `next build`✓ audit(--omit=dev,high) 0 high/critical (2 moderate documented) · **L7 re-walk on HEAD: 0 violations across /dashboard /admin /founder /settings @375px** · **Next: confirm CI green (L3) → `/5-eo-score story-12`** · **(prior `/3-eo-code` `b333cc5`)** · AC-12.1…12.8 implemented · spec `docs/stories/S12.md` (L8) · migration `0013_tenant_branding.sql` (`tenants.logo_path` + public `branding` bucket + tenant-path RLS, applied local) → pure `validation/branding.ts` (PNG/JPEG/WebP/SVG ≤512KB, octet-stream ext fallback) → `lib/branding/logo.ts` (replace-in-place upload / remove / public URL) → admin-only `POST`(201/400/413/403/401)+`DELETE`(200/403/401) `/api/branding` → `BrandingSettings` section in `/settings` (RTL, ≥44px, `<bdi>` lab name, mirrors `<MfaSettings>`) → tenant logo+name in `(app)/layout.tsx` sidebar w/ logo→name→"LabBrain" fallback (`<bdi>` L5, Arabic alt + fixed dims) · **Tests: 25 new green** (11 route-seam L1 · 10 validation unit · 4 live storage-isolation L2 `story-12-branding-storage-db.test.ts` — proves cross-tenant write/list/delete denied by RLS) · full suite **534/534 / 62 files** · gates: tsc✓ eslint✓ `next build`✓ `npm audit --omit=dev --audit-level=high`=0 high/critical (2 moderate next-bundled postcss documented) · **L7 walk cited**: `npm run axe:walk` extended to `/settings` → 0 violations across `/dashboard`+`/admin`+`/founder`+`/settings` @375px · no new npm deps · no new env vars (bucket public-read, reuses service-role key) · elegance: discriminated-union `resolveAdmin`, patterns reused (invitations gate · documents validation · documents-bucket storage flow · MfaSettings UI), SVG served only via `<img src>` (no inline → no stored-XSS) · Status stays 🔨 coding until `/4-eo-review` · **Next: `/4-eo-review story-12`** |
| S13 | Document versioning + re-index | domain | ✅ shipped 2026-06-02 · **composite 90** (P9 A9 E9 Q9 U9, no hat <8) · **PR #25 squash-merged (`e02b114`)** — founder-authorized ("merge PR #25") · CI green on shipped head `ee07687` (run 26833156553 — quality + integration) confirmed BEFORE merge (L3) · post-merge trunk on `main` `e02b114`: **569/569 tests / 66 files** · **no production deploy — Contabo/AWS me-central-1 cutover founder-gated** (prod `supabase db push` would include migrations 0010–0014) · `/5-eo-score story-13` → **Decision: Ship** · score `docs/qa-scores/2026-06-02-1905-S13-document-versioning.md` · `/4-eo-review` clean (0 🔴/0 🟡: no secrets/`any`, AC-13.1–13.7 @AC-tagged + 13.8 cited L7 walk, elegance block in `1e951ec`) · AC-13.1…13.8 in `docs/stories/S13.md` (L8) · **core insight: Q&A history is already decoupled** (`queries.citations` jsonb snapshot, no FK to `documents`) so replace = safe re-index keeping same `document_id`, not a history migration · migration `0014` (`documents.version`+`updated_at` + `replace_document_chunks` RPC for atomic delete-old+insert-new chunk swap, security-definer, tenant-guarded) → `replaceDocument`/`processReplace` in `ingest.ts` (parse+embed→atomic swap→bump version; failure keeps prior file+chunks+version, fail-safe AC-13.5) → `PUT /api/documents/[id]` (L1 401/404/403/400/413/200 — 9 seam tests) → per-row "استبدال" control + `نسخة N` badge on `(app)/documents/page.tsx` (L5 `<bdi>`/L9 ≥44px, reuses active-poll) → live L2 history-survives-replace + atomic-swap + cross-tenant-guard (4 tests) · **tests: 11 S13 (9 service + 9 route-seam + 4 live-DB = full suite 569/569 / 66 files, `--no-file-parallelism`)** · gates: tsc ✓ · eslint ✓ · `next build` ✓ · `npm audit --omit=dev` 2 moderate pre-existing (below high gate) · **L7: `/documents` added to axe-walk → live walk @375px 0 violations across /dashboard /documents /admin /founder /settings** · elegance pause done · next `/4-eo-review story-13` |
| S14 | Slack/email weekly digest | notify |
| S15 | API access (LIMS/ERP integration) | domain |
| S16 | Webinar demo flow | domain |

## Redesign Epic — Two-Panel Workspace (S18–S21)

> Retrofit of the shipped flat `/documents` + plain Q&A into the two-panel workspace from
> `EO-Brain/5-CodeHandover/artifacts/product-demo.jsx` (updated BRD S2/S3/S3b/S4b). Light theme
> BG `#F8FAFC` · navy `#1B2A3D` · amber `#D97706`. Founder decision: existing prod docs migrate into
> the Existing Services panel (`panel_type='existing'`, zero data loss).

| Story | Title | Loop | Status |
|-------|-------|------|--------|
| S18 | Two-panel document workspace retrofit (BRD S2) | domain, compliance | 🧪 scoring · **composite 90** (P9 A9 E9 Q9 U9, no hat <8 → Ship) · **PR #37** · CI green run 27206935183 on `d11e963` (L3 pre-score) · AC-2.1…2.8 green · migration `0017` (service_tabs + panel/tab/section cols + backfill) **prod DDL founder-gated** · tests 606/606 (S18: 22 new) · tsc/eslint/build clean · L7 axe @375px `/documents` 0 viol · `/4-eo-review` clean · score → `docs/qa-scores/2026-06-09-1245-S18-workspace.md` · next `/7-eo-ship` (STOP at merge gate) |
| S19 | Source-filter pills + panel-scoped retrieval (BRD S3) | domain | ⬜ not started · sequence AFTER PR #36 merges (touches `ask.ts`/RPC) |
| S20 | SOP Draft Generator (new-service tabs only, BRD S3b) | domain | ⬜ not started |
| S21 | Founder dynamic-pricing admin (platform_settings, BRD S4b) | money | ⬜ not started |

## Legend
⬜ not started · 🟡 in progress · ✅ shipped (PR merged + score ≥90)

## Retros
- 2026-06-02 — `docs/retros/2026-06-02-sprint9-s13.md` (Sprint 9 / S13, composite 90, flat all-9 — back to infra/data baseline after S12's 94, **exactly as predicted**; 90↔94 swing now a characterized story-mix signal; 4th consecutive zero-round-trip ship; 0 cap bits; 0 new lessons; no production deploy)
- 2026-06-02 — `docs/retros/2026-06-02-sprint8-s12.md` (Sprint 8 / S12, composite 94 — **breaks the two-sprint 90 plateau**; E10+U10 from clean first-pass L7 walk on net-new /settings surface; story-selection thesis confirmed; 0 cap bits; 0 new lessons)
- 2026-06-02 — `docs/retros/2026-06-02-sprint7-s11.md` (Sprint 7 / S11, composite 90, flat all-9; focus: none weak — pick UI stories that can earn 94+; L7 harness paid off on first use)
- 2026-06-02 — `docs/retros/2026-06-02-sprint6-s10.md` (Sprint 6 / S10, composite 90; focus: seeded-session axe-walk harness — shipped PR #20)
- 2026-06-02 — `docs/retros/2026-06-02-sprint5-s9.md` (Sprint 5 / S9, composite 90; L9 minted)
- 2026-06-02 — `docs/retros/2026-06-02-sprint4-s8.md` (Sprint 4 / S8, composite 94; L8 minted)
- 2026-06-02 — `docs/retros/2026-06-02-sprint3-s7.md` (Sprint 3 / S7, composite 96)

## Next
**Sprint 8 — S12 Multi-tenant branding (lab logo) REVIEW CLEAN → 🧪 scoring.** `/4-eo-review` complete on `feat/s12-branding` (`ef8ce5a`): diff vs `main` = 13 files (+`tests/story-12-branding-logo.test.ts`). **One real coverage gap caught + fixed before scoring** — route-seam tests mock `@/lib/branding/logo` out entirely and the live L2 test exercises raw Storage RLS, so `logo.ts` had zero direct coverage and AC-12.4/12.6 had no `@AC` tag, despite S12.md promising a "Service (mocked admin client)" layer. Added 13 service-layer tests (uploadLogo replace-in-place + rollback-on-DB-failure + unsupported-mime throw · removeLogo null-then-delete · publicLogoUrl · logoStoragePath · resolveSidebarBrand fallback+alt) and extracted `resolveSidebarBrand` pure helper (layout.tsx now a thin view). **7/8 ACs @AC-tagged** (12.1·12.2·12.3·12.4·12.5·12.6·12.8); AC-12.7 = cited L7 axe-walk evidence (precedent AC-11.7). Source scan clean (no secrets, no `any`); elegance-pause in `ef8ce5a`. Gates: tsc✓ eslint✓ **547/547 / 63 files**✓ `next build`✓ audit(--omit=dev,high) 0 high/critical (2 moderate documented). **L7 re-walk on HEAD: 0 violations across `/dashboard`+`/admin`+`/founder`+`/settings` @375px.** Punch list: 0 🔴 / 0 🟡 remaining.

**`/5-eo-score story-12` COMPLETE → composite 94 ✅ (P9 A9 E10 Q9 U10), Decision: Ship.** First story to break the two-sprint 90 plateau — driven by the first **live** `npm run axe:walk` on a net-new UI surface (`/settings`) returning 0 violations, layered tests (route + service + validation + live RLS), and every lesson cap (L1–L9) proactively satisfied (no cap bit). Report `docs/qa-scores/2026-06-02-1838-S12-branding.md`; trend.csv row appended (`score-ci-green-pr23-awaiting-merge`).

Next: **`/7-eo-ship story-12` → STOP at merge gate.** Re-score HEAD, run final build/test/audit, post ship verdict to PR #23, then HALT — merge of PR #23 to `main` only on explicit founder authorization naming the PR; no production deploy. Founder-gated standing: 🔴 rotate the GitHub PAT (#1 risk) · approve `.claude/settings.json` SessionStart hook · live Contabo + AWS me-central-1 cutovers.

**Founder-gated, not run (surface, do not work around):**
1. 🔴 **Rotate the GitHub PAT** — pasted plaintext earlier, still live/active for `sukainade-lab` (#1 risk).
2. `.claude/settings.json` SessionStart hook (auto-load lessons) still awaiting explicit founder approval.
3. **Live Contabo cutover** (S11 air-gap path needs self-hosted Ollama + LlamaParse alongside; classified-network host) + **live AWS me-central-1 cutover** (S10 KSA/PDPL). Both need host + on-prem/prod secrets only the founder holds; `deploy.sh` runs on the VPS, prod Supabase migrated via `supabase db push` (incl. 0010+0011+0012), `MIGRATIONS_APPLIED=1`, then `GET /api/health` → 200 live.

---
**Last updated:** 2026-06-09 · **Last command:** `/7-eo-ship story-18` · **Current focus:** Redesign Epic (S18 two-panel workspace → S19 filtered Q&A → S20 SOP generator → S21 dynamic-pricing admin). **`/7-eo-ship story-18` — Ship gate ✅ PASSED → ⏸️ STOPPED at merge gate (PR #37 awaiting founder-named merge authorization).** Re-scored HEAD `402ca66` = **composite 90** (P9 A9 E9 Q9 U9, no hat <8; only commit since `/5-eo-score` is bookkeeping `402ca66` = score md + trend + tracker, source unchanged → score stands). Final gates on `402ca66`: **`next build`** ✓ (`/documents` ƒ) · **Vitest 606/606 / 71 files** ✓ · **`npm audit --omit=dev --audit-level=high`** = 0 high/critical (2 moderate next/postcss documented) · **CI green** run 27207259703 on `402ca66` (PR Quality Gate `success`) confirmed BEFORE posting verdict (L3/retro-P1). Ship-gate verdict posted as PR #37 comment (`#issuecomment-4659899161`): "Ship gate: ✅ PASS (composite 90)" + ⛔ holding-at-merge-gate block. **Status stays 🧪 scoring** until the founder authorizes the merge naming PR #37 (a bare "y" is insufficient — classifier-enforced). On merge authorization → squash-merge PR #37 → flip S18 to ✅ shipped + add `shipped-pr37-<sha>` trend row → `/8-eo-retro`. **Migration `0017` prod apply stays founder-gated** (local-only); **no production deploy** (Contabo cutover founder-gated). _Prior (`/5-eo-score story-18`):_ **S18 scored composite 90 (P9 A9 E9 Q9 U9, no hat <8) → Decision: Ship.** CI confirmed green on **PR #37** (run 27206935183 — PR Quality Gate `success` on head `d11e963`) **before** scoring (L3/retro-P1). **Every applicable lesson cap audited & satisfied — none bit:** L1 (route-seam per branch) ✓ · L2 (serialized unique-tenant live cascade/isolation/tagging) ✓ · L3 (CI green pre-score) ✓ · L4 (all 3 service-tab verbs + tagged upload have UI callers) ✓ · L5 (`<bdi>` on filenames + lab tab names) ✓ · L6 (no secret prefixes, no new env) ✓ · L7 (live `npm run axe:walk` 0 viol @375px `/documents`) ✓ · L8 (AC-2.1…2.10 enumerated in `docs/stories/S18.md`) ✓ · L9 (0 a11y viol on redesigned surface) ✓. **−1 hats:** Product (S19/S20 differentiators deferred + inert SOP stub), Architecture (dual section vocab app-side not DB), Engineering (solid, no standout), QA (no explicit empty-state UI assertion), UX (native `prompt()`/`confirm()` for tab-add+deletes bypass the design system — holds at 90 baseline vs 94). **No new lesson** (clean first-pass 90; native-dialog polish gap noted, revisit if it recurs). Score → `docs/qa-scores/2026-06-09-1245-S18-workspace.md`; trend.csv row `score-ci-green-pr37-awaiting-merge`. Next: **`/7-eo-ship story-18`** — re-score HEAD + build/test/audit + post ship verdict to PR #37 → **STOP at merge gate** (merge of PR #37 needs explicit founder authorization naming the PR; a bare "y" is insufficient — classifier-enforced). **Migration `0017` prod apply stays founder-gated** (local-only); no production deploy. _Prior:_ **`/4-eo-review story-18`** — S18 review CLEAN → Status flipped 🔨 coding → 🧪 scoring on `feat/s18-workspace` (commit `d11e963`, single-commit diff vs `main` = 13 files +1835/−51). **Review findings: 0 🔴 / 0 🟡.** Secret scan across `src/**/*.{ts,tsx}` clean (only `process.env` reads, no hardcoded keys, none in S18 files); no `any` types in the S18 globs (`lib/workspace`, `lib/validation/workspace`, `lib/documents/ingest`, `api/service-tabs`, `api/documents`); **BRD traceability:** S18 tests carry `@AC-2.1` (13) · `@AC-2.4` (10) · `@AC-2.5` (2) · `@AC-2.8` (5) · `@AC-2.9` (3), `docs/stories/S18.md` enumerates AC-2.1…2.10 (L8); **L1** route-seam tests per branch (GET/POST/DELETE 200/201/400/401/404 + documents POST foreign/missing-tab 400) ✓ · **L4** all three `/api/service-tabs` verbs + tagged upload have UI callers in `documents/page.tsx` ✓ · **L5** `<bdi>` on filenames AND lab-named service tabs ✓ · **L9** ≥44px controls (tab pills, ✕, +, upload, replace/delete) with `aria-label` on icon-only buttons ✓; **L7** live `npm run axe:walk` @375px `/documents` 0 violations cited; elegance-pause block present in `d11e963`; SaaSfast M3 consistent. Gates green: 606/606 tests (22 new S18), tsc/eslint/`next build` clean; `npm audit` 2 moderate pre-existing next/postcss (no new deps, carried exception). **Migration `0017` prod apply still founder-gated** (local-only so far). Next: `/5-eo-score story-18`. AC spec at `docs/stories/S18.md`; branch `feat/s18-workspace` off `main`. **S17 (PR #36) parked at merge gate** awaiting founder-named authorization; its bookkeeping (trend.csv, score md, this file's S17 row) is git-stashed on the working tree until merge. _Prior status line:_ **Last updated:** 2026-06-02 · **Current sprint:** 9 (S13 Document versioning + re-index — **✅ shipped, composite 90; Sprint 9 CLOSED via retro**) · **Last command:** `/8-eo-retro Sprint 9 (S13 document versioning)` — **Sprint 9 closed.** S13 shipped at composite **90 first-pass, zero round-trip** (PR #25 squash-merged `e02b114`, founder-authorized "merge PR #25") — **4th consecutive zero-round-trip ship** (90, 90, 94, 90 across Sprints 6–9), back to the infra/data baseline after S12's 94, **exactly as Sprint 8's retro predicted** ("S13 is more domain/data than UI — expect ~90 unless it ships real UI"). Flat all-9 hat profile; the two hats that dropped 10→9 vs S12 (Engineering, UX) trace to the single root that S13 ships **incremental UI on the existing `/documents` surface**, not net-new visible surface — the inverse confirmation of the story-selection thesis, not a regression. **The 90↔94 swing across S10–S13 is now a characterized story-mix signal** (mock-tested infra/seam/data → ~90; net-new visible UI → 94), four data points. **No hat capped** — L1–L9 all proactively satisfied at write-time (4th clean-hygiene ship). **Headline insight: the product guarantee was satisfied by construction** — `queries.citations` is a frozen `jsonb` snapshot with no FK to `documents`, so reading the data model before designing collapsed a feared history-migration into a safe atomic re-index. **No new lesson minted** (9 active L1–L9, 0 archived, 0 pruned — none <90d expired; S13 surfaced no recurring defect; the "read the data model first" habit prevented work rather than costing it → headline note, not a rule). SaaSfast: S13 fits M3, no outlier. **No production deploy run** (founder instruction) — S13 on `main` only; live Contabo + AWS me-central-1 cutovers stay founder-gated (prod `supabase db push` would include 0010–0014). Report → `docs/retros/2026-06-02-sprint9-s13.md`. **Next-sprint focus:** S14 (digest) + S16 (webinar demo) are UI-bearing → 94+ candidates; S15 (API access) integration-shaped → ~90; lead with a UI-bearing story for a 94 sprint. Next: **`/2-eo-dev-plan story-14`** to open Sprint 10. — **(prior `/7-eo-ship story-13` → merge)** Ship gate ✅ PASSED; re-scored shipped head `ee07687` = composite 90 (P9 A9 E9 Q9 U9); CI green (run 26833156553 — quality + integration) confirmed BEFORE merge (L3); founder authorized "merge PR #25" → `gh pr merge 25 --squash --delete-branch` → MERGED `e02b114`, branch deleted; `main` synced; S13 → ✅ shipped, trend row `shipped-pr25-e02b114`; **no production deploy**. — **(prior `/5-eo-score story-13`)** **S13 scored composite 90 (P9 A9 E9 Q9 U9, no hat <8) → Decision: Ship.** CI confirmed green on **PR #25** head `1e951ec` (run 26833156553 — **quality + integration** both success) BEFORE scoring (L3/retro-P1). **Every applicable lesson cap audited & satisfied:** L1 (route-seam every branch) ✓ · L2 (live serialized unique-tenant + cleanup) ✓ · L3 (CI green pre-score) ✓ · L4 (replace control reachable + PUT caller) ✓ · L5 (`<bdi>` on version digit + filename) ✓ · L6 (no secret prefixes, no new env) ✓ · L7 (live `npm run axe:walk` 0 violations @375px incl. net-new `/documents` walk) ✓ · L8 (AC-13.1…13.8 enumerated in `docs/stories/S13.md` pre-code) ✓ · L9 (≥44px replace control, AA) ✓. Hat detail: **Product 9** (full reachable replace loop, same `document_id` keeps citation deep-link valid, history-survives proven, fail-safe; −1 enhancement not net-new surface) · **Architecture 9** (atomic RPC swap = single all-or-nothing point, security-definer + tenant-guard, fail-safe ordering, composition of S2+S12 seams, zero new deps; read-then-increment safe single-writer) · **Engineering 9** (3 test layers 9 service + 9 route-seam + 4 live-DB = full suite 569/569, embed-mismatch guard, no `any`/secrets, `<bdi>`) · **QA 9** (capped at L7/L8 ceiling, both satisfied; every AC `@AC`-tagged, 13.8 cited L7 walk) · **UX 9** (RTL, `<bdi>`, ≥44px, disabled-while-active, live axe 0; −1 incremental UI not net-new surface). Score → `docs/qa-scores/2026-06-02-1905-S13-document-versioning.md`; trend.csv has the `score-ci-green-pr25-awaiting-merge` row. **No new lesson** (clean first-pass 90 — honest data-heavy ceiling, matches the planned ~90 prediction). `/4-eo-review` was CLEAN (0 🔴/0 🟡: no secrets/`any`, AC-13.1–13.7 @AC-tagged + 13.8 cited, elegance block in `1e951ec`). Next: **`/7-eo-ship story-13`** — re-score HEAD + build/test/audit + post ship verdict to PR #25 → **STOP at merge gate** (merge of PR #25 to `main` needs explicit founder authorization naming the PR; a bare "y" is insufficient — classifier-enforced; no production deploy). Standing (surface, do not work around): 🔴 **rotate the GitHub PAT** (#1 risk — pasted plaintext, still live/active for `sukainade-lab`); `.claude/settings.json` SessionStart hook still awaiting explicit approval; live Contabo + live AWS me-central-1 cutovers stay founder-gated (prod `supabase db push` would now include 0010+0011+0012+0013+0014). — **(prior `/3-eo-code story-13`)** **S13 implemented test-first; all 8 ACs green on `feat/s13-doc-versioning`.** Composition of proven seams (S2 ingest + S12 replace-in-place), not new infra. **`docs/stories/S13.md` AC-13.1…13.8 written first (L8).** **Core insight held: Q&A history is decoupled by construction** — `queries.citations` is a frozen `jsonb` snapshot with NO FK to `documents`, so replace = safe atomic re-index keeping the same `document_id`, not a history migration. Build: **migration `0014_document_versioning.sql`** (`documents.version int default 1` + `updated_at` + `replace_document_chunks` RPC = atomic delete-old+insert-new in one tx, `(r->>'embedding')::vector` + `nullif(page_number,'')::int` casts inside, **security-definer + tenant-guard** `tenant mismatch` raise, `revoke public`/`grant service_role` — validated against local PG before building on top; applied **local-only**) → **`replaceDocument`/`processReplace` in `ingest.ts`** (sync: upload-new `upsert:true` + remove-old-if-renamed + status→parsing; background: parse→embed→**atomic RPC swap**→read-then-bump version+updated_at+page_count→ready; **fail-safe AC-13.5**: any throw before the RPC leaves prior file+chunks+version fully intact, row→failed, prior revision still answers) → **`PUT /api/documents/[id]`** next to existing DELETE (tenant from session, reuses `uploadMetaSchema`/`resolveMime`/`MAX_UPLOAD_BYTES`, `processReplace` off the response path via `after()`) → per-row **"استبدال"** file-input control + **`نسخة N`** version badge (only when version>1) on `(app)/documents/page.tsx` (**L5** `<bdi>` on the digit, **L9** ≥44px tap target, disabled while ACTIVE so a 2nd replace can't race, reuses the parsing→indexing→ready active-poll) + GET select now includes `version`. **Tests: 22 new green** — 9 service (`story-13-replace.test.ts`: upload+remove+status, RPC rows + version 2 + page_count + updated_at, empty chunk set, fail-safe parse/embed/swap → failed + no version + RPC-not-called) · 9 route-seam (`story-13-replace-routes.test.ts`, **L1** every branch 401/401/404/403/400/400/413/200/200 + DOCX octet-stream fallback + after-schedules-processReplace + 1 PII-free track) · 4 live-DB (`story-13-versioning-db.test.ts`, **L2** `describe.skipIf().sequential` unique-tenant + cleanup: **AC-13.4** query+citation snapshot byte-for-byte unchanged via `toEqual`, **AC-13.3** RPC swap old-gone/new-present, **AC-13.2** version bump+updated_at+same-id, **AC-13.6** cross-tenant RPC → `tenant mismatch` + victim chunks untouched) · **full suite 569/569 / 66 files** (`--no-file-parallelism`). Gates: **tsc** ✓ · **eslint** ✓ · `next build` ✓ (`/documents` + `/api/documents/[id]` ƒ) · `npm audit --omit=dev` 2 moderate pre-existing next/postcss (below high gate, carried exception). **L7: `/documents` added to `axe-walk.mjs` DEFAULT_ROUTES** (first time that surface is walked) → live `npm run axe:walk` @375px = **0 violations** across /dashboard (19) /documents (19) /admin (27) /founder (22) /settings (21). Elegance pause: `processReplace`/`processDocument` share a parse→embed shape but diverge meaningfully (atomic RPC swap + version bump vs plain insert) — kept separate (honest, not duplication). **Status stays 🔨 coding** until `/4-eo-review`. Next: **`/4-eo-review story-13`** (cite L7 walk) → confirm CI green via `gh run` (L3) BEFORE `/5-eo-score story-13` → `/7-eo-ship story-13` **STOP at merge gate** (founder-named PR authorization required). Standing (surface, do not work around): 🔴 **rotate the GitHub PAT** (#1 risk — pasted plaintext, still live/active for `sukainade-lab`); `.claude/settings.json` SessionStart hook still awaiting explicit approval; live Contabo + live AWS me-central-1 cutovers stay founder-gated (prod `supabase db push` would now include 0010+0011+0012+0013+0014). — **(prior `/2-eo-dev-plan story-13`)** **Sprint 9 opened.** PR #24 (S12 shipped-flip + Sprint 8 retro + lessons prune-header) CI green (run 26831336916) → squash-merged `b27d99d`, branch deleted; `main` fast-forwarded `4680092`→`b27d99d`. **S13 = Document versioning (replace a document + re-index without losing Q&A history)**, BRD line 169, loop domain, Phase-2 one-liner → **L8: AC-13.1…13.8 enumerated** (plan `~/.claude/plans/s13-document-versioning.md`; spec `docs/stories/S13.md` written first in `/3-eo-code`). **Core insight: the schema already decouples Q&A history from documents** — `queries.citations` is a `jsonb` frozen snapshot (`document_id`/`document_name`/`page_number`/`section`/`similarity` from `buildCitations`) with **no FK to `documents`**, so a replace/delete never touches `queries`; the story is a *safe replace + atomic re-index keeping the same `document_id`*, plus a test proving history survives — **not** a history migration. Approach (composition of S2 ingest + S12 replace-in-place seams): migration `0014_document_versioning.sql` (`documents.version int default 1` + `updated_at` + `replace_document_chunks` RPC = atomic delete-old + insert-new chunks in one tx, embeddings as `toVectorLiteral` text cast `::vector(1536)` inside, tenant-guarded) → `replaceDocument`/`processReplace` in `ingest.ts` (sync upload-new + remove-old-if-renamed + status parsing; background parse→embed→atomic RPC swap→bump version/updated_at/page_count→ready; **failure keeps prior file+chunks+version**, status failed — no partial swap) → `PUT /api/documents/[id]` next to existing DELETE (L1 branches 401/404/403/400/413/200, tenant from session, reuses `uploadMetaSchema`/`resolveMime`/`MAX_UPLOAD_BYTES`) → per-row "استبدال" file-input control + `نسخة N` version badge on `(app)/documents/page.tsx` (L5 `<bdi>`, L9 AA, ≥44px, reuses the parsing→indexing→ready active-poll) → live L2 test (unique-tenant, serialized): create doc → insert query+citation snapshot → replace → assert `queries`/`citations` untouched + new chunks present + old gone + version bumped, + storage isolation unchanged. **L7: add `/documents` to `axe-walk.mjs` DEFAULT_ROUTES** (first time that surface is walked) → live `npm run axe:walk` @375px = 0 serious/critical. Honest target ~90 (data/seam-heavy, small UI), shot at 92+ if `/documents` walk is first-pass clean. Tasks #115–#122 queued. Lessons in play: L1·L2·L3·L4·L5·L6·L7·L8·L9. **Next: founder "go" → `/3-eo-code`** (write `docs/stories/S13.md` first per L8, then TDD red→green→blue; branch `feat/s13-doc-versioning` off `main` `b27d99d`; Status stays 📝 planned → 🔨 coding when code starts). Standing (surface, do not work around): 🔴 **rotate the GitHub PAT** (#1 risk — pasted plaintext, still live/active for `sukainade-lab`); `.claude/settings.json` SessionStart hook still awaiting explicit approval; live Contabo + live AWS me-central-1 cutovers stay founder-gated. — **(prior `/8-eo-retro Sprint 8`)** **Sprint 8 closed.** S12 shipped at composite **94 first-pass, zero round-trip** (PR #23 squash-merged `4680092`, founder-authorized "merge PR #23") — **breaks the two-sprint 90 plateau (S10, S11)**, +4 vs Sprint 7. Hat profile P9 A9 **E10** Q9 **U10**: the two 10s came from a clean first-pass **live** L7 `npm run axe:walk` (0 violations @375px on the net-new `/settings` Branding surface) + layered tests (route + service + validation + live RLS = 547/547). **Headline: the Sprint-7 story-selection thesis paid off exactly as predicted** — picking a UX-heavy story with real visible surface + a clean first-pass walk converted directly into 94; the prior plateau was a story-mix artifact (mock-tested infra/seam stories cap at 90 by necessity), not a process failure. **No hat capped this sprint** — L1–L9 all proactively satisfied at write-time. `/4-eo-review` earned its keep: caught that `logo.ts` had zero direct coverage (route tests mock it; live L2 hits raw RLS) + AC-12.4/12.6 untagged → fixed inline before scoring (governed by L1's "test the seam" sibling principle; no new rule needed). **No new lesson minted** (9 active L1–L9, 0 archived, 0 pruned — none <90d expired; S12 surfaced no recurring defect). SaaSfast: S12 fits M3, no outlier. Post-merge trunk gates on `main` `4680092`: tsc ✓ · **547/547 / 63 files** ✓ · `next build` ✓ · audit(--omit=dev,high) 0 high/critical (2 moderate documented). Report → `docs/retros/2026-06-02-sprint8-s12.md`. **Next-sprint focus:** S13–S16 backlog — the 94 pattern is reproducible (net-new visible surface + clean first-pass L7); S13 versioning is data-heavy (expect ~90 unless it ships real UI), S14 digest + S16 webinar demo are UI-bearing (94+ candidates). Adopt L8 for the next Phase-2 story (enumerate AC-13.x into `docs/stories/S13.md` before coding). Next: **`/2-eo-dev-plan story-13`** to open Sprint 9. Standing (surface, do not work around): 🔴 **rotate the GitHub PAT** (#1 risk — pasted plaintext, still live/active for `sukainade-lab`); `.claude/settings.json` SessionStart hook still awaiting explicit approval; live Contabo + live AWS me-central-1 cutovers stay founder-gated. — **(prior `/7-eo-ship story-12`)** **Ship gate ✅ PASSED → merged.** Re-scored HEAD `b0e2b9e` = composite 94 (docs-only since `/5-eo-score`, score stands); final gates green; ship verdict posted to PR #23; founder authorized "merge PR #23" → squash-merged `4680092`, branch deleted, S12 → ✅ shipped, trend row `shipped-pr23-4680092`. — **(prior `/5-eo-score story-12`)** **S12 scored composite 94 (P9 A9 E10 Q9 U10, no hat <8) → Decision: Ship.** First story to break the two-sprint 90 plateau. CI confirmed green on **PR #23** (run 26830185748 — quality + integration) BEFORE scoring (L3/retro-P1). **Every applicable lesson cap audited & satisfied proactively — none bit (cleanest posture in project history):** L1 (11 route-seam tests per branch) ✓ · L2 (live storage-isolation `describe.skipIf().sequential` + cleanup) ✓ · L3 (CI green pre-score) ✓ · L4 (BrandingSettings → /api/branding, header reads logo_path) ✓ · L5 (`<bdi>` on lab name in layout + settings + preview) ✓ · L6 (no secret prefixes, no new env vars) ✓ · L7 (**live** `npm run axe:walk` 0 violations @375px across /dashboard /admin /founder /settings — net-new surface actually walked) ✓ · L8 (AC-12.1…12.8 enumerated in docs/stories/S12.md pre-code) ✓ · L9 (primary CTA #B45309 white 5.02:1 AA-pass) ✓. Hat detail: **Product 9** (full reachable admin-only loop, graceful fallback chain, Arabic copy; −1 button-only remove, MVP-acceptable) · **Architecture 9** (tenant-namespaced storage key + RLS isolation mirroring documents, public-read/writes-gated, replace-in-place with rollback, pure resolver isolates fallback) · **Engineering 10** (layered tests: 11 route-seam + 13 service + validation + live RLS = 547/547, discriminated-union gate, no `any`/secrets, `<bdi>`) · **QA 9** (every AC `@AC`-tagged after review fix; capped at L7/L8 ceiling 9, both satisfied) · **UX 10** (RTL, ≥44px, 375px, AA CTA, Arabic alt + fixed dims, live axe 0). Score → `docs/qa-scores/2026-06-02-1838-S12-branding.md`; trend.csv has the `score-ci-green-pr23-awaiting-merge` row. **No new lesson** (clean first-pass 94). Next: **`/7-eo-ship story-12`** — re-score HEAD + build/test/audit + post ship verdict to PR #23 → **STOP at merge gate** (merge of PR #23 to `main` needs explicit founder authorization naming the PR; a bare "y" is insufficient — classifier-enforced; no production deploy). Standing (surface, do not work around): 🔴 **rotate the GitHub PAT** (#1 risk — pasted plaintext, still live/active for `sukainade-lab`); `.claude/settings.json` SessionStart hook still awaiting explicit approval; live Contabo + live AWS me-central-1 cutovers stay founder-gated. — **(prior `/4-eo-review story-12`)** **S12 review CLEAN → Status flipped 🔨 coding → 🧪 scoring on `feat/s12-branding` (`ef8ce5a`).** Diff vs `main` = 13 files. **Verification before done caught one real gap:** the route-seam tests mock `@/lib/branding/logo` out entirely and the live L2 test hits raw Storage RLS, so `logo.ts` (`uploadLogo` replace-in-place + rollback, `removeLogo` null-then-delete, `publicLogoUrl`) had **zero direct coverage**, and AC-12.4 (header fallback chain) + AC-12.6 (public URL/alt) carried **no `@AC` tag** (brd-traceability gap) — even though `docs/stories/S12.md` explicitly promised a "Service (mocked admin client)" test layer. Fix (commit `ef8ce5a`): added `tests/story-12-branding-logo.test.ts` (13 tests, mocked admin client → runs anywhere) covering uploadLogo (upsert key, remove-prior-only-on-different-key, DB-failure rollback, unsupported-mime throw — @AC-12.3) · removeLogo (null-then-delete — @AC-12.5) · publicLogoUrl (null/empty → null, builds stable URL — @AC-12.6) · logoStoragePath (@AC-12.2/12.3) · `resolveSidebarBrand` fallback chain + Arabic alt + whitespace-name→wordmark (@AC-12.4/12.6); and extracted the new pure `resolveSidebarBrand` helper into `logo.ts` so `(app)/layout.tsx` renders it as a thin view (fallback logo+name→name→wordmark + alt format now in one tested place). **BRD traceability: 7/8 ACs `@AC`-tagged** (12.1·12.2·12.3·12.4·12.5·12.6·12.8); **AC-12.7 = cited L7 axe-walk evidence** (no code test — same precedent as AC-11.7 doc-only). **Source scan clean:** no secret-prefix literals, no `any` across `src/lib/branding`, `src/app/api/branding`, the two pages, the component. **Elegance Pause** block in `ef8ce5a`. SaaSfast M3, no cross-mode leakage. **Gates green:** tsc ✓ · eslint ✓ · **full suite 547/547 / 63 files** ✓ · `next build` ✓ · `npm audit --omit=dev --audit-level=high` = 0 high/critical (2 moderate next/postcss documented). **L7 re-walk on HEAD `ef8ce5a`** (layout.tsx changed): `npm run axe:walk` → **0 violations** across `/dashboard` (20 passes) · `/admin` (27) · `/founder` (22) · `/settings` (22) @375px (wcag2a+2aa) — the seeded owner+platform-admin renders the Branding section so it is actually walked. **Punch list: 0 🔴 must-fix, 0 🟡 blocking.** Next: **push `feat/s12-branding` → confirm both CI jobs green via `gh run` (L3 / retro-P1) BEFORE `/5-eo-score story-12`** (target composite 94+ — first net-new-UI since the harness, clean first-pass review) → `/7-eo-ship` STOP at merge gate (founder-named PR authorization required; no production deploy). Standing (surface, do not work around): 🔴 **rotate the GitHub PAT** (#1 risk — pasted plaintext, still live/active for `sukainade-lab`); `.claude/settings.json` SessionStart hook still awaiting explicit approval; live Contabo + live AWS me-central-1 cutovers stay founder-gated. — **(prior `/3-eo-code story-12` `b333cc5`)** **S12 implemented test-first; all 8 ACs green on `feat/s12-branding`.** Composition of proven seams, not new infra: **migration `0013_tenant_branding.sql`** (`tenants.logo_path` + public `branding` Storage bucket + `branding_storage_tenant_isolation` RLS keyed on first path segment = `current_tenant_id()`, mirroring 0003 documents bucket — applied **local-only**) → pure **`validation/branding.ts`** (PNG/JPEG/SVG/WebP, ≤512KB cap with exact-boundary tests, octet-stream→extension MIME fallback) → **`lib/branding/logo.ts`** (`uploadLogo` replace-in-place + set `logo_path` + rollback on DB failure, `removeLogo` null-then-delete, `publicLogoUrl`) → **admin-only** `POST`+`DELETE /api/branding` (discriminated-union `resolveAdmin` gate: role ∈ owner/admin else 403, tenant from session never client; L1 branch tests 201/400/413/403/401, 200/403/401) → **`BrandingSettings`** section in `/settings` (admin-only render mirroring the 403 gate per L4, mirrors `<MfaSettings>`, RTL ≥44px, `<bdi>` lab name, cache-busted preview) → tenant logo+name in **`(app)/layout.tsx`** sidebar (now async) replacing static "LabBrain" wordmark (fallback logo→name→wordmark; lab name `<bdi>`-wrapped L5; Arabic `alt` + fixed dims). **Tests: 25 new green** — 11 route-seam (L1, every branch) · 10 validation unit · 4 live storage-isolation (`story-12-branding-storage-db.test.ts`, L2 serialized unique-tenant: proves cross-tenant write/list/delete denied by RLS, authoritative delete-survives assertion) · **full suite 534/534 / 62 files**. Gates: **tsc** ✓ (fixed via discriminated-union gate + `publicLogoUrl` null-guard) · **eslint** ✓ · `next build` ✓ · `npm audit --omit=dev --audit-level=high` = 0 high/critical (2 moderate next-bundled postcss documented). **L7 walk cited:** axe-walk extended to `/settings` (`DEFAULT_ROUTES`) → `npm run axe:walk` **0 violations** across `/dashboard`+`/admin`+`/founder`+`/settings` @375px (wcag2a+2aa). **Zero new npm deps · zero new env vars** (bucket public-read, reuses service-role key). Elegance: discriminated-union `resolveAdmin`, proven patterns reused (invitations gate · documents validation · documents-bucket storage flow · MfaSettings UI), SVG served only via `<img src>` (no inline → no stored-XSS). Lessons honored: L1·L2·L4·L5·L6·L7·L8·L9. Commit `b333cc5` carries the Elegance Pause block. **Status stays 🔨 coding** until `/4-eo-review`. Next: **`/4-eo-review story-12`** (cite the L7 walk evidence) → confirm CI green via `gh run` (L3) BEFORE `/5-eo-score story-12`. Standing (surface, do not work around): 🔴 **rotate the GitHub PAT** (#1 risk — pasted plaintext, still live/active for `sukainade-lab`); `.claude/settings.json` SessionStart hook still awaiting explicit approval; live Contabo + live AWS me-central-1 cutovers stay founder-gated. — **(prior `/2-eo-dev-plan story-12`)** AC-12.1…12.8 enumerated in `docs/stories/S12.md` **before coding** (L8); branch `feat/s12-branding` cut off `main`; S12 row → 📝 planned. Approach (composition of proven seams, not new infra): **migration 0013** (`tenants.logo_path` + public `branding` Storage bucket + `branding_storage_tenant_isolation` RLS keyed on first path segment = `current_tenant_id()`, mirroring 0003's documents bucket) → pure **`validation/branding.ts`** (PNG/JPEG/SVG/WebP, ≤512KB, mirrors `documents.ts`) → **`lib/branding/logo.ts`** (`uploadLogo` replace-in-place + set `logo_path`, `removeLogo`, `publicLogoUrl`) → **admin-only** `POST`+`DELETE /api/branding` (role ∈ owner/admin else 403; tenant from session; L1 route tests per branch 201/400/413/403/401, 200/403/401) → **`BrandingSettings`** section in `/settings` (mirrors `<MfaSettings>`, RTL ≥44px) → tenant logo+name in **`(app)/layout.tsx`** sidebar replacing the static "LabBrain" wordmark (fallback logo→name→wordmark; lab name `<bdi>`-wrapped L5). Lessons in play: **L1** (route branch tests), **L2** (serialized unique-tenant live storage-isolation test), **L4** (control reachable + header caller), **L5** (`<bdi>` lab name), **L7** (live `npm run axe:walk` @375px incl. `/settings`), **L8** (spec first ✓), **L9** (fix own-surface AA inline). First net-new-UI since the axe-walk harness → shot at composite 94+ (break the 2-sprint 90 plateau) if the L7 walk is first-pass clean. Next: **`/3-eo-code`** (TDD red→green→blue; Status stays 🔨 coding until `/4-eo-review`). Standing (surface, do not work around): 🔴 **rotate the GitHub PAT** (#1 risk — pasted plaintext, still live/active for `sukainade-lab`); `.claude/settings.json` SessionStart hook still awaiting explicit approval; live Contabo + live AWS me-central-1 cutovers stay founder-gated. — **(prior)** 2026-06-02 · sprint 7 (S11 Air-gap mode — **CLOSED, shipped + retro**) · `/8-eo-retro Sprint 7 (S11 air-gap mode)` — **Sprint 7 closed.** S11 shipped at composite **90 first-pass, zero round-trip** (PR #21 merged `2678e4d`, founder-authorized "merge PR #21") — **2nd consecutive zero-round-trip 90** (matches S10). Flat all-9 hat profile, **no hat capped this sprint** — L3/L4/L5/L6/L7/L8/L9 all proactively satisfied (lessons now applied at write-time, not surfaced at the score gate); L1/L2 N/A. **Headline: the Sprint-6 focus deliverable paid off on first use** — the seeded-session axe-walk harness (PR #20) caught a real serious AA `<dt>` contrast failure (slate-500 3.05:1) on S11's new mode badge that 509 tests + static review both missed → fixed inline before scoring → UX never capped. **No new lesson minted** (9 active L1–L9, 0 archived, 0 pruned — S11 surfaced no recurring defect; an "infra caps at 9 on mocked live integration" lesson would be bloat — it's a known accepted structural limit). 90 is the honest ceiling for a mock-tested infra story (same shape as S10/S9). SaaSfast: S11 fits M3, no outlier. Report → `docs/retros/2026-06-02-sprint7-s11.md`. **Next-sprint focus:** pick UI stories that can earn 94+ (S12 multi-tenant branding is a UI+Storage story — first net-new-UI since the harness, L5/L7/L9 all apply). Next: **`/2-eo-dev-plan story-12`** to open Sprint 8. Standing (surface, do not work around): 🔴 **rotate the GitHub PAT** (#1 risk — pasted plaintext, still live/active for `sukainade-lab`); `.claude/settings.json` SessionStart hook still awaiting explicit approval; live Contabo cutover (S11 self-hosted Ollama+LlamaParse) + live AWS me-central-1 cutover (S10) stay founder-gated. — **(prior)** `/7-eo-ship story-11` — **Ship gate ✅ PASSED → merged.** Re-scored HEAD `cbd5dbc` = **composite 90** (P9 A9 E9 Q9 U9, no hat <8; only commit since `/5-eo-score` is docs-only, score stands). Final gates on `cbd5dbc`: **tsc** ✓ · **eslint/test 509/509 / 59 files** ✓ · `next build` ✓ · `npm audit --omit=dev --audit-level=high` = 0 high/critical (2 moderate postcss/next documented). **CI green on PR #21 head `cbd5dbc`** (run 26826317157 — quality + integration) confirmed (L3/retro-P1). Ship-gate verdict posted as PR #21 comment. **No production deploy run — live Contabo cutover (self-hosted Ollama + LlamaParse) + live AWS me-central-1 cutover stay founder-gated (VPS/host + on-prem secrets only the founder holds).** Status stays 🧪 scoring until the founder authorizes the merge naming PR #21 (a bare "y" is insufficient — classifier-enforced). On merge authorization → squash-merge PR #21 → flip S11 to ✅ shipped + add `shipped-pr21-<sha>` trend row → `/8-eo-retro` (Sprint 7 close). Standing (surface, do not work around): 🔴 **rotate the GitHub PAT** (#1 risk — pasted plaintext, still live/active for `sukainade-lab`); `.claude/settings.json` SessionStart hook still awaiting explicit approval; live AWS me-central-1 + live Contabo cutovers stay founder-gated. — **(prior)** `/5-eo-score story-11` — **S11 scored composite 90 (P9 A9 E9 Q9 U9, no hat <8) → Decision: Ship.** CI confirmed green on **PR #21** (run 26825816130 — quality + integration) BEFORE scoring (L3/retro-P1). **Every applicable lesson cap audited & satisfied proactively, none bit:** L3 (CI green) ✓ · L4 (badge reachable on `/admin`+`/founder`, seams called by existing flows) ✓ · L5 (`<bdi>` on all dynamic model/host strings) ✓ · L6 (`.env.example`/`env-contract` names-only, no secret prefix) ✓ · L7 (live `npm run axe:walk` 0 violations @375px) ✓ · L8 (ACs enumerated in `docs/stories/S11.md` before coding) ✓ · L9 (badge `<dt>` AA contrast fixed inline before scoring) ✓. L1/L2 N/A (no new API route, no live-DB suite). Hat detail: **Product 9** (the named compliance unlock for classified labs, all 8 ACs covered; live no-cloud run vs real Ollama/self-hosted LlamaParse honestly deferred/gated −1) · **Architecture 9** (single pure resolver routing all 3 seams fail-closed, **zero new deps**, no migration, same seam discipline as S6/S9/S10) · **Engineering 9** (throws on unknown mode + dim≠1536, display-safe descriptor never crashes the page, names-only env) · **QA 9** (33 tests, every AC `@AC-11.N`-tagged, pure resolver + mocked-seam coverage proving local-host routing & fail-closed throws; L7 walk cited) · **UX 9** (RTL badge, 3 colour-coded states, `<bdi>` throughout, AA contrast clean). Score → `docs/qa-scores/2026-06-02-1724.md`; trend.csv has the `score-ci-green-pr21-awaiting-merge` row. **No new lesson** (clean first-pass 90 — lessons now applied at write-time). Next: **`/7-eo-ship story-11`** — re-score HEAD + build/test/audit + finalize PR #21 → **STOP at merge gate** (merge of PR #21 to `main` needs explicit founder authorization naming the PR; a bare "y" is insufficient — classifier-enforced; no production deploy — Contabo cutover founder-gated). Standing (surface, do not work around): 🔴 **rotate the GitHub PAT** (#1 risk — pasted plaintext, still live/active for `sukainade-lab`); `.claude/settings.json` SessionStart hook still awaiting explicit approval; live AWS me-central-1 cutover runbook + live Contabo cutover stay founder-gated. — **(prior)** `/4-eo-review story-11` — **S11 review CLEAN → Status flipped 🔨 coding → 🧪 scoring.** Diff vs `main` = **18 files** (15 S11 + tracker + tailwind/types env-fix). **Source scan clean:** no secret-prefix literals, no `any` types across `src/lib/ai/*` + `components/ops` + the two pages. **BRD traceability:** AC-11.1…11.6 + 11.8 carry `@AC-11.N` tags (review fixup commit `baa48a6` — they were bare `(AC-11.N)`, brd-traceability greps `@AC-`); AC-11.7 is a doc-only deliverable (`.env.example` + `docs/env-contract.md`). **Elegance Pause** block confirmed in `b4799e4`. **L7 walk performed + cited:** `npm run axe:walk` → **0 violations** on `/dashboard`+`/admin`+`/founder` @375px (wcag2a+2aa) — first pass caught a real serious AA `<dt>` contrast failure (slate-500 3.05:1) → fixed to slate-400 (≈5.66:1) → clean. SaaSfast M3, no cross-mode leakage. **Punch list: 0 🔴 must-fix, 0 🟡 blocking.** Branch HEAD `baa48a6`. Next: **push `feat/s11-airgap-mode` → confirm CI green via `gh run` (L3/retro-P1) BEFORE `/5-eo-score story-11`**. — **(prior)** `/3-eo-code story-11` — **S11 air-gap mode implemented test-first; all 8 ACs green on `feat/s11-airgap-mode`.** Pure resolver `src/lib/ai/inference-mode.ts` is the single source of truth routing all 3 seams (parse/embed/answer) cloud→local, **fail-closed** (`InferenceConfigError` thrown on any missing local var / dim≠1536 / typo'd mode — never a silent cloud fallback; AC-11.1/11.5/11.6). Seams swapped to the resolver: `lib/ai/embeddings.ts` + `lib/ai/answer.ts` (Ollama OpenAI-compatible base via `baseURL`) + `lib/parsing/llamaparse.ts` (self-hosted base) — **zero new npm deps**, **no DB migration** (AC-11.2/11.3/11.4). Operator-visible indicator (AC-11.8): display-safe `describeInferenceMode()` (never throws) → presentational `components/ops/inference-mode-badge.tsx` (RTL, mode-colour-coded, every dynamic mixed-script value `<bdi>`-wrapped per L5) rendered on **both** `/admin` and `/founder`. Env contract (AC-11.7, L6 names-only): `.env.example` + `docs/env-contract.md` carry the 6 air-gap vars + the fail-closed deploy doc. **L7 walk PASSED with the new seeded-session harness:** `npm run axe:walk` → **0 violations** on `/dashboard`+`/admin`+`/founder` @375px (wcag2a+2aa) — first pass caught a real **serious AA contrast** failure on the badge's `<dt>` labels (slate-500 #64748b on #1B2A3D = 3.05:1) → fixed to slate-400 (#94a3b8 ≈5.66:1) → re-walk clean. **Env fix (Windows webpack-dev):** `tailwind.config.ts` used CJS `require("tailwindcss-rtl")` in an ESM `.ts` config → "require is not defined" panic when the PostCSS/Tailwind loader loaded it under `next dev --webpack`; converted to ESM `import tailwindcssRtl from "tailwindcss-rtl"` + added `types/tailwindcss-rtl.d.ts` (no upstream types). Gates: **tsc** ✓ · **eslint** ✓ · **full suite 509/509 / 59 files** ✓ · `next build` ✓ · `npm audit --omit=dev --audit-level=high` = 0 high/critical (2 moderate postcss/next documented). Tests: `tests/story-11-inference-mode.test.ts` (26 incl. AC-11.8 descriptor) + `tests/story-11-airgap-seams.test.ts`. Lessons honored: L5·L6·L7·L8. **Status stays 🔨 coding** until `/4-eo-review`. Next: **`/4-eo-review story-11`** (cite the L7 walk evidence) → confirm CI green via `gh run` (L3/retro-P1) BEFORE `/5-eo-score story-11`. Sprint 6's focus deliverable **shipped first**: the seeded-session **axe-walk harness** (`scripts/axe-walk.mjs` + `npm run axe:walk` + `docs/qa/axe-walk-harness.md`) — **PR #20 squash-merged to `main` `de4e10a`** (founder-authorized "when CI green, merge PR #20"; both CI jobs green run 26823306999). Now any gated-UI story clears L7 with a real 375px + axe walk on `/admin`+`/founder` (baseline: 0 violations on all three gated routes). **S11 = Air-gap mode** (self-hosted LlamaParse + Ollama for classified-document labs; loops domain+compliance). Phase-2 one-liner → **L8 honored: AC-11.1…11.8 enumerated in `docs/stories/S11.md` before coding.** Shape: a single pure `INFERENCE_MODE` resolver (`src/lib/ai/inference-mode.ts`) routes all 3 seams (parse/embed/answer) cloud→local, **fail-closed** (never silent cloud fallback — the core compliance guarantee), embedding-dim enforced =1536. **Zero new npm deps** (Ollama is OpenAI-compatible → reuse `openai` SDK w/ baseURL; LlamaParse base already env-configurable), **no DB migration**. Branch `feat/s11-airgap-mode` cut off `main` `de4e10a`. Next: **`/3-eo-code story-11`** — TDD red→green from `docs/stories/S11.md` (pure resolver first, then seam swaps, then route/indicator + the new axe:walk for AC-11.8/L7). Standing (surface, do not work around): 🔴 **rotate the GitHub PAT** (#1 risk — pasted plaintext, still live/active for `sukainade-lab`); `.claude/settings.json` SessionStart hook still awaiting explicit approval; live AWS me-central-1 cutover runbook (KSA project) + live Contabo cutover (migrations 0010+0011+0012) stay founder-gated.

**Prior:** 2026-06-02 · **Current sprint:** 6 (S10 KSA migration — **CLOSED**) · **Last command:** `/8-eo-retro Sprint 6 (S10 KSA migration)` — **Sprint 6 closed.** S10 shipped at composite **90 first-pass, zero round-trip** (PR #18 merged `9ddfa28`, founder-authorized; #17 merged first). Hat profile flat all-9 vs S9. **L9 paid off 1 sprint after minting** (both new CTAs AA-verified before scoring — no contrast bridge, unlike S9). **QA = focus hat, capped at 9 by L7** — the gated founder migration tab deferred the live axe walk to static; root cause is **friction on auth/role-gated surfaces**, not a missing rule. **No new lesson minted** (9 active, 0 archived, 0 pruned — L7 already encodes it; a duplicate = lesson-bloat). **Next-sprint focus = build a seeded-session axe-walk harness** (signs in as seeded owner/platform-admin, runs axe @375px against `/admin` + `/founder`) so every future gated-UI story clears L7 with a real walk — the single highest-leverage fix (would have lifted S10 QA to 10). Report → `docs/retros/2026-06-02-sprint6-s10.md`. Next: `/2-eo-dev-plan story-11` (Air-gap mode) to open Sprint 7, or build the axe-walk harness first. Standing (surface, do not work around): 🔴 **rotate the GitHub PAT** (#1 risk — pasted plaintext, still live/active for `sukainade-lab`); `.claude/settings.json` SessionStart hook still awaiting explicit approval; live AWS me-central-1 cutover runbook (KSA project) + live Contabo cutover (migrations 0010+0011+0012) stay founder-gated (VPS + KSA/prod secrets only the founder holds).

<!-- prior sprint-6 ship footer retained below for history -->
**Prior:** 2026-06-02 · **Current sprint:** 6 (S10 KSA migration) · **Last command:** `/7-eo-ship story-10` — **Ship gate ✅ PASSED → ⏸️ STOPPED at merge gate (PR #18 awaiting founder-named merge authorization).** Self-score on HEAD `25ad3b8` = **composite 90** (P9 A9 E9 Q9 U9, no hat <8; source byte-identical to `/5-eo-score`). Final gates on `25ad3b8`: **tsc** ✓ · **eslint** ✓ · **tests 476/476** (one S6 live-DB test — `@AC-6.4` Tap/Stripe id collision — hit a transient Supabase upstream blip `"invalid response from upstream server"` in the local full run → **re-ran S6 suite alone → 3/3 pass**; CI on PR #18 was green for it too — retro-P1 transient, not an S10 regression) · `next build` ✓ (`/api/founder/tenants/[id]/migrate` + `/migrate/cutover` ƒ) · `npm audit --omit=dev --audit-level=high` = 0 high/critical (2 moderate postcss/next documented). CI green on PR #18 head (run 26820835248 — quality + live-DB integration) confirmed. Ship-gate verdict posted as PR #18 comment. **No production deploy run — live Contabo cutover (migrations 0010+0011+0012) + live AWS me-central-1 cutover runbook stay founder-gated (VPS + prod/KSA secrets only the founder holds).** Status stays 🧪 scoring until the founder authorizes the merge naming PR #18 (a bare "y" is insufficient — classifier-enforced). On merge authorization → squash-merge PR #18 → flip S10 to ✅ shipped + add `shipped-pr18-<sha>` trend row → `/8-eo-retro` (Sprint 6 close). **Merge order note:** bookkeeping **PR #17** is still open and its commit `9889972` rides in PR #18's diff; cleanest is merge #17 first, then #18. Standing (surface, do not work around): **rotate the GitHub PAT** (🔴 #1 risk — pasted plaintext earlier, still live/active for `sukainade-lab`); `.claude/settings.json` SessionStart hook still awaiting explicit approval; **PR #17** awaiting founder-named merge authorization.

<!-- prior sprint-6 /5-eo-score footer retained below for history -->
**Prior:** 2026-06-02 · **Current sprint:** 6 (S10 KSA migration) · **Last command:** `/5-eo-score story-10` — **S10 scored composite 90 (P9 A9 E9 Q9 U9, no hat <8) → Decision: Ship.** CI green on **PR #18** head (run 26820835248 — quality ✓ + live-DB integration ✓) confirmed BEFORE scoring (L3 / retro-P1). All caps audited: L1/L2 (route-seam + serialized live cross-tenant) satisfied · L3 (CI green) satisfied · L4 (CTA→handler reachable) satisfied · L5 (`<bdi>`) satisfied · L6 (no secret prefix) satisfied · L8 (ACs enumerated in `docs/stories/S10.md`) satisfied · L9 (both new CTAs AA-pass) satisfied · **L7 applied → QA capped at 9** (S10 ships the founder migration tab but verification was cited *static* — computed AA contrast + `min-h-[44px]` audit + `<bdi>` audit + reuse of axe-cleared S8 chrome — not a live axe-core walk on the new surface). Hat detail: Product 9 (complete PDPL export→verify→cutover, founder-internal, live KSA target deferred) · Architecture 9 (clean `MigrationTarget` seam, deterministic content-hash parity, no-double-cutover state machine, lazy KSA client; live me-central-1 path documented-not-exercised −1) · Engineering 9 (typed errors, no `any`, no secret prefix, named RLS, idempotent, 476/476) · QA 9 (46/46, L1 route branches, L2 P0 cross-tenant + RLS, L8; L7 cap) · UX 9 (`<bdi>` everywhere, migrate #B45309 5.02:1 / cutover #4338CA ≈7.91:1, ≥44px). Score → `docs/qa-scores/2026-06-02-1555-S10-ksa-migration.md`; trend.csv has the `ship-ci-green-pr18-awaiting-merge` row. Next: **`/7-eo-ship story-10`** — re-score HEAD + build/test/audit + finalize PR #18 → **STOP at merge gate** (merge needs explicit founder authorization naming the PR; a bare "y" is insufficient). Standing (surface, do not work around): **rotate the GitHub PAT** (🔴 #1 risk — pasted plaintext earlier, still live/active for `sukainade-lab`); `.claude/settings.json` SessionStart hook still awaiting explicit approval; **bookkeeping PR #17** open, awaiting founder-named merge authorization (PR #18 carries its commit `9889972` until #17 merges); live Contabo cutover (founder-gated: VPS + prod secrets + `supabase db push` incl. 0010+0011+0012); live AWS me-central-1 cutover runbook (founder-gated KSA project).

<!-- prior sprint-6 /4-eo-review footer retained below for history -->
**Prior:** 2026-06-02 · **Current sprint:** 6 (S10 KSA migration) · **Last command:** `/4-eo-review story-10` — **S10 review CLEAN → Status flipped 🔨 coding → 🧪 scoring.** Full review on `feat/s10-ksa-migration` (commit `04a023e`). **Diff vs main = 25 files** (23 S10 + 2 a11y bookkeeping carried from the unmerged PR #17 base — cosmetic, clears on #17 merge; not a blocker). **Source scan clean:** no secret-prefix literals, no `any` types across `src/lib/migration` + `src/app/api/founder/tenants`; RLS user-scoped on `tenant_migrations`; service-key server-only (no `NEXT_PUBLIC_`); error handling present (typed `MigrationError` → 400/404/409/422/500 in `route-core.ts`). **BRD traceability:** 6/6 ACs @AC-tagged (10.1×6 · 10.2×6 · 10.3×2 · 10.4×10 · 10.5×5 · 10.6×13). **Elegance Pause** block confirmed in `04a023e`. **L7 walk performed + cited (static verification — migration tab is platform-admin-gated behind `PLATFORM_ADMIN_EMAILS`, force-dynamic):** every interactive element in the "النقل والامتثال (KSA)" tab carries `min-h-[44px]` (≥44px tap target); AA contrast on the two NEW CTAs computed — migrate `bg-brand-amber` #B45309 white **5.02:1** ✓ / hover #92400E **7.09:1** ✓, cutover `bg-indigo-700` #4338CA white **≈7.91:1** ✓; every dynamic mixed-script value (`r.name`, `regionLabel(r.data_region)`, `ctrl.statusLabel`, `ctrl.cta.label`, `owner_email`) `<bdi>`-wrapped (L5); the tab reuses the S8 founder-panel chrome already cleared by a live axe-core walk last sprint. **Punch list: 0 🔴 must-fix, 0 🟡 blocking.** Next: **push `feat/s10-ksa-migration` → confirm CI green via `gh run` (L3 / retro-P1) BEFORE `/5-eo-score story-10`**. Standing (surface, do not work around): **rotate the GitHub PAT** (🔴 #1 risk — pasted plaintext earlier, still live/active for `sukainade-lab`); `.claude/settings.json` SessionStart hook still awaiting explicit approval; **bookkeeping PR #17** open, awaiting founder-named merge authorization; live Contabo cutover (founder-gated: VPS + prod secrets + `supabase db push` incl. 0010+0011+0012); live AWS me-central-1 cutover runbook (founder-gated KSA project).

<!-- prior sprint-6 /3-eo-code footer retained below for history -->
**Prior:** 2026-06-02 · **Current sprint:** 6 (S10 KSA migration) · **Last command:** `/3-eo-code story-10` — **S10 KSA data migration (PDPL) implemented test-first** on `feat/s10-ksa-migration`. All 6 ACs green coded against the L8-enumerated `docs/stories/S10.md`. **Pure core (TDD red→green):** `lib/migration/bundle.ts` (deterministic FK-ordered export + canonical SHA-256 content hash, array order preserved for vectors — 6 tests) · `verify.ts` + `state.ts` (parity comparator + monotonic state machine, no double-cutover — 9) · `run.ts` orchestrator (`runMigration` export→import→verify, `cutoverMigration` confirmed residency flip; `MigrationError` typed codes; verify-before-cutover, non-destructive — 8). **Seam:** `target.ts` `MigrationTarget` (live AWS me-central-1 via lazy `createKsaTarget()` from `KSA_SUPABASE_URL/_SERVICE_KEY`; shared `supabaseBundleReader` for EU source — sibling to S9 Chromium / S6 provider seam). **Routes (L1):** `route-core.ts` shared handlers + `/api/founder/tenants/[id]/migrate` (POST run / GET status) + `/migrate/cutover` — platform-admin gate→404, bad uuid→400, verify_failed→422+diff, already_cutover/not_verified→409, unexpected→500 (11 tests). **UI (L4/L5):** founder-panel "النقل والامتثال (KSA)" tab — reachable migrate/cutover control, `<bdi>` on region codes/status/labels, AA `brand-amber` CTA (white 5.02:1) + indigo cutover CTA. **Migration `0012`** applied **local-only** (`supabase migration up --local`): `tenants.data_region` pointer + `tenant_migrations` run-log + partial-unique one-active index + RLS `tenant_migrations_tenant_isolation` + overview RPC extended with `data_region`+`migration_status`. **Live L2** (`story-10-migration-db.test.ts`, serialized, unique-tenant): proves export pulls ONLY the target tenant across all tables (P0 cross-tenant exclusion) + `tenant_migrations` RLS isolation against real local Supabase (3 tests). `.env.example` + `docs/env-contract.md` carry the two KSA target vars (L6 names-only, no `NEXT_PUBLIC_` on the service key). **Gates green:** tsc ✓ · eslint ✓ · **full suite 476/476 / 57 files** ✓ · `next build` ✓ (`/api/founder/tenants/[id]/migrate` + `/cutover` ƒ) · `npm audit --omit=dev --audit-level=high` = 0 high/critical (2 moderate postcss/next documented). Lessons honored: L1·L2·L4·L5·L6·L8. **Status stays 🔨 coding** until `/4-eo-review`. Next: **`/4-eo-review story-10`** incl. the **L7 walk** (migration tab at 375px + axe under a real platform-admin session). Standing (surface, do not work around): **rotate the GitHub PAT** (🔴 #1 risk — pasted plaintext earlier, still live/active for `sukainade-lab`); `.claude/settings.json` SessionStart hook still awaiting explicit approval; **bookkeeping PR #17** open, awaiting founder-named merge authorization; live Contabo cutover (founder-gated: VPS + prod secrets + `supabase db push` incl. 0010+0011+0012); live AWS me-central-1 cutover runbook (founder-gated KSA project).

**Prior:** 2026-06-02 · **Current sprint:** 6 (opening) · **Last command:** a11y Button-token follow-up **SHIPPED at composite 100** (P10 A10 E10 Q10 U10). The spun-off app-wide accessibility fix that closes the recurring white-on-`#D97706` (3.19:1, fails WCAG-AA) contrast pattern for good. **Systemic, not one-off:** added AA-safe semantic tokens `brand-amber` `#B45309` (white 5.02:1 ✓) + `brand-amber-hover` `#92400E` (7.09:1 ✓) to `tailwind.config.ts`; fixed `src/components/ui/button.tsx` primary variant to the token; migrated **19 call sites** off raw `bg-[#D97706]` / `bg-amber-600` (hex + Tailwind-class forms) across onboarding, auth (5), qa, documents, billing, account-paused, marketing landing+pricing, admin (2), founder-panel. **CI guard minted:** `tests/a11y-button-contrast.test.ts` computes WCAG relative-luminance and fails the build on any future solid-amber-bg + `text-white` pair (proved load-bearing — caught 4 `bg-amber-600` class-form sites my hex grep missed). Commit `30890c9` → PR #16 squash-merged to `main` `9c2e7e4` (founder-authorized: "merge PR #16"); PR #15 (S9 bookkeeping) merged first `39e3463`. **Post-merge trunk gates on `main` `9c2e7e4`:** lint ✓ · tsc ✓ · **430/430 tests / 51 files** ✓ · `next build` ✓ · `npm audit --omit=dev --audit-level=high` = 0 high/critical (2 moderate postcss/next documented). Honors **L9** (fix AA on the story's own CTA before scoring) systemically + **L7** (axe-walk discipline) + **L3** (CI green confirmed before score). Score → `docs/qa-scores/2026-06-02-1505-a11y-button-token.md`; trend.csv has the `shipped-pr16-9c2e7e4` row. **No production deploy run** — live Contabo cutover stays founder-gated. Next: **`/2-eo-dev-plan story-10`** (KSA → AWS me-central-1 / PDPL) to open Sprint 6. Standing (surface, do not work around): **rotate the GitHub PAT** (🔴 #1 risk — pasted plaintext earlier, still live/active for `sukainade-lab`); `.claude/settings.json` SessionStart hook still awaiting explicit approval; live Contabo cutover (founder-gated: VPS + prod secrets + `supabase db push` incl. 0010 + 0011).

<!-- prior sprint-5 /8-eo-retro footer retained below for history -->
**Prior:** 2026-06-02 · **Current sprint:** 5 (CLOSED — retro done) · **Last command:** `/8-eo-retro` — **Sprint 5 retro** (S9 audit export). S9 shipped **90** (P9 A9 E9 Q9 U9); first-pass 88 → one bridge round-trip, **breaking the 2-sprint first-pass streak** (S7 96, S8 94). Flat all-9 hat profile — no weak hat, but UX is the focus because it's the only hat that needed a round-trip and the cause is **structural + recurring**. **Headline: L7 paid off a 2nd consecutive sprint** — the mandated 375px + axe-core walk caught an AA-failing export CTA (white-on-`#D97706` = 3.19:1) that 425 automated tests + static `/4-eo-review` both missed. **L8 adopted + worked** on its first story (AC-9.x enumerated in `docs/stories/S9.md` before coding → its QA cap never bit). L5 `<bdi>` held a **5th sprint** (one h1 slip caught+fixed in review). New **L9** added: *fix AA failures on the story's own primary CTA before `/5-eo-score`; "pre-existing/app-wide" is not a shield* (caps UX at 9) — minted because S8 fixed the contrast inline (UX 9, no round-trip) while S9 deferred the same root cause (UX 8 → bridge). **9 active lessons, 0 archived, 0 pruned** (all <90d). SaaSfast: S9 fits M3, no outlier. Report → `docs/retros/2026-06-02-sprint5-s9.md`. **Next sprint focus:** land the spun-off **app-wide a11y Button-token follow-up** (~10 remaining `#D97706` files → shared AA token) — the systemic fix that retires the recurring contrast pattern for good. Next command: `/2-eo-dev-plan story-10` (KSA → AWS me-central-1 / PDPL) to open Sprint 6, or the a11y follow-up first. Standing (surface, do not work around): **rotate the GitHub PAT** (🔴 #1 risk — pasted plaintext earlier, still live/active for `sukainade-lab`); `.claude/settings.json` SessionStart hook still awaiting explicit approval; **bookkeeping PR #15** open, awaiting founder-named merge authorization; live Contabo cutover (founder-gated: VPS + prod secrets + `supabase db push` incl. 0010 + 0011).

<!-- prior sprint-5 /7-eo-ship footer retained below for history -->
**Prior:** 2026-06-02 · **Current sprint:** 5 (S9 audit export — CLOSED, shipped) · **Last command:** `/7-eo-ship story-9` — **S9 SHIPPED at composite 90.** Founder authorized both merges ("merge PR #13" — already merged `c734128`; "merge PR #14" — squash-merged to `main` `b90c3c9`). `/7-eo-ship` re-scored HEAD `b00c19e` = **90** (P9 A9 E9 Q9 U9, no hat <8). CI green on `b00c19e` (run 26817077242 — quality + live integration) confirmed BEFORE merge (retro P1). **Post-merge trunk gates on `main` `b90c3c9`:** lint ✓ · tsc ✓ · **425/425 tests / 50 files** ✓ · `next build` ✓ · `npm audit --omit=dev --audit-level=high` = 0 high/critical, 2 moderate documented (postcss<8.5.10 XSS via next — carried exception). PR #14 branch `feat/s9-audit-export` merged; local synced to `main`. Score `docs/qa-scores/2026-06-02-1730-S9-audit-export.md`; trend.csv has the 88 + 90 rows. **No production deploy run** — live Contabo cutover stays founder-gated (VPS + prod secrets + `supabase db push` incl. 0010 + 0011). Next: **`/8-eo-retro`** (Sprint 5 close) or `/2-eo-dev-plan story-10`. Standing (surface, do not work around): **rotate the GitHub PAT** (#1 risk — pasted plaintext earlier, still live/active for `sukainade-lab`); `.claude/settings.json` SessionStart hook still awaiting explicit approval; spun-off **app-wide a11y follow-up** (~10 remaining `#D97706` files → shared Button token).

<!-- prior sprint-5 /6-eo-bridge-gaps footer retained below for history -->
**Prior:** 2026-06-02 · **Current sprint:** 5 (S9 audit export) · **Last command:** `/6-eo-bridge-gaps story-9 (UX)` — **S9 bridged 88 → 90, ship-ready.** `/5-eo-score` first pass = **88** (P9 A9 E9 Q9 **U8**); UX capped on merit because the export CTA shipped white-on-`#D97706` = 3.19:1 (fails WCAG-AA 1.4.3) — the app-wide button pattern, but the primary CTA of the S9 surface, unfixed at scoring time. **Bridge (UX):** darkened both /admin CTAs (export S9 + invite S1, same page) to amber-700 `#B45309` (white **5.02:1** ✓; hover `#92400E` **7.09:1** ✓) + added `aria-label="دور العضو"` to the invite role `<select>` (axe `select-name` critical) + tap-target parity (commit `8eef618`). **Verified live** (not just by math): re-ran axe-core wcag2a+wcag2aa on `/admin` at 375px under a real owner session → **0 violations / 24 passes**, computed bg `rgb(180,83,9)`, select name resolves. Re-score → UX 9 → **composite 90** ✅ (no hat <8). CI green on PR #14 (quality + live integration) confirmed BEFORE scoring (retro P1). Score → `docs/qa-scores/2026-06-02-1730-S9-audit-export.md`; trend.csv has both the 88 and 90 rows. The remaining ~10 app-wide `#D97706` files stay in the **spun-off a11y follow-up** (shared Button token). Next: **`/7-eo-ship`** — re-score HEAD + build/test/audit; PR #14 already open → **STOP at the merge gate** (merge needs explicit founder authorization naming the PR). Standing (surface, do not work around): **rotate the GitHub PAT** (pasted plaintext earlier, still live/active for `sukainade-lab`); `.claude/settings.json` SessionStart hook still awaiting explicit approval; live Contabo cutover (founder-gated: VPS + prod secrets + `supabase db push` incl. 0010 + **0011**); **PR #13** still open, awaiting founder-named merge authorization.

<!-- prior sprint-5 /4-eo-review footer retained below for history -->
**Prior:** 2026-06-02 · **Current sprint:** 5 (S9 audit export) · **Last command:** `/4-eo-review story-9` — **S9 review clean → Status flipped 🔨 coding → 🧪 scoring.** Full source review on `feat/s9-audit-export`: no secrets, no `any`, RLS user-scoped, every dynamic value HTML-escaped, filename injection-safe (ASCII slug + digit stamp), error handling present (try/catch → captureError → 500). **2 findings fixed:** 🔴 AC-9.6 was implemented + asserted but no test carried the `@AC-9.6` tag (brd-traceability non-negotiable) → added to 2 route tests, now 6/6 ACs @AC-tagged; 🟡 L5 — report `<h1>` lab name used `esc()` while every other mixed-script value used `<bdi>` → wrapped in `bidi()` + added a mixed-script lab-name test. Tests now **40/40 mocked** (report-html 11→12). Migration 0011 RLS re-confirmed (named `audit_exports_tenant_isolation`). **L7 walk performed + cited:** real owner session (signup MetroLab → login → `/admin`) at 375px → export form owner-reachable (L4 ✓) · tap targets 47–48px (≥44px ✓) · date inputs aria-labeled + `dir="ltr"` (dates are LTR — correct ✓) · axe-core wcag2a+2aa → **2 violations, both pre-existing / NOT S9 regressions**: (1) **color-contrast** white-on-`#D97706` = 3.19:1 — the shared primary-button pattern across **12 files / all shipped stories S1–S8** (my S9 export button merely inherits it; no one-off divergent shade introduced); (2) **select-name** on the S1 invite-form role `<select>`. Both spun off to a dedicated **app-wide a11y follow-up task** (darken token to amber-700 `#B45309` ≈5:1 via a shared Button + label the select) rather than diverging S9. Punch list: 0 remaining 🔴 in the S9 diff. Next: confirm CI green via `gh run` (retro P1) → **`/5-eo-score`**. Standing (surface, do not work around): **rotate the GitHub PAT** (pasted plaintext earlier, still live/active for `sukainade-lab`); `.claude/settings.json` SessionStart hook still awaiting explicit approval; live Contabo cutover (founder-gated: VPS + prod secrets + `supabase db push` incl. 0010 + **0011**); **PR #13** still open, awaiting founder-named merge authorization.

<!-- prior sprint-5 /3-eo-code footer retained below for history -->
**Prior:** 2026-06-02 · **Current sprint:** 5 (S9 audit export) · **Last command:** `/3-eo-code story-9` — **S9 audit-export PDF implemented test-first** on `feat/s9-audit-export` (commit `6738d1c`, 21 files / +2309). All 6 ACs coded against the L8-enumerated `docs/stories/S9.md`: owner/admin-gated `GET /api/audit/export` → A4 RTL PDF (puppeteer-core → system Chromium behind the `renderPdfFromHtml()` seam, mocked in CI), every Q&A row chronological with its citation, inclusive from/to range (reversed→400, omitted→full), `audit_exports` audit row under named RLS (migration 0011), reachable owner/admin admin-page trigger. Dockerfile now ships chromium + Noto Arabic/Latin fonts + `PUPPETEER_EXECUTABLE_PATH` for the Contabo prod render path. Gates green: tsc ✓ · eslint ✓ · **39/39 S9 mocked tests** (full suite 50 files / 424 prior) · `next build` ✓ (`/admin` ƒ, `/api/audit/export` ƒ). Lessons honored: L1 (route-seam per HTTP branch) · L2 (serialized live-DB, unique tenant, P0 cross-tenant exclusion) · L4 (UI→route→file reachable, gate mirrors route) · L5 (`<bdi>` from first write) · L6 (no secret-prefix literals) · L8 (coded to enumerated ACs). **Status stays 🔨 coding** until `/4-eo-review` (the only gate that flips to 🧪 scoring). **L7 walk pending review:** admin export UI is auth+role-gated → 375px + axe-core walk needs a seeded owner/admin session. Next: **`/4-eo-review`** → push + confirm CI green (retro P1) → `/5-eo-score`. Standing (surface, do not work around): **rotate the GitHub PAT** (pasted plaintext earlier, still live/active for `sukainade-lab`); `.claude/settings.json` SessionStart hook still awaiting explicit approval; live Contabo cutover (founder-gated: VPS + prod secrets + `supabase db push` incl. 0010 + **0011**); **PR #13** still open, awaiting founder-named merge authorization.

<!-- prior sprint-4 retro footer retained below for history -->
**Prior:** 2026-06-02 · **Current sprint:** 4 (CLOSED — retro done) · **Last command:** `/8-eo-retro` — **Sprint 4 retro** (S8 founder panel). S8 shipped **94** (P9 A10 E10 Q9 U9), 2nd consecutive zero-round-trip sprint. **Headline: L7 paid off on its first application** — the retro-mandated 375px + axe walk caught a real 15-node WCAG-AA contrast bug that 381 automated tests + static review missed (the self-improvement loop demonstrably converting a lesson into a caught defect one sprint later). Architecture scored a clean un-bridged 10 (first since S5; retro-P1 neutralized the L3 cap, 2nd sprint running). New **L8** added (Phase-2 backlog stories must enumerate AC-N.N criteria before coding — caps QA at 9; supported by S6+S8). 8 active lessons, 0 archived/pruned. Report → `docs/retros/2026-06-02-sprint4-s8.md`. Next: `/2-eo-dev-plan story-9` (audit-export PDF) — **adopt L8 immediately** (enumerate AC-9.x into the BRD before coding). Standing (surface, do not work around): **rotate the GitHub PAT** (pasted plaintext earlier, still live/active for `sukainade-lab`); `.claude/settings.json` SessionStart hook still awaiting explicit approval; live Contabo cutover (founder-gated: VPS + prod secrets + `supabase db push` incl. 0010); **PR #13** (S8 ship bookkeeping) open, awaiting founder-named merge authorization.

<!-- prior sprint-4 ship footer retained below for history -->
**Prior:** 2026-06-02 · sprint 4 · `/7-eo-ship` story-8 — **PR #12 merged to `main`** (`017c5bc`), founder-authorized ("Yes, merge PR #12 to main"). `/5-eo-score` = **composite 94** (P9 A10 E10 Q9 U9, no hat <8); all 7 lesson caps satisfied (none apply). CI green on PR head `50af591` (quality + live integration) confirmed BEFORE merge (retro P1). Post-merge trunk gates on `main`: lint ✓ · tsc ✓ · **381/381 tests** ✓ · `next build` ✓ · `npm audit --omit=dev --audit-level=high` ✓ (0 high/critical prod; 2 moderate documented). Branch deleted. Score → `docs/qa-scores/2026-06-02-1530-S8-founder-panel.md`. Next: **`/8-eo-retro`** (Sprint 4) or `/2-eo-dev-plan story-9`. Standing (surface, do not work around): **rotate the GitHub PAT** (pasted plaintext earlier, still live/active for `sukainade-lab`); `.claude/settings.json` SessionStart hook still awaiting explicit approval; live Contabo cutover (founder-gated: VPS + prod secrets + `supabase db push` incl. 0010). — prior `/3-eo-code` detail: S8 founder super-admin panel implemented test-first on `feat/s8-founder-panel` (commit `b9ece6d`). AC-8.1 env-allowlist gate (`PLATFORM_ADMIN_EMAILS`, no DB role, fails closed, non-admin → 404 on page + every route) · AC-8.2/8.3 one security-definer RPC `founder_tenant_overview` (service_role only) aggregates per-tenant usage server-side, feeds cards + table in one round-trip · AC-8.4 pause/unpause flip `tenants.status`, proxy freezes 'paused' labs → `/account-paused` (scoped to ONLY 'paused', no onboarding regression) · AC-8.5 "mark invoice paid" reuses `activation-core.applyOutcome` with provider 'manual' keyed on stable `manual:<tenant>` (idempotent) · AC-8.6 bad uuid → 400. 31 new tests, **381/381** total green, lint + build clean. `ProviderName` kept to the two real rails; added `PersistedProvider` (= ProviderName | "manual") for the stored row. **L5** `<bdi>` on all dynamic mixed-script values from first implementation; **L4** reuses activation reducer (no parallel path); **L1** route-seam tests; **L2** serialized live-DB suite. Status stays 🔨 coding until `/4-eo-review`. **L7 note:** live 375px + axe-core walk pending `/4-eo-review` (founder page is auth+allowlist-gated → needs a seeded platform-admin session); static a11y/RTL pass done. Next: `/4-eo-review` → push + confirm CI green (retro P1) → `/5-eo-score`. Standing (surface, do not work around): **rotate the GitHub PAT** (pasted plaintext earlier, still live/active for `sukainade-lab`); `.claude/settings.json` SessionStart hook still awaiting explicit approval; live Contabo cutover (founder-gated: VPS + prod secrets + `supabase db push` incl. 0010).

<!-- prior sprint-3 footer retained below for history -->
**Prior:** 2026-06-02 · sprint 3 (closed) · `/8-eo-retro` — **Sprint 3 retro** (S7 + CI-hardening PR #10). S7 shipped at composite **96 with zero bridge or procedural re-score round-trips — a project first**: P1 (confirm CI green *before* `/5-eo-score`) was finally adopted, so the L3 Architecture cap cost nothing and Architecture scored 9 on merit. The recurring shared-DB Kong-502 CI flake (bit S6 bridge + S7 ship) was **fixed** in PR #10 (`vitest --retry=2`, merge `6ee95d7`, both jobs green first-attempt). One repeat miss: automated/CI-only QA verification with no cited manual 375px + axe walk (S6 + S7, same cause) → **new lesson L7** (caps QA at 9 for UI stories lacking a cited manual walk). L1/L2/L4/L5/L6 all honored; L5 (`<bdi>`) held a third sprint with no recurrence. Lessons: 7 active, 0 archived, 0 pruned. Report → `docs/retros/2026-06-02-sprint3-s7.md`. Next: `/2-eo-dev-plan story-8` (Founder super-admin panel) to open Sprint 4. Standing (surface, do not work around): **rotate the GitHub PAT** (pasted plaintext earlier, still live/active for `sukainade-lab`); `.claude/settings.json` SessionStart hook still awaiting explicit approval; live Contabo cutover (founder-gated: VPS + prod secrets + `supabase db push` incl. 0009).

## Retros
- 2026-06-02 — `docs/retros/2026-06-02.md` (Sprint 1 / Weekend MVP S1–S5; 5 PRs shipped; first-pass avg composite 87.6, all bridged to 100; focus hat: UX — repeat `<bdi>` miss → L5; process proposals P1 CI-before-score, P2 ingester regex).
- 2026-06-02 — `docs/retros/2026-06-02-sprint2.md` (Sprint 2 / S6 Tap Payments + PR #6/#7 hardening; 3 PRs shipped; S6 first-pass 88 → shipped 90; loop prevented both chronic Sprint-1 regressions on first pass — L4 kept Product at 9 (no orphaned flow), L5 kept UX at 9 (`<bdi>` from first commit); new L6 = no secret-prefix literals in example/doc files; focus: process P1 CI-before-score to kill the recurring L3 cap).
- 2026-06-02 — `docs/retros/2026-06-02-sprint3-s7.md` (Sprint 3 / S7 SMS 2FA + CI-hardening PR #10; 2 PRs shipped; S7 shipped **96 with zero round-trips — project first**; P1 adopted → L3 cap neutralized, Architecture 9 on merit; recurring Kong-502 CI flake fixed via `vitest --retry=2`; new **L7** = UI stories must cite a manual 375px + axe walk (caps QA at 9), supported by S6+S7; L5 `<bdi>` held a 3rd sprint; 7 active lessons, 0 archived/pruned; focus: L7 manual-walk discipline + formalize P1).
- 2026-06-02 — `docs/retros/2026-06-02-sprint4-s8.md` (Sprint 4 / S8 Founder panel; 1 feature PR #12 + bookkeeping PR #13; S8 shipped **94**, 2nd consecutive zero-round-trip sprint; **headline: L7 paid off on its first application** — the mandated 375px + axe walk caught a real 15-node WCAG-AA contrast bug that 381 automated tests + static review missed; Architecture clean un-bridged 10 (first since S5, retro-P1 neutralized L3 a 2nd sprint); new **L8** = Phase-2 backlog stories must enumerate AC-N.N criteria before coding (caps QA at 9), supported by S6+S8; 8 active lessons, 0 archived/pruned; focus: adopt L8 in S9 planning).
- 2026-06-02 — `docs/retros/2026-06-02-sprint5-s9.md` (Sprint 5 / S9 Audit export PDF; 1 feature PR #14 + bookkeeping PR #15; S9 shipped **90**, first-pass 88 → one bridge round-trip (breaks the 2-sprint first-pass streak); flat all-9 hat profile; **headline: L7 paid off a 2nd consecutive sprint** — the 375px + axe walk caught an AA-failing export CTA (white-on-`#D97706` 3.19:1) that 425 tests + static review missed; **L8 adopted + worked** (AC-9.x enumerated before coding, cap never bit); L5 `<bdi>` held a 5th sprint; new **L9** = fix AA failures on the story's own primary CTA before scoring, "pre-existing" is not a shield (caps UX at 9), supported by S8+S9; 9 active lessons, 0 archived/pruned; focus: land the app-wide a11y Button-token follow-up to retire the recurring `#D97706` contrast pattern).
- 2026-06-02 — `docs/retros/2026-06-02-sprint6-s10.md` (Sprint 6 / S10 KSA data migration PDPL; 2 PRs merged — #17 a11y bookkeeping + #18 S10; S10 shipped **90 first-pass, zero round-trip** — every lesson cap anticipated + satisfied in `/2-eo-dev-plan`/`/3-eo-code`; flat all-9 hat profile vs S9; **headline: L9 paid off 1 sprint after minting** — both new CTAs (migrate brand-amber 5.02:1, cutover indigo-700 ≈7.91:1) AA-verified BEFORE scoring, no contrast bridge; **QA is focus hat, capped at 9 by L7** — gated founder migration tab deferred the live axe walk to static (structural pattern: S8/S9 did live walks on gated surfaces + found real bugs, S10's platform-admin gate made the walk costly → skipped); **no new lesson minted** (L7 already covers it; a duplicate = lesson-bloat) — actionable is **infra not a rule**; 9 active lessons, 0 archived/pruned; clean stacked-branch rebase recovery (`rebase --onto` after #17 squash); SaaSfast M3 fit, no outlier; **focus: build a seeded-session axe-walk harness** so gated-UI stories clear L7 with a real walk — would have lifted S10 QA to 10).

## Reconciliation log
- 2026-06-01 — `/eo-guide`: filesystem matches tracker (all S1–S5 ⬜, no plans, git local without remote). No diff. Phase = `ready-to-plan`.
- 2026-06-01 — `/2-eo-dev-plan story-1`: S1 planned → `docs/handovers/plan-S1-auth.md`. Phase = `ready-to-code`.
- 2026-06-01 — `/3-eo-code` S1: implemented AC-1.1…1.6 test-first. Backend (provision/login/invitations/seat-limits) + RTL auth UI (signup/login/forgot/onboarding + admin invite + logout). 18/18 live+pure tests pass against local Supabase; `next build` + `eslint .` green. RLS promoted to migration `0002_rls_policies.sql`. Resend + Stripe clients made lazy to survive build with empty keys. Phase = `ready-to-review`.
- 2026-06-01 — `/4-eo-review` S1: 35 files reviewed. No secrets, no `any`, 6/6 ACs tagged, RTL classes clean. 🔴 must-fix found+fixed: invite-mode UI signup sent `labName:"—"` (1 char) which failed `signupSchema.min(2)` → every UI invite-signup 400'd; the unit test missed it by calling `provisionSignup` directly. Fixed: schema `superRefine` (labName required only without invite token) + form omits labName on invite + 2 regression tests. 🟡 hardening applied: invite acceptance now bound to invited email. 20/20 tests green. Phase = `ready-to-score`.
- 2026-06-01 — `/5-eo-score` S1: composite **88** (Product 9 / Arch 9 / Eng 9 / QA 8 / UX 9). Report → `docs/qa-scores/2026-06-01-1731-S1-auth.md`; trend.csv started. Below 90 gate → bridge gaps. Lowest hat QA(8): no integration tests at the HTTP route seam (the layer where the labName bug hid). First score <90 + first bug → captured lesson **L1** (test the HTTP seam). Phase = `bridging-gaps`.
- 2026-06-01 — `/6-eo-bridge-gaps qa` S1: added `tests/story-1-auth-routes.test.ts` — 10 route-handler integration tests POSTing to `/api/auth/signup` (400 bad-body, 201 new-lab, 409 duplicate, 201 invite-join, 400 invite-email-mismatch, 402 seat_limit) and `/api/invitations` (401, 403 member, 201 owner, 402 seat_limit). signup runs live; invitations mocks only the cookie-bound auth/role, createInvitation runs live. Also fixed a latent isolation flaw: the AC-1.1 rollback test counted global tenants → now asserts on a unique tenant name (immune to parallel test files). 30/30 tests green; build+lint clean. QA 8→9. Phase = `ready-to-rescore`.
- 2026-06-01 — `/7-eo-ship` S1: final HEAD gate green — 36/36 tests, `eslint .`, `next build` all clean; `npm audit` 0 high/critical in production runtime (dev-only vitest/vite/postcss advisories documented). Score gate ✅ (composite 100, no hat <8). Trunk→PR: pushed `feat/s1-auth`, opened **PR #1**, founder-authorized merge (`--merge`), merge commit `2c93614`; local main fast-forwarded + branch deleted. Status → ✅ shipped. NOTE: production runtime deploy (Contabo VPS + Caddy + PM2) + live `/api/health` are **S5 scope** (Deploy/Observability, ⬜) — S1 ships code to main; the deploy pipeline lands with S5.
- 2026-06-01 — `/3-eo-code` S2: implemented AC-2.1…2.6 test-first. Migration `0003` (status +`indexing`, `document_chunks.section`, private `documents` Storage bucket + path-namespaced RLS, `match_document_chunks` RPC filtering `tenant_id` BEFORE cosine — AC-2.4). Live AC-2.4 vector-isolation test FIRST (2 tests: own-tenant retrieval + Lab A blocked from Lab B even with B's exact embedding, synthetic orthogonal vecs, no OpenAI key). Pure helpers: `lib/documents/chunk.ts` (js-tiktoken cl100k_base, ≤500 tok/50 overlap, page+section per chunk, never blends pages → citation stays page-exact), `lib/documents/limits.ts` (`PLAN_DOC_LIMITS` 50/200, `assertDocAvailable`→`DocLimitError`), `lib/validation/documents.ts` (mime∈{pdf,docx,xlsx}, ≤50MB). Lazy seams: `lib/parsing/llamaparse.ts` (upload→poll→page blocks), `lib/ai/embeddings.ts` (text-embedding-3-small, batched). Orchestrator `lib/documents/ingest.ts` (inline pipeline w/ DB checkpoints parsing→indexing→ready/failed; storage cleanup on failure). Routes `POST/GET /api/documents` + `DELETE /api/documents/[id]` (402 over-cap, 413 oversize, 403 cross-tenant vs 404 missing). RTL `(app)/documents` UI matching `product-demo.jsx` (type tile, status badge, delete, cap line). **L1 satisfied:** 12 route-handler integration tests (seams mocked, Storage+DB live). 66/66 tests green (30 new S2), `eslint .` + `next build` clean. Status stays 🔨 coding until `/4-eo-review`. Phase = `ready-to-review`.
- 2026-06-01 — `/4-eo-review` S2: 18-file diff reviewed (security/RTL/mobile/traceability). No secrets, no `any`, all 6 ACs `@AC`-tagged, elegance pause present, RTL classes clean, tap targets ≥44px. **🔴 must-fix found+fixed:** `/documents` is a new (app)-group route but was missing from `src/proxy.ts` auth matcher → page rendered for unauthenticated users (data still safe via API auth+RLS, but the gate leaked). Matcher now `["/dashboard/:path*","/documents/:path*","/admin/:path*"]`. **🟡 robustness found+fixed:** strict MIME check rejected valid DOCX/XLSX when browsers report `""`/`application/octet-stream` (violates AC-2.1) → added `resolveMime(filename, declaredType)` extension fallback, wired into the POST route; +4 tests (3 `resolveMime` unit + 1 octet-stream-DOCX route → 201). 34 S2 tests (was 30), 70/70 live+pure green, `eslint .` + `next build` clean. Status 🔨→🧪 scoring. Phase = `ready-to-score`.
- 2026-06-01 — `/5-eo-score` S2: composite **86** (Product 8 / Arch 9 / Eng 9 / QA 9 / UX 8) via 5 parallel hat subagents. Report → `docs/qa-scores/2026-06-01-2110-S2-upload.md`; trend.csv appended. **L1 did NOT cap QA** — S2 has 13 route-handler integration tests covering every flow. 80–89 gate → bridge. Two 8s (each +1 = +2 composite; need both to reach 90): **UX 8** (filename lacks `<bdi>`/BiDi isolation → mixed-script SOP names mis-render; cap line dropped plan+usage context — both mechanical), **Product 8** (upload→ready is one blocking spinner; `parsing`/`indexing` UI states never render because POST returns only at `ready` → AC-2.2's visible progression unseen). Eng/Arch each flagged the same root cause (inline pipeline blocks until terminal state). Phase = `bridging-gaps`.
- 2026-06-01 — `/6-eo-bridge-gaps` → "bridge to 10/10" S2: closed every −1 from the 86-run honestly; composite **86 → 100** (10/10/10/10/10). Report → `docs/qa-scores/2026-06-01-2120-S2-upload.md`; trend.csv appended. **Root-cause fix shared by Product+Architecture:** ingestion split into sync `createDocument` (cap+store+row at 'parsing') + background `processDocument` run via Next `after()` — POST returns `201 parsing` instantly (no 120s connection hold), UI polls every 2.5s so the parsing→indexing→ready progression actually renders (AC-2.2). **Engineering (9→10):** `embeddings.length===chunks.length` assert before row map; `setDocumentStatus` throws on failed `.update` (no stuck rows); `deleteDocument` deletes row-first then Storage; `after()` callback `.catch()`es. **QA (9→10):** +2 live failure-path tests (parse-reject + embed-mismatch → doc `failed`, 0 chunks); happy path now asserts both the `parsing` response and the async `ready`. **UX (8→10):** `<bdi>` filename BiDi isolation, enriched cap line (plan + usage %, shown when empty), delete button full ≥44×44 tap target, page count for every ready doc. **Arch (9→10):** vector index ivfflat→HNSW (migration 0003 §5) per stack decision; `npx supabase db reset` re-applies clean. 36 S2 tests (was 34), 72/72 green, eslint + next build clean. Forward note (not yet a lesson): `after()` lacks durable retry — a stranded-row sweep belongs in S5 observability or a v2 hardening story. Phase = `ready-to-ship`.
- 2026-06-01 — `/2-eo-dev-plan story-3`: S3 planned → `docs/handovers/plan-S3-qa.md`. Approach: reuse `match_document_chunks` RPC (user-scoped client, not admin) + `embedTexts`; migration 0004 aligns `queries` columns to AC-3.7 (rename question/answer → question_text/answer_text, add question_lang + found_answer) and extends the RPC to return `document_filename` for the citation (AC-3.4); new seams `lib/qa/{lang,prompt,citations,ask}.ts` + `lib/ai/answer.ts` (lazy GPT-4o-mini); `POST /api/qa`; `(app)/qa` UI matching product-demo.jsx + `/qa` added to proxy matcher (S2 lesson). **P0 safety (AC-3.5):** LLM gated behind non-empty retrieval — a test asserts `generateAnswer` is NOT called when zero chunks clear 0.75. L1 honored via route-handler tests (found/not-found/400/401). Phase = `ready-to-code`.
- 2026-06-01 — `/3-eo-code` S3: implemented AC-3.1…3.7 test-first. Migration `0004_qa.sql` aligns `queries` to AC-3.7 (rename `question`/`answer`→`question_text`/`answer_text`, add `question_lang`, `found_answer not null default false`) and re-creates `match_document_chunks` to also return `document_filename` (join `documents`) for the citation (AC-3.4). New seams: `lib/qa/lang.ts` (Arabic-script detection → any Arabic present = "ar"), `lib/qa/prompt.ts` (`NOT_FOUND_AR` sentinel + per-lang refusal, `buildSystemPrompt` grounding contract, `formatContext`, `isNotFoundAnswer`), `lib/qa/citations.ts` (dedupe by doc+page, order preserved), `lib/qa/types.ts`, `lib/ai/answer.ts` (lazy GPT-4o-mini, temp 0), orchestrator `lib/qa/ask.ts`. **P0 (AC-3.5) structurally enforced:** the model is reached ONLY when retrieval returns ≥1 chunk; zero chunks → `NOT_FOUND[lang]`, no LLM call. Route `POST /api/qa` (user-scoped client so `current_tenant_id()` resolves; 401/400/500). RTL `(app)/qa` UI matching `product-demo.jsx` (textarea `dir="auto"`, "🔍 ابحث في وثائقك", citation badge 📄 doc — الصفحة N shown only when found, answer dir follows lang, refusal never styled as a confident answer), `/qa` added to nav + `proxy.ts` matcher (S2 lesson). **L1 honored:** `tests/story-3-qa-routes.test.ts` (7 route-handler tests: 401/400×2/401-no-tenant/200-found/200-not-found/500). Live orchestrator `tests/story-3-qa.test.ts` (2 tests: grounded match → answer+citation+logged; **P0 not-found → `generateAnswer` NOT called**) runs end-to-end against local Supabase (only OpenAI seams mocked). Pure units `tests/story-3-qa-helpers.test.ts` (11). Also fixed `tests/story-1-auth.test.ts` seed (`question`→`question_text`) broken by 0004. 92/92 live+pure tests green (20 new S3), `eslint .` + `next build` clean, `npx supabase db reset` re-applies 0001→0004 clean. Status stays 🔨 coding until `/4-eo-review`. Phase = `ready-to-review`.
- 2026-06-01 — `/5-eo-score` S3: composite **88** (Product 9 / Arch 9 / Eng 8 / QA 9 / UX 9) via 5 parallel hat subagents. Report → `docs/qa-scores/2026-06-01-2204-S3-qa.md`; trend.csv appended. **L1 cap LIFTED** for S3 (route-handler tests present). 80–89 gate → bridge. Lowest **Eng 8**: `detectLang` first-Arabic-wins misclassifies a mostly-English question carrying one Arabic term → wrong answer/refusal language (AC-3.1/3.5) — needs majority-script detection. Other −1s: Arch (drop+recreate drops EXECUTE grant, works only by PUBLIC default), QA (Q&A-path cross-tenant isolation not re-proven live), UX (citation meta contrast <AA + nav tap targets), Product (no empty-corpus guidance / non-actionable citation). Phase = `bridging-gaps`.
- 2026-06-01 — `/4-eo-review` S3: 17-file diff reviewed (security/RTL/mobile/traceability/SaaSfast-mode). No secrets, no `any`, all 7 ACs traced (AC-3.6 visual-only → covered by RTL review; root `lang="ar" dir="rtl"` + IBM Plex Arabic confirmed; page adds per-lang `dir` + `<bdi>`), elegance pause present, tap targets ≥44px, migration 0004 keeps the P0 tenant-before-cosine filter + `security definer`/`search_path`. **🟡 found+fixed (correctness, AC-3.5/3.6):** the zero-chunk path localized the refusal (`NOT_FOUND[lang]`) but the model-refusal-with-chunks path (AC-3.3) returned the model's raw output — and the grounding prompt always dictates the *Arabic* sentinel, so an English user with off-topic excerpts got an Arabic refusal. Fixed in `ask.ts` (`if (!found) answer = NOT_FOUND[lang]`) + regression test (EN question, chunks retrieved, model emits sentinel → answer === `NOT_FOUND.en`, model still ran once, citations []). 21 S3 tests (was 20), 93/93 live+pure green, `eslint .` clean. Status 🔨→🧪 scoring. Phase = `ready-to-score`.
- 2026-06-01 — `/7-eo-ship` S2: final HEAD gate green — 72/72 tests, `eslint .`, `next build` all clean; production-runtime `npm audit` 0 high/critical (lone critical = dev-only `vitest`, confirmed via `--omit=dev`; 2 moderate = postcss bundled in Next, fix needs a breaking Next downgrade — documented, same as S1). Score gate ✅ (composite 100, no hat <8). **Flake observed (not a regression):** one S1 route test (`@AC-1.4` invite-email-mismatch) failed at invitation INSERT under parallel vitest workers, then passed on isolated re-run AND on the clean full re-run — transient local-Supabase hiccup; forward note → capture as L2 + add retry/serialization guard. Trunk→PR: pushed `feat/s2-upload`, opened **PR #2**, founder-authorized merge (`--merge --delete-branch`), merge commit `22f6b30`; local main fast-forwarded + branch deleted. Status → ✅ shipped. NOTE: production runtime deploy (Contabo VPS + Caddy + PM2) + live `/api/health` remain **S5 scope** — S2 ships code to main.
- 2026-06-01 — "bridge to 10/10" S3 (`/6-eo-bridge-gaps` → `/5-eo-score` recompute): closed every −1 from the 88-run honestly; composite **88 → 100** (10/10/10/10/10). Report → `docs/qa-scores/2026-06-01-2220-S3-qa.md`; trend.csv appended. **Engineering (8→10):** rewrote `detectLang` to majority-script-by-word with Arabic-first tiebreak (`src/lib/qa/lang.ts`) — fixes the real AC-3.1/3.5 bug (mostly-English question + one Arabic word was misclassified ar); helpers test pins the three regression cases (Arabic+EN-terms ⇒ ar, mostly-EN+1 AR word ⇒ en, bare technical EN ⇒ en). **Architecture (9→10):** appended explicit `grant execute on function public.match_document_chunks(vector(1536), int, float) to authenticated` to migration 0004 (no PUBLIC-default reliance); `db reset` re-applies clean. **QA (9→10):** added live cross-tenant P0 test (Lab A querying Lab B's *exact* embedding → `found=false`, model not called — tenant filter beats cosine 1.0) + live empty-corpus test (Lab C zero docs → `emptyCorpus=true`, model not called); existing miss test now asserts `emptyCorpus=false`. **UX (9→10):** citation meta → `text-slate-300` (WCAG AA); extracted `components/layout/nav-links.tsx` (client) — nav links ≥44px tap targets + active-route highlight via `aria-current`; dropped dead `.bidi-term` CSS. **Product (9→10):** `ask()` now returns `emptyCorpus` (cheap ready-doc head count only on the miss path) so a brand-new lab is nudged to upload instead of getting a generic refusal; citation badges are click-through to `/documents?doc=ID` which highlights + scrolls the exact source file (read via lazy initializer to keep the page static). Route tests updated for the new field. 96/96 live+pure tests green (was 93; +1 isolation, +1 empty-corpus, +1 detectLang regression), `eslint .` + `next build` + `npx supabase db reset` clean; `npm audit --omit=dev --audit-level=high` 0 high/critical (2 moderate postcss-via-next, documented). Status 🩹→🧪 scoring. Phase = `ready-to-ship`.
- 2026-06-01 — `/7-eo-ship` S3: founder-authorized merge of **PR #3** (`feat/s3-qa` → `main`), merge commit **`1d3af51`**; local main fast-forwarded, branch deleted. Final pre-merge state: CI green (run `26778448921` — Install/Lint/Test/Build/Audit ✓), composite **100** (no hat <8), `.bidi-term` regression fixed on-branch (`bb5d51f`) so the merge does NOT strip BiDi isolation. Branch carried 7 commits: `c0724ef` (S3 feature) → `035f669` (review fix) → `bb767ed` (88 score) → `55d59e2` (bridge→100) → `fd96333` (CI gate fix) → `bb5d51f` (bidi regression fix) → `28bd04a` (lessons/backlog/log). Status → ✅ shipped. NOTE: production runtime deploy (Contabo VPS + Caddy + PM2) + live `/api/health` remain **S5 scope** — S3 ships code to main. Next: `/2-eo-dev-plan story-4`.
- 2026-06-01 — **solution-architect pass #2** (S4-readiness review + regression catch): reviewed the S4 Stripe scaffold, subscriptions schema, and the live QA P0 path (`ask.ts` clean — model gated behind retrieval, refusal normalized to lang, audit-log throws). **Caught a real regression in PR #3:** the S3 bridge deleted `.bidi-term` from `globals.css` claiming "dead CSS", but it's referenced by 6 live components (auth signup/forgot, onboarding, landing, pricing) — merging PR #3 would strip BiDi isolation from English technical terms inside Arabic (incl. S1 auth, already shipped). CI never caught it (no CSS-reference validation). **Restored the rule** (`unicode-bidi: isolate; direction: ltr;`) in `src/app/globals.css` with a do-not-remove comment; `eslint .` + `next build` clean. Uncommitted — must land on `feat/s3-qa` before PR #3 merges. Also logged S4 design gaps for `/2-eo-dev-plan story-4`: checkout creates duplicate Stripe customers (reuse `stripe_customer_id`), webhook needs idempotent upsert on `stripe_subscription_id`, define `tenants.status` as the access gate vs `subscriptions.status` Stripe mirror, AC drift (annual −25% + Request-Invoice fallback not scaffolded), `APP_URL` prod env (S5/env-contract).
- 2026-06-01 — **solution-architect pass** (pre-merge hardening, PR #3 held for founder merge): root-caused a **red CI Quality Gate** that S1+S2 had merged through unnoticed — `ci.yml` Audit step audited *all* deps (`npm audit --audit-level=high`) and died on a dev-only esbuild/vitest critical that never ships, and the Test step soft-failed (`|| echo`) so tests couldn't block a merge. Fixed both → Audit `--omit=dev --audit-level=high` (production-runtime convention) + hard `npm run test`; verified locally (audit `--omit=dev` exit 0 vs all-deps exit 1; CI-sim test 44 pass/64 skip/exit 0); committed `fd96333`, pushed `feat/s3-qa`. New CI run **success** (Install/Lint/Test/Build/Audit all ✓). Captured **L2** (live DB suites must be serialized — vitest parallelizes files but all live suites share one Supabase → seed/cleanup races; CI live job must use `--no-file-parallelism`) and **L3** (verify the actual CI run is green via `gh run list`; never assume local gates == CI). Designed the S5 CI live-DB integration job (`supabase start` + serialized live run) and recorded it as a backlog spec → `docs/backlog/S5-ci-integration-db.md` (deferred to S5 per BRD sequence; out-of-sequence now would conflict ci.yml with PR #3). GitHub-issue creation for this was classifier-denied (external write) → captured locally instead. No change to S3 score/status; PR #3 remains ship-ready and held.
- 2026-06-01 — `/2-eo-dev-plan story-4`: S4 planned → `docs/handovers/plan-S4-pricing.md`; branch `feat/s4-pricing` cut off `main` (carries the pending S3-shipped bookkeeping for committing on-branch, not on main). Approach: migration `0005_billing.sql` (add `subscriptions.plan` + `price_interval`; tenant-self read RLS); AC-4.1 pricing page fixes the wrong limits (copy says 100 docs/1 user/unlimited/10 users → correct to 5u·50d / 20u·200d) + monthly⇄annual −25% toggle + `<bdi>JOD</bdi>`; AC-4.2 `lib/payment/stripe/prices.ts` typed env price map + harden `createCheckoutSession` to reuse `stripe_customer_id` + `POST /api/checkout` + `POST /api/invoice-request` (bank-transfer fallback → Resend to founder); AC-4.3 webhook 3 handlers idempotent (upsert on `stripe_subscription_id`): completed→active+activation email, deleted→inactive, payment_failed→past_due; AC-4.4 `sendWelcomeEmail` from S1 provisioning; AC-4.5 dashboard real counters (user-scoped). **L1** honored (route tests for /api/checkout + /api/invoice-request + webhook). **L2** honored (webhook live DB test serialized). Price IDs are founder config → `.env.example` + env-contract TODO placeholders. Out of scope: PostHog `invoice_requested`→S5, super-admin→S8. Phase = `ready-to-code`.
- 2026-06-01 — `/3-eo-code` S4: implemented AC-4.1…4.5 test-first on `feat/s4-pricing`. Migration `0005_billing.sql` adds `subscriptions.plan` + `price_interval` (the `subscriptions_select_own` read RLS already existed in 0002). **AC-4.1:** pricing logic extracted to pure `lib/pricing/plans.ts` (Starter 35 / Pro 70 JOD, annual −25%, limits sourced from `PLAN_SEAT_LIMITS`/`PLAN_DOC_LIMITS` — single source of truth, no stale "100 docs/unlimited" copy); `(marketing)/pricing` rebuilt as a client component with monthly⇄annual toggle (`aria-pressed`, ≥44px) + `<bdi>JOD</bdi>` + Request-Invoice link. **AC-4.2:** `lib/payment/stripe/prices.ts` (typed env price map, throws on missing id), `createCheckoutSession` hardened to reuse `stripe_customer_id` (no duplicate Stripe customers) + carry `plan`/`interval` in session metadata, `POST /api/checkout` (401/400/500), plus bank-transfer fallback `POST /api/invoice-request` + `lib/validation/billing.ts` + `sendInvoiceRequestEmail` + `components/billing/invoice-request-form.tsx` + `(marketing)/invoice-request`. **AC-4.3:** webhook core extracted to `lib/payment/stripe/activation.ts` (`handleStripeEvent`): `checkout.session.completed`→tenant active + plan recorded + idempotent find-then-write subscription upsert keyed on the partial-unique `stripe_subscription_id` (NOT PostgREST on-conflict) + owner activation email; `customer.subscription.deleted`→inactive/canceled; `invoice.payment_failed`→past_due/past_due. `tenants.status` = access gate, `subscriptions.status` = Stripe mirror. **AC-4.4:** `sendWelcomeEmail` (lab name, admin name, 3 onboarding steps, demo link) wired into `provisionSignup` best-effort (swallows errors — never fails a good signup). **AC-4.5:** `lib/dashboard/stats.ts` (`getDashboardStats` reuses the same count/limit helpers the gates use; pure `monthStartISO` for the "this month" window) + `GET /api/dashboard` + dashboard page real counters. **L1 honored:** route-seam tests for checkout (7), invoice (5), webhook (5), dashboard (3). **L2 honored:** live DB suites (webhook activation 6, dashboard 3) seed unique tenants, run serialized. 133/133 live+pure tests green (7 S5 skip), `eslint .` + `next build` + `npx supabase db reset` (0001→0005) clean. env-contract + .env.example updated (STRIPE_PRICE_*_MONTH/YEAR, INVOICE_REQUEST_TO, DEMO_VIDEO_URL). Status stays 🔨 coding until `/4-eo-review`. Phase = `ready-to-review`.
- 2026-06-01 — `/4-eo-review` S4: 30-file diff reviewed (commits `a5f2dc9`+`b7eccbf` on `feat/s4-pricing`; security / RTL / mobile / @AC-traceability / SaaSfast-mode). **No 🔴 must-fix.** No secrets, no `any` types. All 5 ACs traced — 34 `@AC-4.x` occurrences across 8 test files (4.1 pricing units; 4.2 checkout+invoice route seams; 4.3 webhook route seam + live activation/idempotency; 4.4 welcome-email content + wired into `provisionSignup`; 4.5 dashboard route seam + live counts + pure month-window). Elegance-pause block present in `b7eccbf`. UI clean RTL: pricing page (`<bdi>JOD</bdi>`, `aria-pressed` toggle, `role="group"`, `ms-2` logical prop, ≥44px buttons), invoice-request form (`dir="ltr"` on email, ≥44px submit), dashboard page (logical layout, brand tokens, loading "…"/"—" states). SaaSfast-M3 + Stripe consistent — the only `tap`/fallback grep hits are a "tap target" mobile comment and the documented Tap/bank-transfer fallback note in `stripe/index.ts` (founder override), no cross-mode leakage. Full suite green: 133 passed / 7 S5-skip; `eslint .` + `next build` + `npx supabase db reset` (0001→0005) all clean. 🟢 nice-to-have (deferred, not blocking): dashboard fetch has no visible error toast on 500 (silently shows "—"); `monthStartISO` uses UTC not Asia/Amman (documented MVP-acceptable). Status 🔨→🧪 scoring. Phase = `ready-to-score`.
- 2026-06-01 — "bridge to 10/10" S1: closed every remaining −1 honestly. **Product:** `/api/auth/resend` route + signup success-state "أعد الإرسال" button (AC-1.2). **Arch/Eng:** extracted `lib/auth/seats.ts` (single source of truth for seat accounting — `getPlanLimit`+`countSeats`; both create/accept paths use it) and added `confirm` route `token_hash`/`verifyOtp` fallback for the OTP/magic-link shape. **QA:** route tests 10→16 (added login 200/401/400, forgot 200×2, logout 200 — server seam mock now delegates `signInWithPassword`/`resetPasswordForEmail`/`signOut` to a real anon client, so credentials are checked for real); lesson L1 cap marked satisfied for S1. **UX:** real 375px audit (Claude Preview) across signup/login/forgot — RTL, zero h-overflow, inputs 49px@16px (no iOS zoom), CTA 48px. 36/36 tests green; `eslint .` + `next build` clean. `/5-eo-score` re-run → composite **100** (10/10/10/10/10). Report → `docs/qa-scores/2026-06-01-1755-S1-auth.md`. Phase = `ready-to-ship`.
- 2026-06-01 — `/5-eo-score` S4: composite **80** (Product 6 / Arch 8 / Eng 9 / QA 9 / UX 8) via 5 parallel hat subagents. Report → `docs/qa-scores/2026-06-01-2341-S4-pricing.md`; trend.csv appended. **🔴 Product 6 (dead-end):** AC-4.2's Stripe money loop was unreachable from the UI — the pricing CTA still linked to `/signup` and nothing in the app POSTed to `/api/checkout` (verified by grep: no caller; signup ignored plan/interval). Money loop existed file-by-file but had no user-reachable path. Secondary −1s: **Arch 8** (subscription upsert was find-then-write — a TOCTOU window under concurrent Stripe retries), **UX 8** (dashboard counters lacked `<bdi>` BiDi isolation + no fetch-error state), QA/Eng minor. 80–89 gate → bridge. Phase = `bridging-gaps`.
- 2026-06-01 — "bridge to 10/10" S4 (`/6-eo-bridge-gaps` → `/5-eo-score` recompute): closed the 🔴 + every secondary −1; composite **80 → 96** (P9/A10/E10/Q10/U9). Report → `docs/qa-scores/2026-06-01-2353-S4-pricing.md`; trend.csv appended. **Product (6→9):** extracted pure `lib/payment/checkout-client.ts` (`startCheckout` — node-testable, no jsdom) and wired the pricing CTA to it: button → `POST /api/checkout` → Stripe redirect; 401 → `/signup?plan=&interval=`; failure → inline `role="alert"`. 5 new `@AC-4.2` unit tests pin the wiring. The money loop is now genuinely end-to-end. (residual −1: signup doesn't yet consume the carried plan/interval — Phase-2.) **Architecture (8→10):** migration `0006_subscription_upsert.sql` — atomic `upsert_subscription` RPC (`INSERT … ON CONFLICT` on the partial unique index, which PostgREST can't target); `activation.ts` now calls the RPC, closing the find-then-write TOCTOU window. **Engineering (9→10):** `lib/observability/log.ts` `captureError` seam (single S5/Sentry wire-in point); checkout/dashboard/webhook catch blocks now report instead of swallow; `InvoiceWithSubscription` narrowing removes an inline cast. **QA (9→10):** webhook route `it.each` test forwards `customer.subscription.deleted` + `invoice.payment_failed` verbatim. **UX (8→10→9):** dashboard counters + plan badge wrapped in `<bdi>`; fetch-error `role="alert"` panel + `min-h-[44px]` retry button (residual −1: error states color-only / no `aria-busy` — Phase-2). 140/140 live+pure tests green (7 S5-skip), `eslint .` + `next build` + `npx supabase db reset` (0001→0006) clean. Verified the UX agent's `<\b>` welcome-email flag = **false positive** (line 76 is correctly `<b>…</b>`). Captured lesson **L4**: `/4-eo-review` must trace the user-reachable end-to-end path, not only audit files in isolation. Bridge committed `f9b2777`. Status 🩹→🧪 scoring. Phase = `ready-to-ship`.
- 2026-06-02 — `/7-eo-ship` S4 (paused at merge boundary): final HEAD gate green — 140/140 tests, `eslint .`, `next build` clean; `npm audit --omit=dev --audit-level=high` 0 high/critical (2 moderate postcss-via-next, documented same as S1–S3). Score gate ✅ (composite **96**, no hat <8). Pushed `feat/s4-pricing`, opened **PR #4**. **L3 satisfied:** CI Quality Gate **green** on PR #4 (run `26781684535` — Install/Lint/Test/Build/Audit all ✓). The autonomous `gh pr merge` was **classifier-denied** (merging to `main` is a founder-authorized boundary) — correctly stopped, awaiting explicit founder merge approval. NOT yet shipped. Standing (surface, do not work around): **rotate the GitHub PAT** (pasted plaintext earlier, still live/active for `sukainade-lab`); `.claude/settings.json` SessionStart hook still awaiting explicit approval.
- 2026-06-02 — "bridge to 10/10" S4 (`/6-eo-bridge-gaps` → `/5-eo-score` recompute): closed the two residual −1s from the 96-run honestly; composite **96 → 100** (10/10/10/10/10). Report → `docs/qa-scores/2026-06-02-0008-S4-pricing.md`; trend.csv appended. **Product (9→10):** the carried plan choice now survives the full logged-out round-trip — new pure `lib/payment/resume.ts` (`parseResume` allow-list / `onboardingNext` / `resumeCheckoutHref` / `checkoutCtaState`) threads `plan`+`interval` through pricing CTA → `/signup?plan=&interval=` → signup POST → route `parseResume` → `provisionSignup` sets the confirmation-email `next` to `/onboarding?plan=&interval=` → onboarding "أكمل الاشتراك في خطة X" → `/pricing?plan=&interval=` (interval pre-selected + chosen card `ring-2` highlighted) → Stripe. Off-list params rejected before they reach any redirect (no open-redirect echo). **UX (9→10):** the pricing CTA binding is now unit-tested without a DOM (pure `checkoutCtaState` → label/busy/disabled); `aria-busy` added to pricing CTAs + dashboard grid; both error regions carry a ⚠️ non-color signal alongside `role="alert"`. `/pricing` stays static (○) via a `useState` lazy initializer over `window.location.search` (no `useSearchParams`). +14 tests (`story-4-resume.test.ts` ×12 + 2 live signup-route tests asserting carried `pro/year` → `next=/onboarding?plan=pro&interval=year` and off-list → plain `/onboarding`). 154/154 live+pure tests green (7 S5-skip), `eslint .` + `next build` clean. Bridge committed `6aa49b9`, pushed; CI re-confirmed green (run `26782414324` ✓). Status stays 🧪 scoring — ⏳ awaiting founder-authorized merge of PR #4.
- 2026-06-02 — `/2-eo-dev-plan story-5`: S5 (last Weekend-MVP story) planned → `docs/handovers/plan-S5-deploy.md`; branch `feat/s5-deploy` cut off `main` (carries the pending S4-shipped tracker bookkeeping for committing on-branch, not on main). Approach: AC-5.1 health route-test (already implemented); AC-5.5 pure typed PostHog event builders (`signup_completed`/`document_uploaded`/`question_asked{found_answer,lang}`/`invoice_requested`) unit-tested for **zero PII** + lazy `posthog-node` server seam wired to the 4 real route call-sites (L4 end-to-end); AC-5.4 `instrumentation.ts` + Sentry client init, `captureError` seam → `Sentry.captureException`, `setSentryTenant` helper (no CI auth token); AC-5.7 live `pg_policies` seed-test asserting RLS-enabled + named policy on all 7 tables; AC-5.2/5.3 `Dockerfile`+`docker-compose.yml`+`Caddyfile`+`deploy.sh`+README runbook (config verified; **live VPS deploy founder-gated** — no server/domain/SSH here); AC-5.6 `.env.example` completeness test + env-contract docs; CI `integration` job per `docs/backlog/S5-ci-integration-db.md` (`supabase start` + serialized `--no-file-parallelism` run, L2/L3, watch 3 green). All four lessons apply. Phase = `ready-to-code`.
- 2026-06-02 — `/7-eo-ship` S4: founder-authorized merge of **PR #4** (`feat/s4-pricing` → `main`), merge commit **`d562f19`**; local main fast-forwarded, branch deleted (local + remote). Final pre-merge state: CI green (run `26782414324` — Install/Lint/Test/Build/Audit ✓), composite **100** (no hat <8), 154 tests / 7 S5-skip. Branch carried 3 commits past plan/bookkeeping: `b7eccbf` (S4 feature) → `f9b2777` (80→96 bridge) → `6aa49b9` (96→100 bridge). Status → ✅ shipped. NOTE: production runtime deploy (Contabo VPS + Caddy + PM2) + live `/api/health` remain **S5 scope** — S4 ships code to main. **Weekend MVP now 4/5 stories shipped (S1–S4); only S5 (Deploy/Observability) remains.** Next: `/2-eo-dev-plan story-5`. Standing (surface, do not work around): **rotate the GitHub PAT** (pasted plaintext earlier, still live/active for `sukainade-lab`); `.claude/settings.json` SessionStart hook still awaiting explicit approval.
- 2026-06-02 — `/3-eo-code` → `/4-eo-review` → `/5-eo-score` S5 (AC-5.1…5.7) on `feat/s5-deploy`: implemented test-first. **AC-5.1** health route test; **AC-5.4** `instrumentation-client.ts` browser Sentry (DSN-guarded), `captureError → reportException` seam, `setSentryTenant`; **AC-5.5** PII-free PostHog event builders wired into all 4 real handlers (signup/documents/qa/invoice) via a dependency-free `fetch` server seam (no posthog-node) — L4 end-to-end; **AC-5.6** `.env.example` + `env-contract.md` completeness test; **AC-5.7** live RLS proof on all 7 tenant tables via service-role-only `rls_policy_report()` RPC (migration `0007`, since no `pg` pkg / PostgREST can't read `pg_policies`); **AC-5.2/5.3** Dockerfile + docker-compose + Caddyfile + deploy.sh + README runbook (config-verified; **live Contabo cutover founder-gated**). CI `integration` job added (live-DB, `supabase start` + serialized `--no-file-parallelism`, L2/L3). All four lessons satisfied. 231/231 tests green, `eslint .` + `next build` clean, `npm audit --omit=dev --audit-level=high` exit 0 (moderate postcss-via-next, documented). `/4-eo-review` clean (no 🔴; one fix: added NODE_ENV row to env-contract.md → env-contract test 44/44). `/5-eo-score` composite **96** (P10/A8/E10/Q10/U10) — Arch held at 8 by **L3** (CI-green-on-PR not yet confirmed). Commits: `e736b4b` (feature) → `63c8f70` (review clean) → `6aa4927` (score 96). Phase = `ready-to-ship`.
- 2026-06-02 — `/7-eo-ship` S5: pushed `feat/s5-deploy`, opened **PR #5**; both CI jobs **green** — `quality` + the new live-DB `integration` (run `26784034388`, confirmed via `gh run view --json jobs`). **L3 cap lifted: Architecture 8→10 → composite 100** (10/10/10/10/10); re-score committed `03c0fbf`, report `docs/qa-scores/2026-06-02-0041-S5-deploy.md` updated. **Founder go-ahead received** → merged PR #5 (`--merge --delete-branch`), merge commit **`9bc1af3`**; local main fast-forwarded, branch deleted. Status → ✅ shipped. trend.csv: `shipped-pr5-9bc1af3`. **🎉 Weekend MVP complete — all 5 stories (S1–S5) shipped 100/100.** Deferred (founder-gated): **live Contabo cutover** (VPS provision + prod secrets, then `./deploy.sh` + live `/api/health` 200). Standing (surface, do not work around): **rotate the GitHub PAT** (pasted plaintext earlier, still live/active for `sukainade-lab`); `.claude/settings.json` SessionStart hook still awaiting explicit approval. Next: live cutover (founder-gated) or `/8-eo-retro`.
- 2026-06-02 — `/7-eo-ship` S6: **founder-authorized merge** ("merge PR 6, PR 7, and PR 8 in order, then run /7-eo-ship"). Confirmed all three CLEAN/MERGEABLE + CI-green (runs `26784996746`/`26785544209`/`26788116534` all success), then merged in order via `gh pr merge --merge --delete-branch`: **PR #6** deploy-hardening → `69f7e72`, **PR #7** qa-refusal-hardening → `5e1c329`, **PR #8** S6 Tap Payments → `8a80179`. Each re-checked MERGEABLE before the next (no doc-only conflicts materialized — the 4 shared bookkeeping files merged clean). Local `main` fast-forwarded to `8a80179`. Post-merge trunk gates: `npx tsc --noEmit` clean · `npm test` **287/287** (33 files; count grew 270→287 with #6/#7 tests now on trunk) · `npm run build` green (all routes incl. `/api/webhooks/tap`) · `npm audit --audit-level=high` documented (lone critical = dev-only `vitest` UI-server advisory, not in prod bundle; 6 moderate = `next`/`postcss` build chain, breaking Next downgrade declined — same exceptions accepted on S1–S5). Score gate ✅ composite **90** (P9 A9 E9 Q9 U9, no hat <8). Status → ✅ shipped (code on `main`). **Live production deploy NOT run — founder-gated:** `deploy.sh` runs on the Contabo VPS and requires a provisioned VPS + prod `.env` (`LABBRAIN_DOMAIN`) + prod Supabase migrated (`supabase db push`, incl. 0008) + `MIGRATIONS_APPLIED=1`; none exist here (only `APP_URL=http://localhost:3000`). No live `/api/health` check possible until cutover. trend.csv: `shipped-pr8-8a80179`. **🎉 Merge gate fully cleared — zero open PRs.** Standing (surface, do not work around): **rotate the GitHub PAT** (pasted plaintext earlier, still live/active for `sukainade-lab`); `.claude/settings.json` SessionStart hook still awaiting explicit approval. Next: `/8-eo-retro` (Sprint 2) or `/2-eo-dev-plan story-7`.
- 2026-06-02 — `/3-eo-code` S7 (SMS 2FA via Unifonic, AC-7.1…7.6) on `feat/s7-sms-2fa`: implemented test-first, committed `b2be4ea` (28 files, +1989). **Migration `0009_mfa.sql`:** `users.phone`/`mfa_enabled`/`phone_verified_at` + `mfa_challenges` table (RLS **on, no policies** → service-role only; AC-7.5 fail-closed, proven by a live test where even the owner's signed-in session reads 0 rows). **Pure helpers (TDD red→green):** `lib/auth/phone.ts` (Jordan mobile → E.164 `+9627XXXXXXXX`, `toUnifonicRecipient` strips `+`; AC-7.1), `lib/auth/otp.ts` (HMAC-SHA256 hash, constant-time `compareOtp` via `timingSafeEqual`, `evaluateVerify` fail-closed order consumed→attempts-cap→expiry→compare, 5-min TTL / 5-attempt cap / 60s cooldown / 5-per-hour budget; AC-7.2/7.5), `lib/auth/mfa-cookie.ts` (HMAC-signed `lb_mfa` elevation marker, `verifyMfaCookie` never throws; AC-7.3/7.4). **Service layer `lib/auth/mfa.ts`:** `issueChallenge` (cooldown + send-budget checks, generate→hash→insert→SMS, **deletes the row if send fails** so no dead challenge), `verifyChallenge` (newest live challenge; ok→consume, wrong→increment; missing→"expired"), `verifyErrorMessage` localized AR. **Unifonic seam `lib/sms/unifonic.ts`:** native `fetch` (no SDK), form-encoded AppSid/SenderID/Body/Recipient/responseType=JSON, **fails closed** on non-2xx OR `success:false`; AR OTP body. **4 routes** `/api/auth/2fa/{enroll,send,verify,disable}` (400/401/429/200 per branch). **Login routing** (`login/route.ts` + `lib/auth/login.ts` now returns `userId`): 2FA user → `next=/login/verify` + login OTP issued; plain user → `/dashboard`. **Proxy gate** (`src/proxy.ts`): a `mfa_enabled` user must also carry a valid matching `lb_mfa` cookie or be redirected to `/login/verify` — Supabase session alone is not enough (AC-7.4, the security core of S7); dropped the now-disallowed `runtime:"nodejs"` config key (Next 16 Proxy always runs on Node). **RTL UI (L5):** `(auth)/login/verify` form + `components/auth/verify-form.tsx`, and account `(app)/settings` page + `components/auth/mfa-settings.tsx` (enroll/disable, shared code-entry stage) — `<bdi>` on phone/digits, ≥44px targets, `aria-live`; `/settings` added to nav + proxy matcher. **env (L6):** `.env.example` + `docs/env-contract.md` add `UNIFONIC_API_KEY`/`UNIFONIC_SENDER_ID`/`MFA_COOKIE_SECRET` (names/placeholder shapes only, no real prefixes); `MFA_COOKIE_SECRET` generated into gitignored `.env.local`. **Tests (all green):** L1 route-seam per HTTP branch (`story-7-2fa-routes` 18), L4 login+proxy-gate trace (`story-7-2fa-gate` 8), L2 live `mfa_challenges` DB + RLS + lifecycle serialized (`story-7-mfa-db` 6), Unifonic seam (6), phone/otp/cookie units (25) = **63 new; 350/350 total**. tsc clean (fixed test-only typing: generic `postJson<T>` for NextResponse cookies + typed fetch-mock tuple), `next build` green (all S7 routes present), `npx supabase db reset` 0001→0009 clean. Status stays 🔨 coding until `/4-eo-review`. Phase = `ready-to-review`. Next: `/4-eo-review` (then push + confirm CI green per retro P1 before `/5-eo-score`).
- 2026-06-02 — `/4-eo-review` S7 (`feat/s7-sms-2fa` vs `main`, 30-file diff): **clean — no 🔴 must-fix.** **Traceability:** 6/6 ACs covered (AC-7.1 phone+enroll, 7.2 otp+unifonic+send, 7.3 verify, 7.4 cookie+proxy-gate, 7.5 rate-limit+RLS, 7.6 disable) — 61 `@AC-7.x` tags / 63 new tests / **350 total green**. **Security pass:** no secrets, no `any` in `src/`; `mfa_challenges` RLS-on-no-policies = service-role only (proven live: owner's own session reads 0 rows); constant-time OTP compare (`timingSafeEqual`); SMS seam fails closed (non-2xx OR `success:false`); proxy gate requires a *matching* `lb_mfa` cookie (another user's cookie → redirect, tested); disable requires a fresh OTP (no silent walk-up disable); enroll never flips `mfa_enabled` on save, only after verify. **L4 (user-reachable path):** login-form redirects to `data.next` → `/login/verify` for 2FA users; `/settings` reachable via nav link — no orphaned routes. **L5 (RTL):** verify-form + mfa-settings use `<bdi>` on phone/digits, `dir="ltr"` on code/phone inputs, ≥44px targets, `aria-live` regions, `role="alert"` with ⚠️ non-color signal. **L6:** no secret-prefix literals in `.env.example`/`env-contract.md`/`tests` for S7 (placeholders only; `MFA_COOKIE_SECRET` generated into gitignored `.env.local`). **SaaSfast M3** consistent (no cross-mode leakage). 🟢 deferred non-blocking: mfa-settings updates `enabled` optimistically (no re-fetch); login OTP issued best-effort with result swallowed (prior code stays valid). Status 🔨→🧪 scoring. Phase = `ready-to-score`. Next: push `feat/s7-sms-2fa` + confirm CI green (retro P1) → `/5-eo-score`.
- 2026-06-02 — **retro P1 + `/5-eo-score` S7:** pushed `feat/s7-sms-2fa`, opened **PR #9**. CI doesn't run on a feature-branch push (workflow triggers on PR + push-to-main), so the PR is what triggers it — **both jobs green** (`quality` + live `integration`, run `26808384508`, confirmed via `gh run view --json jobs`). retro P1 honored: CI confirmed green BEFORE scoring. **5-hat score (parallel subagents):** Product **10** (6/6 ACs traced, scoped, L4 paths reachable, strong MENA fit), Architecture **9** (clean module separation + fail-closed seam; −1 no `mfa_challenges` TTL/pruning note), Engineering **10** (fail-closed SMS, `timingSafeEqual`, RLS-no-policies, Elegance Pause, L6 clean), QA **9** (L1 route-seam + L2 serialized live suite both satisfied; −1 automated-only verification), UX **10** (RTL + `<bdi>` + ≥44px + aria-live). **No lesson caps** — L1–L6 all satisfied. Composite **(10+9+10+9+10)×2 = 96 ✅ Ship** (≥90, no hat <8). One UX defect surfaced by scoring (verify-form error was color-only) **fixed during scoring** → added `⚠️` non-color signal matching mfa-settings (UX 9→10); re-ran gates clean (tsc 0, **350/350 tests**). Report `docs/qa-scores/2026-06-02-1144-S7-sms-2fa.md`; trend.csv `ship-ci-green-pr9-awaiting-merge`. Status stays 🧪 scoring until merge. Phase = `ready-to-ship`. Next: `/7-eo-ship` — **STOP at merge gate: founder must authorize merging PR #9 by name** (no auto-merge to main). Standing (surface, do not work around): **rotate the GitHub PAT** (pasted plaintext earlier, still live/active for `sukainade-lab`); `.claude/settings.json` SessionStart hook still awaiting explicit approval.
- 2026-06-02 — `/7-eo-ship` S7: **founder-authorized merge** of **PR #9** (`feat/s7-sms-2fa` → `main`). Pre-merge: PR state CLEAN/MERGEABLE, composite **96** (no hat <8). The post-score commit (`a60c5d5`, verify-form ⚠️ fix) re-triggered CI and hit **one transient failure** on the live `integration` job — `Unknown Error: An invalid response was received from the upstream server` (a Supabase/Kong 502) on the **unrelated S2** test `@AC-2.3 embedding count mismatch` (349/350 passed; my commit only touched `verify-form.tsx` + docs; same test passed on the prior run f08e951). Diagnosed as L2-class shared-DB flakiness, **not a regression** → re-ran the failed job (`gh run rerun --failed`) → both jobs green. Merged via `gh pr merge 9 --merge --delete-branch`, merge commit **`5985c80`**; local main fast-forwarded, branch deleted. Post-merge trunk gates: `tsc --noEmit` clean · `npm test` **350/350** (40 files). Status → ✅ shipped. trend.csv: `shipped-pr9-5985c80`. **Live production deploy NOT run — founder-gated** (Contabo VPS + prod secrets + `supabase db push` incl. 0009; only `APP_URL=localhost` exists). **Architect decision (founder-approved this session):** the recurring live-DB CI flake (transient Kong 502) gates every ship via retro P1, so fix it **now as its own small isolated PR** — `chore(ci): vitest run --retry=2` on the integration job (90/10 fix; root-cause Kong health-wait kept as backlog if flakes persist) — **not** deferred to a "last phase" and **not** folded into S7. Phase = `ready-to-harden-ci`. Next: open the CI-retry PR (separate branch off `main`), then `/8-eo-retro` (Sprint 3) or `/2-eo-dev-plan story-8`.
- 2026-06-02 — `/2-eo-dev-plan story-6` (first Phase-2 story): S6 (Tap Payments card integration, JOD/KWD/SAR) planned → `docs/handovers/plan-S6-tap-payments.md`. BRD lists S6 as a one-line v2 row, so the plan **defines** its ACs (AC-6.1…6.6). Two architect decisions resolved: (1) **Tap = primary JOD/Gulf rail behind a `pickProvider(currency)` router** — both providers implement one `PaymentProvider` interface, Stripe refactored behind it with **zero behavior change** (all S4 tests stay green), JOD/KWD/SAR → Tap else Stripe; (2) **per-interval charge (MVP)** — one Tap HPP charge activates the tenant, webhook flips status; Tap recurring/auto-renew carved to an S6 follow-up. Approach: extract DB effects from `stripe/activation.ts` into provider-agnostic `payment/activation-core.ts` (both webhooks call it); new `src/lib/payment/tap/` on **native `fetch` + node `crypto`** for HMAC (no axios — consistent with dependency-free posthog-server); server-side provider switch in `/api/checkout` (pricing page already POSTs there → minimal UI churn); `src/lib/pricing/currency.ts` (decimals JOD/KWD=3, SAR=2; `amountFor(plan,interval,currency)`; KWD/SAR price points config-gated, no FX guessing); migration `0008_tap_payments.sql` (provider-neutral columns `provider`/`provider_customer_id`/`provider_subscription_id`/`currency` + provider-aware atomic `upsert_subscription` RPC, RLS unchanged). Lessons L1 (route tests `/api/checkout` Tap branch + `/api/webhooks/tap`), L2 (serialized live activation test), L4 (CTA→router→Tap HPP→webhook→activation end-to-end), L5 (`<bdi>` on currency/amount) all apply. **Risk to verify first:** Tap's exact webhook signature/hashstring scheme against Tap API docs before implementing the verifier (security-critical). Out of scope (S6 follow-ups): Tap recurring, live KWD/SAR price points, refunds. Branch `feat/s6-tap-payments` to be cut off `main`. Phase = `ready-to-code`. Next: `/3-eo-code`.
- 2026-06-02 — `/4-eo-review` S8 (`feat/s8-founder-panel` vs `main`, 23-file diff, commits `b9ece6d`+`326d48d`): **static review clean** — no secrets (only grep hit a pre-existing comment in `tap/index.ts`, outside the diff), no `any`/`as any` in any new `src/` file, all 6 ACs traced (25 `@AC-8.x` tags / 5 test files), elegance-pause present (`b9ece6d`). **SaaSfast M3** consistent; **L1** route-seam tests per branch, **L2** serialized live-DB suite, **L4** manual activation reuses `activation-core.applyOutcome` (no parallel path), **L5** `<bdi>` on every dynamic value. **L7 live walk (the discipline S7 retro added):** seeded a confirmed `founder@labbrain.test` platform-admin session + demo tenants (1 active Pro, 1 inactive Starter) → signed in → `/founder` at **375px** via Claude Preview. Panel rendered correct (4 cards: active=1, MRR **70 د.أ**, pending=1, questions=4; both tenant rows w/ status+plan pills; invoice queue=1). **axe-core (wcag2a+wcag2aa) found 1 🔴 must-fix:** color-contrast, **15 nodes** — muted `text-slate-500` (#64748b, 3.0–3.8:1) on dark cards/table + white-on-amber `#D97706` (3.18:1) logo/active-tab/activate-CTA. **Fixed** in `founder-panel.tsx` (commit `6ae39f1`): `text-slate-500→text-slate-400` (≥5.4:1), `#D97706→#B45309` amber-700 (≥4.9:1). **Re-ran axe → 20 passes / 0 violations.** Post-fix gates: **381/381** tests · `eslint .` · `next build` all clean. No other 🔴/🟡; 🟢 deferred (app-wide): the white-on-`#D97706` brand token also under-contrasts on login/pricing CTAs (pre-existing, not S8 — candidate for a token-contrast follow-up). Status 🔨→🧪 scoring. Phase = `ready-to-score`. Next: push `feat/s8-founder-panel` + confirm CI green (retro P1) → `/5-eo-score`.
